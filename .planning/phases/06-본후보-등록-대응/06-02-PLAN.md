---
phase: 06-본후보-등록-대응
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - js/tabs/candidate-tab.js
autonomous: false
requirements: [CAND-02, CAND-03]

must_haves:
  truths:
    - "5/15 18:00 이후 후보 탭에 NOMINATED 후보만 표시되고 DECLARED/EXPECTED/RUMORED 후보는 숨겨진다"
    - "5/15 18:00 이후 후보가 기호순(ballotNumber)으로 정렬된다"
    - "등록 취소(WITHDRAWN) 후보가 governor/superintendent/mayor 분기 모두에서 제거된다"
    - "buildModel()의 .map()에서 ballotNumber 필드가 프론트엔드로 전달된다"
    - "지도에서 선거구 클릭 시 후보 탭의 NOMINATED 필터가 적용되어 최신 등록 후보가 표시된다 (SC-4 달성 — map.js 수정 불필요. 지도 클릭 → 후보 탭 렌더링 흐름에서 Plan 02 필터가 자동 적용됨)"
  artifacts:
    - path: "js/tabs/candidate-tab.js"
      provides: "NOMINATED 필터, WITHDRAWN 필터, ballotNumber 전달"
      contains: "NOMINATED"
  key_links:
    - from: "js/tabs/candidate-tab.js render()"
      to: "ElectionCalendar.getCandidateSortMode()"
      via: "sortMode === 'ballot_number' 조건 분기"
      pattern: "getCandidateSortMode"
    - from: "js/tabs/candidate-tab.js buildModel()"
      to: "data/candidates/*.json"
      via: "ElectionData.getRegion/getSuperintendentData/getMayorData"
      pattern: "ballotNumber"
---

<objective>
후보 탭(candidate-tab.js)에 NOMINATED 전용 필터와 WITHDRAWN 제거 로직을 추가하여, 5/15 18:00 이후 공식 후보만 기호순으로 표시한다.

Purpose: 본후보 등록 마감 후 비공식 후보(DECLARED/EXPECTED/RUMORED)를 자동으로 숨기고, 등록 취소 후보도 전 선거 유형에서 제거한다 (CAND-02, CAND-03 달성).
Output: candidate-tab.js에 NOMINATED 필터 + WITHDRAWN 필터 + ballotNumber 전달 + 미확정 선거구 안내 문구
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-본후보-등록-대응/06-CONTEXT.md
@.planning/phases/06-본후보-등록-대응/06-RESEARCH.md

@js/tabs/candidate-tab.js
@js/election-calendar.js

<interfaces>
<!-- election-calendar.js에서 사용할 함수 -->

From js/election-calendar.js (line 128-131):
```javascript
const getCandidateSortMode = () => {
    const now = getKST();
    // CANDIDATE_REG_END = new Date('2026-05-15T18:00:00+09:00')
    return now > DATES.CANDIDATE_REG_END ? 'ballot_number' : 'status_priority';
};
```

<!-- candidate-tab.js 현재 구조 (변경 대상) -->

From js/tabs/candidate-tab.js:
```javascript
// buildModel() governor 분기 (line 92-104):
// .map()에 ballotNumber 필드 없음 — 추가 필요 (Pitfall 4)

// buildModel() superintendent 분기 (line 122-136):
// .map()에 ballotNumber 필드 없음 — 추가 필요

// buildModel() mayor 분기 (line 165-175):
// .map()에 status, ballotNumber 필드 없음 — 추가 필요

// render() (line 277-278):
// sortMode === 'ballot_number' 분기에서 NOMINATED 필터 없음 — 추가 필요
// WITHDRAWN 필터는 byElection(line 57)에만 있고 다른 분기에 없음 — 추가 필요
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: buildModel() ballotNumber/status 전달 + render() NOMINATED/WITHDRAWN 필터 추가</name>
  <files>js/tabs/candidate-tab.js</files>
  <action>
**변경 1: buildModel() governor 분기 (line 92~104) — ballotNumber 전달**
기존 `.map()` 내부에 `ballotNumber: candidate.ballotNumber || null` 추가.
현재 코드 (line 92~):
```javascript
candidates: govCandidates.map((candidate) => ({
    name: candidate.name,
    badgeLabel: ...,
    badgeColor: ...,
    age: candidate.age,
    career: candidate.career,
    pledges: ...,
    status: candidate.status,
    statusMeta: getStatusMeta(candidate.status),
    incumbent: incumbentName === candidate.name
})),
```
→ `incumbent` 다음 줄에 `ballotNumber: candidate.ballotNumber || null` 추가.

**변경 2: buildModel() superintendent 분기 (line 122~136) — ballotNumber 전달**
동일하게 `.map()` 내부에 `ballotNumber: candidate.ballotNumber || null` 추가.
`incumbent` 다음 줄에 추가.

**변경 3: buildModel() mayor 분기 (line 165~175) — status + ballotNumber 전달**
현재 mayor 분기의 `.map()`에는 `status` 필드가 없다. 추가해야 한다:
```javascript
candidates: candidates.map((candidate) => ({
    name: candidate.name,
    badgeLabel: ...,
    badgeColor: ...,
    age: candidate.age,
    career: candidate.career,
    pledges: ...,
    status: candidate.status,              // 추가
    statusMeta: getStatusMeta(candidate.status),  // 추가
    ballotNumber: candidate.ballotNumber || null,  // 추가
    incumbent: districtSummary?.mayor?.name === candidate.name
})),
```

**변경 4: render() 함수 — NOMINATED 필터 + WITHDRAWN 필터 (line 277~)**
현재 코드:
```javascript
if (sortMode === 'ballot_number') {
    model.candidates.sort((a, b) => (a.ballotNumber || 999) - (b.ballotNumber || 999));
}
```
→ 변경:
```javascript
if (sortMode === 'ballot_number') {
    // D-07: 본후보 등록 마감 후 NOMINATED만 표시
    // D-09: WITHDRAWN 제거 (per D-08: getCandidateSortMode() 기준 활용)
    model.candidates = model.candidates.filter(c => c.status === 'NOMINATED');
    // D-10: ballotNumber 없는 NOMINATED 후보는 999 폴백 (기존 로직 유지)
    model.candidates.sort((a, b) => (a.ballotNumber || 999) - (b.ballotNumber || 999));
} else {
    // status_priority 모드에서도 WITHDRAWN 필터 추가 (CAND-03)
    // 기존 byElection 분기(line 57)에만 있던 WITHDRAWN 필터를 전체 선거 유형으로 확대
    model.candidates = model.candidates.filter(c => c.status !== 'WITHDRAWN');
    const statusOrder = { NOMINATED: 0, DECLARED: 1, EXPECTED: 2, RUMORED: 3 };
    model.candidates.sort((a, b) => {
        const sa = statusOrder[a.status] ?? 2.5;
        const sb = statusOrder[b.status] ?? 2.5;
        return sa - sb;
    });
}
```
이 변경으로:
- ballot_number 모드: NOMINATED만 표시 (DECLARED/EXPECTED/RUMORED/WITHDRAWN 모두 제거) — D-07 충족
- status_priority 모드: WITHDRAWN만 제거 (나머지는 모두 표시) — CAND-03 충족
- 단일 변경점(render)에서 처리 — buildModel() 각 분기에 중복 필터 추가하지 않음 (Anti-Pattern 회피)

**변경 5: 선거구 미확정 안내 문구 (D-14)**
mayor 분기의 buildModel()에서, 후보가 0명인 경우의 `emptyMessage`를 sortMode에 따라 분기:
- `render()` 함수 내에서, `model.candidates.length === 0`이고 `sortMode === 'ballot_number'`이고 `electionType === 'mayor'`일 때:
  emptyMessage를 `"공식 후보 등록 마감 후 선거구 확정 중입니다"` (per D-14)로 교체.
- 구현 위치: render() 함수의 `if (!model.candidates.length)` 블록(line 287~) 직전에:
  ```javascript
  if (!model.candidates.length && sortMode === 'ballot_number' && electionType === 'mayor') {
      model.emptyMessage = '공식 후보 등록 마감 후 선거구 확정 중입니다';
  }
  ```

주의사항:
- candidate-tab.js만 수정한다. 다른 탭 파일, map.js, app.js 등은 건드리지 않는다 (CLAUDE.md 수정 범위 규칙).
- `statusOrder` 객체에서 `WITHDRAWN: 4`를 제거한다 (이미 filter로 제거되므로 불필요).
- byElection 분기(line 57)의 기존 WITHDRAWN 필터는 그대로 유지한다 (buildModel 내부 처리).
  </action>
  <verify>
    <automated>cd /Users/isawufo/Desktop/AI-cording-project/korea-local-election && grep -n "ballotNumber" js/tabs/candidate-tab.js | head -10 && grep -n "NOMINATED" js/tabs/candidate-tab.js | head -10 && grep -c "WITHDRAWN" js/tabs/candidate-tab.js && grep -A 25 "electionType === 'mayor'" js/tabs/candidate-tab.js | grep "status:"</automated>
  </verify>
  <done>
    - governor/superintendent/mayor buildModel() 분기 모두에서 ballotNumber 필드가 전달됨
    - mayor buildModel()에서 status, statusMeta 필드도 전달됨
    - render()의 ballot_number 모드에서 NOMINATED 필터 적용 (D-07)
    - render()의 status_priority 모드에서 WITHDRAWN 필터 적용 (CAND-03)
    - 선거구 미확정 안내 문구 표시 (D-14)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 후보 탭 정렬 전환 수동 UAT</name>
  <what-built>
    candidate-tab.js에 NOMINATED 필터, WITHDRAWN 필터, ballotNumber 전달, 선거구 미확정 안내 문구를 추가했습니다.
  </what-built>
  <how-to-verify>
    **사전 준비:** `python -m http.server 8000` 실행 후 Chrome에서 `http://localhost:8000` 접속

    **테스트 1: 현재 상태 (5/15 18:00 이전 — status_priority 모드)**
    1. 서울 클릭 → 후보 탭 확인
    2. 확인: DECLARED/EXPECTED/RUMORED 후보가 상태 우선순위로 정렬됨
    3. 확인: WITHDRAWN 상태 후보가 있었다면 목록에서 제거됨 (CAND-03)

    **테스트 2: ballot_number 모드 시뮬레이션**
    1. Chrome DevTools Console에서 실행:
       ```javascript
       // getCandidateSortMode()를 임시로 ballot_number 반환하도록 오버라이드
       ElectionCalendar._origGetCandidateSortMode = ElectionCalendar.getCandidateSortMode;
       ElectionCalendar.getCandidateSortMode = () => 'ballot_number';
       ```
    2. 서울 클릭 → 후보 탭 확인
    3. 확인: NOMINATED 후보만 표시되고 DECLARED/EXPECTED/RUMORED 후보가 사라짐 (D-07)
    4. 확인: 후보가 기호순으로 정렬됨 (ballotNumber가 있는 경우)

    **테스트 3: 기초단체장 선거구 미확정 안내 문구 (D-14)**
    1. ballot_number 모드 유지 상태에서
    2. 기초단체장 후보가 0명인 시군구를 클릭 (또는 임의의 시군구)
    3. 확인: "공식 후보 등록 마감 후 선거구 확정 중입니다" 안내 문구 표시

    **테스트 4: 복원**
    1. Console에서 실행:
       ```javascript
       ElectionCalendar.getCandidateSortMode = ElectionCalendar._origGetCandidateSortMode;
       ```
    2. 서울 클릭 → 원래 상태로 복원 확인
  </how-to-verify>
  <resume-signal>"approved" 또는 발견된 문제 설명</resume-signal>
</task>

</tasks>

<verification>
1. `grep -c "ballotNumber" js/tabs/candidate-tab.js` — 4개 이상 (governor/superintendent/mayor .map() + render sort)
2. `grep -c "NOMINATED" js/tabs/candidate-tab.js` — 3개 이상 (filter + statusOrder + getStatusMeta)
3. `grep "WITHDRAWN" js/tabs/candidate-tab.js` — render()의 status_priority 분기에 filter 존재
4. 브라우저 수동 UAT 통과 (Task 2)
</verification>

<success_criteria>
- CAND-02: 5/15 18:00 이후 기호순 정렬이 ballotNumber 필드를 기반으로 올바르게 동작함
- CAND-03: WITHDRAWN 후보가 governor/superintendent/mayor 모든 분기에서 제거됨
- D-07: ballot_number 모드에서 NOMINATED 이외 후보가 숨겨짐
- D-14: 기초단체장 미확정 선거구에 안내 문구 표시
- 브라우저 UAT 통과
</success_criteria>

<output>
After completion, create `.planning/phases/06-본후보-등록-대응/06-02-SUMMARY.md`
</output>
