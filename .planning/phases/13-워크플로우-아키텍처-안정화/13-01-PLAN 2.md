---
phase: 13-워크플로우-아키텍처-안정화
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/update-byelection.yml
  - .github/workflows/update-polls.yml
  - .github/workflows/update-overview.yml
  - .github/workflows/update-gallup.yml
  - .github/workflows/update-election-stats.yml
  - .github/workflows/update-governor-status.yml
  - .github/workflows/update-local-council.yml
  - .github/workflows/update-local-media.yml
  - .github/workflows/update-mayor-status.yml
  - .github/workflows/update-superintendent-status.yml
  - .github/workflows/data-health-check.yml
  - .github/workflows/fetch-disclosures.yml
  - .github/workflows/monitor-failures.yml
  - .github/workflows/update-candidates.yml
autonomous: true
requirements:
  - GIT-01
  - INDEP-01

must_haves:
  truths:
    - "동일 워크플로우가 동시에 두 번 실행될 때 하나가 대기 후 순차 실행된다"
    - "외부 API 호출 스텝이 실패해도 커밋·푸시 단계까지 도달한다"
    - "monitor-failures.yml의 핵심 스텝에는 continue-on-error가 적용되지 않는다"
    - "diff/commit/push 스텝에는 continue-on-error가 적용되지 않는다"
  artifacts:
    - path: ".github/workflows/update-byelection.yml"
      provides: "concurrency + continue-on-error on 3 steps"
      contains: "concurrency"
    - path: ".github/workflows/update-polls.yml"
      provides: "concurrency + continue-on-error on 2 steps"
      contains: "concurrency"
    - path: ".github/workflows/monitor-failures.yml"
      provides: "concurrency only (no continue-on-error)"
      contains: "concurrency"
  key_links:
    - from: "all 14 workflows"
      to: "GitHub Actions scheduler"
      via: "concurrency group"
      pattern: "concurrency:\\s+group:"
    - from: "API/scraping steps"
      to: "commit step"
      via: "continue-on-error: true"
      pattern: "continue-on-error: true"
---

<objective>
14개 워크플로우 전체에 concurrency 블록을 추가하고(GIT-01), monitor-failures.yml을 제외한 13개 워크플로우의 외부 의존 스텝에 continue-on-error: true를 적용한다(INDEP-01).

Purpose: 동시 실행 시 git push 경쟁 상태를 제거하고, 일시적 API 장애가 전체 파이프라인을 중단하지 않도록 한다.
Output: 14개 수정된 워크플로우 YAML 파일
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-워크플로우-아키텍처-안정화/13-RESEARCH.md

<interfaces>
<!-- 기존 update-candidates.yml의 패턴을 참조 — 이 파일은 이미 완성된 참조 모델 -->

concurrency 블록 위치 (on: 블록과 jobs: 블록 사이):
```yaml
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
```

continue-on-error 적용 패턴 (외부 의존 스텝에만):
```yaml
      - name: Step N - 설명
        continue-on-error: true
        run: python scripts/xxx.py
```

적용 금지 스텝: actions/checkout, actions/setup-python, Install dependencies, Check for changes (id: diff), Commit and push
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 14개 워크플로우에 concurrency 블록 일괄 추가 (GIT-01)</name>
  <files>
    .github/workflows/update-byelection.yml,
    .github/workflows/update-polls.yml,
    .github/workflows/update-overview.yml,
    .github/workflows/update-gallup.yml,
    .github/workflows/update-election-stats.yml,
    .github/workflows/update-governor-status.yml,
    .github/workflows/update-local-council.yml,
    .github/workflows/update-local-media.yml,
    .github/workflows/update-mayor-status.yml,
    .github/workflows/update-superintendent-status.yml,
    .github/workflows/data-health-check.yml,
    .github/workflows/fetch-disclosures.yml,
    .github/workflows/monitor-failures.yml,
    .github/workflows/update-candidates.yml
  </files>
  <read_first>
    .github/workflows/update-candidates.yml (참조 패턴 — concurrency가 이미 없음을 확인),
    .github/workflows/update-byelection.yml (변경 대상 현재 상태),
    .planning/phases/13-워크플로우-아키텍처-안정화/13-RESEARCH.md (패턴 1: concurrency 블록 위치)
  </read_first>
  <action>
14개 워크플로우 YAML 파일 각각에 concurrency 블록을 추가한다.

위치: `on:` 블록과 `jobs:` 블록 사이 (빈 줄로 구분).

추가할 내용 (모든 파일 동일):
```yaml
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
```

주의사항:
- update-overview.yml은 `on: workflow_call`을 포함하지만 concurrency 설정은 동일하게 적용 (cron 실행과 연쇄 호출이 직렬화되는 것이 원하는 동작)
- YAML 들여쓰기 정확하게 유지 (최상단 레벨, 들여쓰기 없음)
- 기존 `on:` 블록의 내용은 절대 변경하지 않음
  </action>
  <verify>
    <automated>cd /Users/isawufo/Desktop/AI-cording-project/korea-local-election && COUNT=$(grep -l "concurrency" .github/workflows/*.yml | wc -l | tr -d ' ') && echo "concurrency 적용: ${COUNT}/14" && python3 -c "import yaml; [yaml.safe_load(open(f'.github/workflows/{f}')) for f in __import__('os').listdir('.github/workflows') if f.endswith('.yml')]; print('YAML 파싱 전체 통과')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "concurrency" .github/workflows/*.yml | wc -l 결과가 14
    - 모든 14개 YAML 파일이 python yaml.safe_load()로 파싱 성공
    - grep "cancel-in-progress: false" .github/workflows/*.yml | wc -l 결과가 14
  </acceptance_criteria>
  <done>14개 워크플로우 전체에 concurrency 블록이 추가되어 동일 워크플로우 중복 실행이 직렬화된다</done>
</task>

<task type="auto">
  <name>Task 2: 13개 워크플로우에 continue-on-error 적용 (INDEP-01)</name>
  <files>
    .github/workflows/update-byelection.yml,
    .github/workflows/update-polls.yml,
    .github/workflows/update-overview.yml,
    .github/workflows/update-gallup.yml,
    .github/workflows/update-election-stats.yml,
    .github/workflows/update-governor-status.yml,
    .github/workflows/update-local-council.yml,
    .github/workflows/update-local-media.yml,
    .github/workflows/update-mayor-status.yml,
    .github/workflows/update-superintendent-status.yml,
    .github/workflows/data-health-check.yml,
    .github/workflows/fetch-disclosures.yml,
    .github/workflows/update-candidates.yml
  </files>
  <read_first>
    .github/workflows/update-candidates.yml (이미 적용된 패턴 확인 — factcheck 계열 9개 스텝),
    .github/workflows/update-byelection.yml (3개 스텝: detect/fetch/factcheck — 모두 미적용),
    .github/workflows/update-polls.yml (2개 스텝: NESDC pipeline/reparse — 모두 미적용),
    .github/workflows/update-overview.yml (4개 독립 스텝),
    .planning/phases/13-워크플로우-아키텍처-안정화/13-RESEARCH.md (패턴 2: continue-on-error 적용 원칙 + 14개 워크플로우 현황 상세 표)
  </read_first>
  <action>
13개 워크플로우(monitor-failures.yml 제외)의 외부 의존 스텝에 `continue-on-error: true`를 추가한다.

적용 원칙:
- 적용 대상: 외부 API 호출, Claude API 호출, 웹 스크래핑, Python 스크립트 실행 스텝
- 적용 금지: actions/checkout, actions/setup-python, Install dependencies, Check for changes (id: diff), Commit and push
- monitor-failures.yml: 적용 제외 (실패 감지 자체가 핵심 동작이므로)

워크플로우별 적용 대상:

1. **update-byelection.yml** — 3개 스텝:
   - "Step 1 - Detect new districts" → continue-on-error: true
   - "Step 2 - Fetch & update candidates" → continue-on-error: true
   - "Step 3 - Factcheck candidates" → continue-on-error: true

2. **update-polls.yml** — 2개 스텝:
   - "Run NESDC pipeline" → continue-on-error: true
   - "Reparse empty results" → continue-on-error: true

3. **update-overview.yml** — 4개 스텝 (각각 독립 Python 스크립트 실행):
   - 각 Python 스크립트 실행 스텝에 continue-on-error: true 추가

4. **단일 스크립트 워크플로우 (8개):**
   update-gallup.yml, update-election-stats.yml, update-governor-status.yml,
   update-local-council.yml, update-local-media.yml, update-mayor-status.yml,
   update-superintendent-status.yml, data-health-check.yml, fetch-disclosures.yml
   - 각 파일의 핵심 Python 스크립트 실행 스텝(1개씩)에 continue-on-error: true 추가
   - "Install dependencies" 스텝에는 적용하지 않음

5. **update-candidates.yml** — 이미 factcheck 계열 9개 스텝 적용됨.
   - Open Question 2 확인: Step 0~0.5 스텝(detect byelection news, detect byelection districts, sync winners, fetch NEC official candidates) 중 외부 API 의존 스텝에도 continue-on-error: true 추가 (5/14 이전 NEC API가 empty response 반환 시 실패 방지)
   - 각 스텝을 읽고 외부 API 호출 여부를 확인 후 적용 판단

`continue-on-error: true`는 해당 스텝의 `run:` 라인 바로 위에 위치 (name 라인 다음).
  </action>
  <verify>
    <automated>cd /Users/isawufo/Desktop/AI-cording-project/korea-local-election && echo "=== continue-on-error 적용 현황 ===" && for f in .github/workflows/*.yml; do NAME=$(basename "$f"); COUNT=$(grep -c "continue-on-error" "$f" 2>/dev/null || echo 0); echo "$NAME: $COUNT개"; done && echo "=== monitor-failures.yml 확인 ===" && grep -c "continue-on-error" .github/workflows/monitor-failures.yml && echo "(0이어야 정상)" && echo "=== YAML 파싱 검증 ===" && python3 -c "import yaml; [yaml.safe_load(open(f'.github/workflows/{f}')) for f in __import__('os').listdir('.github/workflows') if f.endswith('.yml')]; print('전체 YAML 파싱 통과')"</automated>
  </verify>
  <acceptance_criteria>
    - update-byelection.yml에 continue-on-error가 3개 이상 존재 (grep -c "continue-on-error" 결과 >= 3)
    - update-polls.yml에 continue-on-error가 2개 이상 존재
    - update-overview.yml에 continue-on-error가 4개 이상 존재
    - monitor-failures.yml에 continue-on-error가 0개 (적용 금지)
    - 모든 YAML 파일 python yaml.safe_load() 파싱 성공
    - diff/commit/push 스텝에는 continue-on-error가 없음: grep -B1 "continue-on-error" .github/workflows/update-byelection.yml에 "Check for changes"나 "Commit" 포함 안 됨
  </acceptance_criteria>
  <done>monitor-failures.yml을 제외한 13개 워크플로우의 외부 의존 스텝에 continue-on-error: true가 적용되어, 앞 단계 실패 시에도 커밋·푸시 단계까지 도달한다</done>
</task>

</tasks>

<verification>
1. GIT-01 검증: `grep -l "concurrency" .github/workflows/*.yml | wc -l` → 14
2. INDEP-01 검증: `grep -c "continue-on-error" .github/workflows/update-byelection.yml` → 3 이상
3. 안전성 검증: `grep -c "continue-on-error" .github/workflows/monitor-failures.yml` → 0
4. YAML 무결성: 14개 파일 전체 yaml.safe_load() 통과
</verification>

<success_criteria>
- 14개 워크플로우 전체에 concurrency 블록 존재 (GIT-01 충족)
- 13개 워크플로우의 외부 의존 스텝에 continue-on-error: true 적용 (INDEP-01 충족)
- monitor-failures.yml에는 continue-on-error 미적용
- diff/commit/push 스텝에는 continue-on-error 미적용
- 모든 YAML 파일 구문 유효
</success_criteria>

<output>
After completion, create `.planning/phases/13-워크플로우-아키텍처-안정화/13-01-SUMMARY.md`
</output>
