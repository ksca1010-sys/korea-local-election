---
phase: 08-선거일-운영-준비
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [workers/FALLBACK-GUIDE.md]
autonomous: true
requirements: [OPS-03]

must_haves:
  truths:
    - "FALLBACK-GUIDE.md를 읽고 단계별로 따라하면 5분 내 수동 JSON 폴백 전환이 가능하다"
    - "문서에 포함된 JSON 예시가 실제 _handleManualJsonInput() 함수에서 파싱 성공한다"
    - "_setManualFallbackMode(true) 호출 시 #manual-fallback-container가 표시된다"
  artifacts:
    - path: "workers/FALLBACK-GUIDE.md"
      provides: "Worker 장애 시 수동 폴백 전환 절차 문서"
      min_lines: 40
  key_links:
    - from: "workers/FALLBACK-GUIDE.md"
      to: "js/app.js:_setManualFallbackMode"
      via: "문서가 App._setManualFallbackMode(true) 호출을 안내"
      pattern: "_setManualFallbackMode"
    - from: "workers/FALLBACK-GUIDE.md"
      to: "index.html:#manual-fallback-container"
      via: "문서가 textarea에 JSON 붙여넣기를 안내"
      pattern: "manual-fallback"
    - from: "js/app.js:_handleManualJsonInput"
      to: "js/map.js:MapModule.applyElectionNightLayer"
      via: "JSON 적용 시 지도 레이어 업데이트"
      pattern: "applyElectionNightLayer"
---

<objective>
Worker 장애 시 수동 JSON 폴백 전환 절차를 문서화하고, 폴백 코드 경로가 정상 동작하는지 검증한다.

Purpose: OPS-03 — 선거 당일 Worker 다운 시 5분 내 복구 가능한 운영 매뉴얼 확보.
Output: workers/FALLBACK-GUIDE.md (운영 매뉴얼) + 코드 경로 검증 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@js/app.js (line 1047~1086 — _setManualFallbackMode, _handleManualJsonInput)
@index.html (line 421~434 — #manual-fallback-container)
@workers/election-night/fixtures/2022-sample.json

<interfaces>
<!-- app.js fallback 관련 함수 (App IIFE 내부) -->
From js/app.js (line 1047~1086):
```javascript
function _setManualFallbackMode(active) {
    _manualFallbackMode = active;
    const container = document.getElementById('manual-fallback-container');
    if (!container) return;
    container.style.display = active ? 'block' : 'none';
}

function _handleManualJsonInput(jsonString) {
    const data = JSON.parse(jsonString);
    if (!data.regions || typeof data.regions !== 'object') throw new Error('regions 필드 없음');
    if (typeof MapModule !== 'undefined' && MapModule.applyElectionNightLayer) {
        MapModule.applyElectionNightLayer(data);
    }
    // status 요소에 '적용 완료' 표시
}
```

<!-- index.html fallback UI -->
From index.html (line 422~434):
```html
<div id="manual-fallback-container" style="display:none; ...">
    <textarea id="manual-fallback-input" rows="6" placeholder='{"regions":{"seoul":{"countRate":72.5,...}}}'></textarea>
    <button id="manual-fallback-apply">적용</button>
    <span id="manual-fallback-status"></span>
</div>
```

<!-- 2022 sample JSON schema -->
From workers/election-night/fixtures/2022-sample.json:
```json
{
  "fetchedAt": "2022-06-01T22:00:00+09:00",
  "electionId": "0020220601",
  "sgTypecode": "11",
  "regions": {
    "seoul": { "countRate": 100, "leadingCandidate": "오세훈", "leadingParty": "ppp", "leadingVoteRate": 59.0, "declared": true },
    ...17개 지역
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FALLBACK-GUIDE.md 작성 (per D-04)</name>
  <files>workers/FALLBACK-GUIDE.md</files>
  <action>
Worker 장애 시 수동 폴백 전환 운영 매뉴얼을 작성한다 (per D-04).

문서 구조 (CAPTURE-GUIDE.md와 동일한 체크박스 스타일, per specifics):
1. **상황 판단** — Worker 다운 징후 (curl /health 실패, 브라우저 콘솔 오류)
2. **Step 1: 폴백 모드 활성화** — 브라우저 Console에서 `App._setManualFallbackMode(true)` 입력
3. **Step 2: JSON 데이터 준비** — NEC 개표 페이지에서 직접 수치 확인 → JSON 포맷으로 작성
4. **Step 3: JSON 붙여넣기 + 적용** — textarea에 붙여넣기 → "적용" 버튼 클릭
5. **Step 4: 확인** — 지도에 개표 레이어 반영 확인, status에 "적용 완료" 표시 확인
6. **반복** — 새 개표 데이터 갱신 시 Step 2~4 반복

포함할 JSON 예시 (per D-04 — 2022-sample.json 스키마 기반):
- regions 객체에 17개 광역 키 (seoul, busan, daegu, ...)
- 각 지역: countRate, leadingCandidate, leadingParty, leadingVoteRate, declared
- 최소 예시 (3개 지역만)와 전체 스키마 참조

주의사항 섹션:
- declared: true는 선관위 공식 "당선" 확정 시에만 (헌법 제2조)
- 수학적 추정으로 declared를 true로 설정 금지
- JSON syntax 오류 시 status에 오류 메시지 표시됨

비기술 운영자도 따라할 수 있는 수준으로 작성 (per specifics).
5분 내 복구 가능하도록 절차를 최소화.
  </action>
  <verify>
    <automated>test -f workers/FALLBACK-GUIDE.md && wc -l workers/FALLBACK-GUIDE.md | awk '{if ($1 >= 40) print "OK: " $1 " lines"; else print "FAIL: too short"}'</automated>
  </verify>
  <done>workers/FALLBACK-GUIDE.md가 40줄 이상이며, 6단계 체크박스 절차 + JSON 예시 + 주의사항을 포함한다.</done>
</task>

<task type="auto">
  <name>Task 2: 폴백 코드 경로 검증</name>
  <files></files>
  <action>
_handleManualJsonInput() 함수가 올바른 JSON을 파싱하고, 잘못된 JSON에 적절히 반응하는지 코드 레벨 검증.

Node.js 스크립트로 검증 (app.js는 브라우저 IIFE이므로 로직만 재현):
1. 유효한 JSON (2022-sample.json 스키마) → JSON.parse 성공 + regions 필드 존재 확인
2. regions 없는 JSON (예: `{"foo":"bar"}`) → "regions 필드 없음" 오류
3. 잘못된 JSON 문자열 (예: `{broken`) → JSON.parse 실패
4. 빈 regions (예: `{"regions":{}}`) → 파싱 성공 (빈 지도 레이어)

추가로 _setManualFallbackMode 로직 검증:
5. active=true → container.style.display === 'block'
6. active=false → container.style.display === 'none'

모든 assert 통과 시 "OPS-03 CODE PASS: 6/6 scenarios verified" 출력.

**중요:** app.js를 수정하지 않는다. 검증 스크립트만 임시 작성.
  </action>
  <verify>
    <automated>node /tmp/test-fallback-code.js 2>&1 | grep "OPS-03 CODE PASS"</automated>
  </verify>
  <done>폴백 JSON 파싱 6개 시나리오 통과. 유효 JSON은 정상 처리, 잘못된 JSON은 오류 메시지 반환이 확인됨.</done>
</task>

</tasks>

<verification>
- workers/FALLBACK-GUIDE.md 존재, 40줄 이상, 단계별 체크박스 포함
- /tmp/test-fallback-code.js 실행 결과 6/6 통과
- js/app.js 및 index.html 수정 없음 (git diff 변경 없음)
</verification>

<success_criteria>
FALLBACK-GUIDE.md를 읽고 따라하면 Worker 장애 시 5분 내 수동 폴백 전환이 가능하며, 폴백 코드 경로가 유효 JSON/무효 JSON 모두 올바르게 처리함이 검증됨
</success_criteria>

<output>
After completion, create `.planning/phases/08-선거일-운영-준비/08-02-SUMMARY.md`
</output>
