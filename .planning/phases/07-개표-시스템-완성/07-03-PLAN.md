---
phase: 07-개표-시스템-완성
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: []
autonomous: false
requirements: [ELEC-03]

must_haves:
  truths:
    - "지도에 개표 결과 색상 레이어가 올바르게 렌더링된다"
    - "Worker 폴링 실패 시 #manual-fallback-container가 표시된다"
    - "개표 배너에 전체 개표율과 시각이 표시된다"
  artifacts: []
  key_links:
    - from: "MapModule.applyElectionNightLayer(data)"
      to: "SVG path fill + stroke"
      via: "D3 style 적용"
      pattern: "applyElectionNightLayer"
    - from: "App._setManualFallbackMode(true)"
      to: "#manual-fallback-container display:block"
      via: "DOM style 토글"
      pattern: "manual-fallback-container"
    - from: "App._updateElectionBanner(data)"
      to: "#election-banner .banner-text"
      via: "textContent 업데이트"
      pattern: "개표 진행 중"
---

<objective>
브라우저 UAT 3건(지도 시각화, 폴백 UI, 개표 배너)을 수행하여 ELEC-03을 달성한다.

Purpose: 선거 당일 실시간 개표 기능이 브라우저에서 정상 작동하는지 최종 확인한다.
Output: UAT 3건 통과 확인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-개표-시스템-완성/07-CONTEXT.md
@.planning/phases/07-개표-시스템-완성/07-02-SUMMARY.md

@js/app.js (line 996~1110)
@js/map.js (line 4112~4187)
@workers/election-night/fixtures/2022-sample.json

<interfaces>
From js/map.js:
```javascript
// Line 4112-4141: applyElectionNightLayer(data)
// data.regions에서 각 광역 key별로 partyColor + countRate 기반 채도 적용
// declared === true → stroke '#ffffff' width 3

// Line 4177-4187: clearElectionNightLayer()
```

From js/app.js (Plan 02에서 추가됨):
```javascript
// _updateElectionBanner(data) — 17개 countRate 평균 → 배너 텍스트
// _setManualFallbackMode(active) — #manual-fallback-container 표시/숨김
// _startElectionNightPolling() — 폴링 시작
// _stopElectionNightPolling() — 폴링 중지
```

From js/election-calendar.js:
```javascript
// getCurrentPhase() — 현재 선거 캘린더 페이즈 반환
// 'election_night' = 2026-06-03 18:00 ~ 2026-06-04 00:00
```

KV 응답 스키마 (테스트용):
```json
{
  "fetchedAt": "2022-06-01T22:00:00+09:00",
  "regions": {
    "seoul": { "countRate": 100, "leadingCandidate": "오세훈", "leadingParty": "ppp", "leadingVoteRate": 59.0, "declared": true },
    "busan": { "countRate": 100, "leadingCandidate": "박형준", "leadingParty": "ppp", "leadingVoteRate": 66.5, "declared": true },
    ...
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UAT 환경 준비 + 자동 검증 가능한 항목 사전 확인</name>
  <files></files>
  <action>
UAT 실행 전 사전 검증을 수행한다.

1. **코드 존재 확인:**
   ```bash
   grep -n "applyElectionNightLayer" js/map.js | head -3
   grep -n "_updateElectionBanner" js/app.js | head -3
   grep -n "_setManualFallbackMode" js/app.js | head -3
   grep -n "manual-fallback-container" index.html | head -3
   grep -n "election-banner" index.html | head -3
   ```

2. **로컬 서버 시작:**
   ```bash
   python3 -m http.server 8000
   ```

3. **UAT 콘솔 명령 준비:**

   UAT 1 (지도 시각화) — DevTools Console에서 실행:
   ```javascript
   // fixture 데이터 직접 로드
   const testData = await fetch('/workers/election-night/fixtures/2022-sample.json').then(r => r.json());
   MapModule.applyElectionNightLayer(testData);
   // 기대: 지도에 정당색 레이어 표시, 서울(ppp 파란색) 확인
   ```

   UAT 2 (폴백 UI) — DevTools Console에서 실행:
   ```javascript
   App._setManualFallbackMode(true);
   // 기대: #manual-fallback-container가 display:block으로 전환
   // 확인 후:
   App._setManualFallbackMode(false);
   ```

   UAT 3 (개표 배너) — DevTools Console에서 실행:
   ```javascript
   // 먼저 배너를 표시 상태로 만들기 (sidebar.js renderElectionBanner가 election_night 아닐 때 숨김)
   const banner = document.getElementById('election-banner');
   banner.style.display = '';
   banner.innerHTML = '<div class="election-banner-content banner-info"><span class="banner-icon">📊</span><span class="banner-text">테스트</span></div>';
   // 배너 업데이트 함수 호출
   const testData2 = await fetch('/workers/election-night/fixtures/2022-sample.json').then(r => r.json());
   App._updateElectionBanner(testData2);
   // 기대: 배너 텍스트가 "개표 진행 중 — 전체 100.0% (22:00 기준)" 으로 변경
   ```
  </action>
  <verify>
    <automated>grep -c "applyElectionNightLayer" js/map.js && grep -c "_updateElectionBanner" js/app.js && grep -c "manual-fallback-container" index.html</automated>
  </verify>
  <done>
- 3개 UAT 대상 함수/DOM 요소가 코드에 존재 확인
- 로컬 서버 + 콘솔 명령 준비 완료
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 브라우저 UAT 3건 검증 (ELEC-03)</name>
  <files></files>
  <action>
사용자가 브라우저에서 3건의 UAT를 직접 실행하고 결과를 확인한다.

**검증 대상:**
1. 지도 위 개표 결과 색상 레이어
2. Worker 장애 시 수동 폴백 UI
3. 개표 진행률 배너

**검증 방법:**
Chrome에서 `http://localhost:8000` 접속 후 DevTools Console (Cmd+Option+J)을 열고 다음 3건을 순서대로 실행한다.

**UAT 1: 지도 시각화**
```javascript
const testData = await fetch('/workers/election-night/fixtures/2022-sample.json').then(r => r.json());
MapModule.applyElectionNightLayer(testData);
```
확인: 지도에 17개 광역지자체가 정당색으로 채색된다. 서울/부산/대구 등 PPP 지역은 붉은 계열, 광주/전남/전북 등 민주당 지역은 파란 계열. 각 지역에 흰색 테두리(declared=true). 초기화: `MapModule.clearElectionNightLayer()`

**UAT 2: 폴백 UI**
```javascript
App._setManualFallbackMode(true);
```
확인: 화면에 수동 JSON 입력 영역(textarea + 적용 버튼)이 표시된다.
복구: `App._setManualFallbackMode(false);`

**UAT 3: 개표 배너**
```javascript
const banner = document.getElementById('election-banner');
banner.style.display = '';
banner.innerHTML = '<div class="election-banner-content banner-info"><span class="banner-icon">📊</span><span class="banner-text">테스트</span></div>';
const testData2 = await fetch('/workers/election-night/fixtures/2022-sample.json').then(r => r.json());
App._updateElectionBanner(testData2);
```
확인: 배너 텍스트가 "개표 진행 중 -- 전체 100.0% (22:00 기준)" 형태로 표시된다 (17개 지역 모두 countRate=100이므로 평균 100.0%).

**resume-signal:** 3건 모두 통과하면 "approved" 입력. 문제 있으면 어느 UAT에서 무엇이 잘못됐는지 설명.
  </action>
  <verify>사용자가 "approved" 입력</verify>
  <done>
- UAT 1: 지도 색상 레이어 올바르게 렌더링 확인
- UAT 2: 폴백 컨테이너 표시/숨김 동작 확인
- UAT 3: 배너 텍스트 "개표 진행 중 -- 전체 XX%" 형식 표시 확인
- ELEC-03 달성
  </done>
</task>

</tasks>

<verification>
- UAT 1: 지도 색상 레이어 올바르게 렌더링 (인간 확인)
- UAT 2: 폴백 컨테이너 표시/숨김 동작 (인간 확인)
- UAT 3: 배너 텍스트 "개표 진행 중 -- 전체 XX%" 형식 표시 (인간 확인)
</verification>

<success_criteria>
1. UAT 3건 모두 "approved" 승인
2. ELEC-03 달성: 브라우저 UAT 3건(지도 시각화, 폴백 UI, 개표 배너) 완료
</success_criteria>

<output>
After completion, create `.planning/phases/07-개표-시스템-완성/07-03-SUMMARY.md`
</output>
