---
phase: 05-poll-supplement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/reparse_pdfs.py
  - data/polls/state.json
  - data/polls/polls.json
autonomous: true
requirements:
  - POLL-01

must_haves:
  truths:
    - "여론조사 빈값(results: []) 32건 중 party_support 4건 제외 28건이 모두 results를 가진다 (채울 수 없는 건은 문서화)"
    - "reparse_pdfs.py가 party_support 항목을 스킵하여 불필요한 실패 로그가 발생하지 않는다"
    - "수동 기입된 수치는 poll_audit_pdf.py 검증을 통과한다"
  artifacts:
    - path: "data/polls/state.json"
      provides: "빈값 채워진 여론조사 상태 데이터"
      contains: "results"
    - path: "data/polls/polls.json"
      provides: "프론트엔드 서빙용 여론조사 데이터"
      contains: "results"
    - path: "scripts/reparse_pdfs.py"
      provides: "party_support 필터 포함된 재파싱 스크립트"
      contains: "party_support"
  key_links:
    - from: "scripts/reparse_pdfs.py"
      to: "data/polls/state.json"
      via: "save_state() + export_frontend_json()"
      pattern: "save_state|export_frontend_json"
    - from: "data/polls/state.json"
      to: "data/polls/polls.json"
      via: "export_frontend_json() 재생성"
      pattern: "export_frontend_json"
---

<objective>
여론조사 빈값(results: []) 32건 중 지역조사 28건의 결과 수치를 PDF 재파싱 및 수동 확인으로 채운다.

Purpose: POLL-01 충족 — 사용자가 여론조사 탭에서 빈 결과 없이 모든 지역조사 수치를 볼 수 있다
Output: `data/polls/state.json` + `data/polls/polls.json` 업데이트, `reparse_pdfs.py` party_support 필터 추가
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-poll-supplement/05-CONTEXT.md
@.planning/phases/05-poll-supplement/05-RESEARCH.md

@scripts/reparse_pdfs.py
@scripts/nesdc_poll_pipeline.py
@data/polls/state.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: reparse_pdfs.py에 party_support 필터 추가 후 전체 실행</name>
  <files>scripts/reparse_pdfs.py, data/polls/state.json, data/polls/polls.json</files>
  <read_first>
    - scripts/reparse_pdfs.py (현재 필터 로직 확인 — results 빈 항목 필터링 위치)
    - scripts/nesdc_poll_pipeline.py (save_state, export_frontend_json 함수 시그니처 확인)
    - data/polls/state.json (현재 빈값 현황 확인, 첫 100줄이면 충분)
  </read_first>
  <action>
1. `scripts/reparse_pdfs.py`에서 빈값 항목 필터링 로직을 찾는다. `results: []`인 항목을 수집하는 부분에 `electionType != 'party_support'` 조건을 추가한다. 구체적으로:
   - 빈값 필터링 리스트 컴프리헨션에 `and p.get('electionType') != 'party_support'` 조건 추가
   - 스킵된 party_support 건수를 로그로 출력: `print(f"Skipped {skipped} party_support polls")`

2. `--dry-run` 실행으로 처리 대상 확인:
   ```bash
   python scripts/reparse_pdfs.py --dry-run
   ```
   - party_support 4건이 스킵되고 28건(또는 그 이하)만 대상으로 잡히는지 확인

3. 전체 실행:
   ```bash
   python scripts/reparse_pdfs.py
   ```
   - 실행 완료 후 성공/실패 건수 로그 확인
   - state.json과 polls.json이 자동 갱신됨 (export_frontend_json 호출)

4. 잔여 빈값 확인:
   ```bash
   python3 -c "
   import json
   with open('data/polls/state.json') as f:
       state = json.load(f)
   polls = state.get('polls', [])
   empty = [p for p in polls if not p.get('results') and p.get('electionType') != 'party_support']
   print(f'잔여 빈값: {len(empty)}건')
   for p in empty:
       print(f\"  nttId={p.get('nttId')} region={p.get('region')} type={p.get('electionType')}\")
   "
   ```
   - 잔여 건이 있으면 Task 2에서 처리

per D-01: reparse_pdfs.py 실행 우선. per D-03: Gemini 폴백 사용하지 않음.
  </action>
  <verify>
    <automated>python3 -c "import json; s=json.load(open('data/polls/state.json')); party=[p for p in s['polls'] if not p.get('results') and p.get('electionType')=='party_support']; regional=[p for p in s['polls'] if not p.get('results') and p.get('electionType')!='party_support']; print(f'party_support 빈값(정상): {len(party)}건'); print(f'지역조사 잔여 빈값: {len(regional)}건'); print('FILTER:', 'PASS' if len(party)==4 else 'CHECK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/reparse_pdfs.py` 파일에 `party_support` 문자열이 포함되어 있음 (grep으로 확인 가능)
    - `--dry-run` 실행 시 party_support 건이 처리 대상에서 제외됨
    - `data/polls/state.json`과 `data/polls/polls.json`이 reparse 결과로 갱신됨
    - 잔여 지역조사 빈값 수가 reparse 전보다 감소함
  </acceptance_criteria>
  <done>reparse_pdfs.py가 party_support를 스킵하고, 자동 파싱 가능한 모든 건의 results가 채워짐. 잔여 실패 건 목록이 Task 2에 전달됨.</done>
</task>

<task type="auto">
  <name>Task 2: 잔여 빈값 수동 확인 및 기입 (D-02)</name>
  <files>data/polls/state.json, data/polls/polls.json</files>
  <read_first>
    - data/polls/state.json (Task 1 결과 반영된 최신 상태)
    - scripts/nesdc_poll_pipeline.py (load_state, save_state, export_frontend_json 함수 — 수동 기입 시 사용)
    - scripts/poll_audit_pdf.py (검증 스크립트 사용법 확인)
  </read_first>
  <action>
1. Task 1 이후 잔여 빈값 목록 확인 (위 검증 스크립트 재실행)

2. 각 잔여 nttId에 대해:
   a. `data/polls/pdfs/{nttId}.pdf` 파일을 Read 도구로 열어 결과표 존재 여부 확인
   b. 결과표가 있으면 후보명, 정당, 지지율 수치를 추출
   c. 결과표가 없으면 (설문지-only) sourceUrl에서 NESDC 상세 페이지 확인 시도
   d. 어디서도 결과표를 찾을 수 없는 건은 "미공개" 상태로 기록

3. 결과 수치가 확인된 건은 state.json에 직접 기입 (RESEARCH.md의 수동 기입 패턴 사용):
   ```python
   import sys, json
   sys.path.insert(0, 'scripts')
   from nesdc_poll_pipeline import load_state, save_state, export_frontend_json
   state = load_state()
   polls = state['polls']
   for p in polls:
       if p.get('nttId') == TARGET_NTTID:
           p['results'] = [
               {'candidateName': '이름', 'party': '정당코드', 'support': 수치},
               ...
           ]
           break
   save_state({'last_id': state['last_id'], 'polls': polls})
   export_frontend_json(polls)
   ```

4. 기입 후 검증 (per 헌법 제3조, D-02):
   ```bash
   python scripts/poll_audit_pdf.py
   ```
   - PDF 원본 수치와 state.json 수치 대조 — 불일치 0건이어야 함

5. 채울 수 없는 건(미공개) 목록을 커밋 메시지에 명시:
   - 예: `data(polls): fill empty results — 28건 중 N건 완료, M건 미공개 (nttId: ...)`

per D-02: 파싱 실패 건은 NESDC 원본 페이지 수동 확인 후 수치 기입.
per D-04: 완료 기준은 32건 모두 results 비어있지 않음 (미공개 건 제외 문서화).
  </action>
  <verify>
    <automated>python3 -c "import json; s=json.load(open('data/polls/state.json')); e=[p for p in s['polls'] if not p.get('results') and p.get('electionType')!='party_support']; print('POLL-01:', 'PASS' if not e else f'FAIL — 잔여 {len(e)}건')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c` 검증 스크립트 실행 시 `POLL-01: PASS` 출력 (잔여 빈값 0건)
    - 또는 잔여 빈값이 있다면 모두 "미공개" 사유가 커밋 메시지에 기록됨
    - `python scripts/poll_audit_pdf.py` 실행 시 수치 불일치 0건
    - `data/polls/polls.json` 파일에서 `"results":[]` 패턴이 party_support 외에 존재하지 않음
  </acceptance_criteria>
  <done>지역조사 빈값이 모두 채워지거나 미공개 사유가 문서화됨. poll_audit_pdf.py 검증 통과. POLL-01 충족.</done>
</task>

</tasks>

<verification>
```bash
# POLL-01 최종 검증
python3 -c "
import json
with open('data/polls/state.json') as f:
    state = json.load(f)
polls = state.get('polls', [])
empty = [p for p in polls if not p.get('results') and p.get('electionType') != 'party_support']
total = len(polls)
filled = len([p for p in polls if p.get('results')])
print(f'전체: {total}건, 결과 있음: {filled}건, 지역조사 빈값: {len(empty)}건')
print('POLL-01:', 'PASS' if not empty else f'FAIL — 잔여 {len(empty)}건')
"

# poll_audit_pdf.py 검증 (수치 정확성)
python scripts/poll_audit_pdf.py
```
</verification>

<success_criteria>
1. `data/polls/state.json` 내 지역조사 빈값(party_support 제외)이 0건이거나 미공개 건만 남음
2. `data/polls/polls.json`이 state.json과 동기화되어 프론트엔드에서 결과 표시 가능
3. `scripts/reparse_pdfs.py`가 party_support를 스킵 처리
4. `poll_audit_pdf.py` 검증 통과 (수치 불일치 0건)
</success_criteria>

<output>
After completion, create `.planning/phases/05-poll-supplement/05-01-SUMMARY.md`
</output>
