---
phase: 07-개표-시스템-완성
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/election-night/index.js
  - workers/CAPTURE-GUIDE.md
autonomous: true
requirements: [ELEC-01]

must_haves:
  truths:
    - "parseNECResponse('')는 빈 regions 객체를 포함한 올바른 스키마를 반환한다"
    - "parseNECResponse(html)는 fixture 스키마와 동일한 구조의 regions를 반환한다"
    - "test-parser.cjs 8/8 테스트가 모두 통과한다"
    - "CAPTURE-GUIDE.md에 NEC URL 캡처 절차가 단계별로 문서화되어 있다"
  artifacts:
    - path: "workers/election-night/index.js"
      provides: "parseNECResponse() skeleton 구현"
      contains: "parseNECResponse"
    - path: "workers/CAPTURE-GUIDE.md"
      provides: "NEC URL 캡처 절차 문서"
      contains: "NEC_URL"
  key_links:
    - from: "workers/election-night/index.js"
      to: "workers/election-night/fixtures/2022-sample.json"
      via: "parseNECResponse 출력 스키마가 fixture 스키마와 일치"
      pattern: "regions.*countRate.*leadingCandidate"
---

<objective>
parseNECResponse() 파서 skeleton을 구현하고, NEC URL 캡처 절차를 문서화한다.

Purpose: Worker가 NEC HTML을 받아 17개 광역지자체 개표 데이터를 추출할 수 있는 파서 뼈대를 완성한다. 실제 NEC HTML 포맷은 5/26 이후 확정되므로 현재는 fixture 기반 skeleton + TODO 마커로 구현하되, test-parser.cjs 8/8 통과를 보장한다.
Output: 구현된 parseNECResponse(), CAPTURE-GUIDE.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-개표-시스템-완성/07-CONTEXT.md
@.planning/phases/07-개표-시스템-완성/07-RESEARCH.md

@workers/election-night/index.js
@workers/election-night/test-parser.cjs
@workers/election-night/fixtures/2022-sample.json
@workers/election-night/wrangler.toml

<interfaces>
<!-- workers/election-night/index.js 현재 exports -->
From workers/election-night/index.js:
```javascript
// Line 138: parseNECResponse(html) — 현재 stub, 빈 regions 반환
function parseNECResponse(html) {
  return {
    fetchedAt: new Date().toISOString(),
    electionId: NEC_CONFIG.ELECTION_ID_2026,
    sgTypecode: '11',
    regions: {},
    _source: 'info.nec.go.kr',
    _parserVersion: '1.0',
  };
}

// Line 12-18: NEC_CONFIG
const NEC_CONFIG = {
  USER_AGENT: 'ElectionInfoMap/1.0',
  ELECTION_ID_2026: '0020260603',
  ELECTION_ID_2022: '0020220601',
  ELECTION_NIGHT_START: new Date('2026-06-03T18:00:00+09:00').getTime(),
  ELECTION_NIGHT_END:   new Date('2026-06-04T00:00:00+09:00').getTime(),
};

export default { scheduled, fetch };
export { parseNECResponse, NEC_CONFIG };
```

From workers/election-night/test-parser.cjs:
```javascript
// REQUIRED_REGIONS: 17개 광역지자체 영문 키
const REQUIRED_REGIONS = [
  'seoul', 'busan', 'daegu', 'incheon', 'gwangju', 'daejeon', 'ulsan', 'sejong',
  'gyeonggi', 'gangwon', 'chungbuk', 'chungnam', 'jeonbuk', 'jeonnam',
  'gyeongbuk', 'gyeongnam', 'jeju',
];
// REQUIRED_FIELDS: 5개 필드
const REQUIRED_FIELDS = ['countRate', 'leadingCandidate', 'leadingParty', 'leadingVoteRate', 'declared'];
// Tests 1~5: fixture regions 스키마 검증
// Test 6: parseNECResponse('') 호출 → { regions: object, fetchedAt: string, _parserVersion: string }
// Tests 7~8: fixture 메타데이터 검증
```

From workers/election-night/fixtures/2022-sample.json (스키마 예시):
```json
{
  "fetchedAt": "2022-06-01T22:00:00+09:00",
  "electionId": "0020220601",
  "sgTypecode": "11",
  "regions": {
    "seoul": { "countRate": 100, "leadingCandidate": "오세훈", "leadingParty": "ppp", "leadingVoteRate": 59.0, "declared": true }
  },
  "_source": "info.nec.go.kr",
  "_parserVersion": "1.0"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: parseNECResponse() skeleton 구현</name>
  <files>workers/election-night/index.js</files>
  <action>
`workers/election-night/index.js`의 `parseNECResponse(html)` 함수 (line 138~149)를 다음과 같이 교체한다.

**빈 입력 방어 (per D-01):**
- `html`이 falsy이거나 길이 100자 미만이면 빈 regions + `_source: 'stub'`, `_parserVersion: '0.0'` 반환 (Test 6 통과용)

**HTML 파싱 skeleton (per D-01):**
- NEC HTML 포맷은 5/26 DevTools 캡처 전까지 미확정이므로, 정규식 기반 skeleton을 구현한다.
- 17개 광역지자체 영문 키 매핑 테이블을 함수 내에 정의한다:
  ```
  const REGION_MAP = { '서울': 'seoul', '부산': 'busan', ... '제주': 'jeju' };
  ```
- HTML `<tr>` 행 추출 → `<td>` 셀 추출 → 지역명/개표율/후보명/득표율/당선여부 파싱 로직을 정규식으로 작성한다.
- `stripTags(html)` 헬퍼: `html.replace(/<[^>]+>/g, '').replace(/&nbsp;/g, ' ').trim()`
- 각 region 객체: `{ countRate: number, leadingCandidate: string, leadingParty: string, leadingVoteRate: number, declared: boolean }`
- **declared 판정 (per D-07, 헌법 제2조):** HTML 셀에 "당선" 텍스트가 정확히 존재할 때만 `true`. 수학적 추정(득표율 50% 초과 등) 절대 금지. `// 헌법 제2조: declared는 선관위 공식 플래그만` 주석 필수.
- 파싱 실패 region은 건너뛰고 `console.warn` 로깅. 1개도 파싱 못하면 빈 regions 반환.

**TODO 마커:**
- 정규식 패턴마다 `// TODO(5/26): 실제 NEC HTML 구조 확인 후 조정` 주석 추가.
- 열 인덱스(지역명=0, 개표율=1 등)에 `// TODO(5/26): 실제 컬럼 순서 확인` 주석 추가.
- 정당 매핑(후보명 → 정당 키)도 `// TODO(5/26): 실제 정당 컬럼/텍스트 확인` 주석 추가.

**반환 스키마:**
```javascript
return {
  fetchedAt: new Date().toISOString(),
  electionId: NEC_CONFIG.ELECTION_ID_2026,
  sgTypecode: '11',
  regions,
  _source: 'info.nec.go.kr',
  _parserVersion: '1.0',
};
```

**주의:**
- Worker 런타임에 DOMParser 없음. 정규식/텍스트 파싱만 사용 (Claude's Discretion).
- `NEC_CONFIG`는 파일 상단에 이미 정의됨. import 불필요.
- `fetchAndParseNEC()` (line 95~119)는 수정하지 않는다 (NEC_URL placeholder 유지).
  </action>
  <verify>
    <automated>node workers/election-night/test-parser.cjs</automated>
  </verify>
  <done>
- parseNECResponse('')가 빈 regions + stub 메타 반환 (Test 6 통과)
- parseNECResponse(html)에 정규식 파서 skeleton 존재 (TODO 마커 포함)
- declared 판정 로직에 헌법 제2조 주석 명시
- test-parser.cjs 8/8 tests passed
  </done>
</task>

<task type="auto">
  <name>Task 2: CAPTURE-GUIDE.md 작성 (ELEC-01)</name>
  <files>workers/CAPTURE-GUIDE.md</files>
  <action>
`workers/CAPTURE-GUIDE.md` 파일을 새로 생성한다 (per D-05).

**문서 구조:**

```markdown
# NEC 개표 API URL 캡처 가이드

**마감:** 2026-05-26 (선거일 1주 전)
**대상:** info.nec.go.kr 실시간 개표 진행 페이지

## 사전 준비

- Chrome 브라우저
- DevTools 열기 단축키: Cmd+Option+I (Mac) / F12 (Windows)

## 캡처 절차

### Step 1: 개표 페이지 접속
1. Chrome으로 `https://info.nec.go.kr` 접속
2. 투표·개표 메뉴 클릭
3. "개표 진행상황" 또는 "개표 현황" 선택
4. "광역단체장(시·도지사)" 선거 유형 선택

### Step 2: DevTools Network 탭 캡처
1. DevTools 열기 (Cmd+Option+I)
2. Network 탭 선택
3. XHR/Fetch 필터 활성화
4. 페이지 새로고침 (Cmd+R)
5. `info.nec.go.kr` 도메인 요청 중 개표 데이터를 반환하는 AJAX 요청 찾기
   - URL 패턴 추정: `electionInfo_report.xhtml` 또는 JSON endpoint
   - 파라미터 확인: `electionId`, `sgTypecode=11`, `cityCode`

### Step 3: URL 복사
1. 해당 요청 우클릭 → "Copy as cURL" 또는 "Copy URL"
2. URL에서 `electionId=0020260603` 확인 (2026 선거 ID)
3. Response 탭에서 HTML/JSON 형태 확인

### Step 4: Worker 코드에 기입
1. `workers/election-night/index.js` 열기
2. `fetchAndParseNEC()` 함수 내 `NEC_URL` 변수 (line 98) 찾기
3. `const NEC_URL = '';` → `const NEC_URL = '캡처한 URL';` 교체
4. `electionId` 파라미터가 `0020260603`(2026)인지 확인 (2022 ID 혼입 방지)

### Step 5: 파서 조정
1. Response 실제 HTML/JSON 구조를 확인
2. `parseNECResponse()` 내 `// TODO(5/26)` 마커 위치에서 정규식 조정
3. `node workers/election-night/test-parser.cjs` 통과 확인

## 체크리스트

- [ ] NEC_URL에 실제 URL 기입 완료
- [ ] electionId = 0020260603 확인
- [ ] parseNECResponse() 정규식 실제 HTML에 맞게 조정
- [ ] test-parser.cjs 8/8 통과
- [ ] `npx wrangler dev --test-scheduled` 로컬 테스트 통과
- [ ] `wrangler deploy` 배포 완료

## 주의사항

- NEC_CONFIG.ELECTION_ID_2022 (0020220601)와 혼동 금지
- declared: true는 HTML에 "당선" 텍스트 존재 시에만 (헌법 제2조)
```

**주의:** 이 파일은 CLAUDE.md 수정 규칙에 의해 workers/ 디렉토리 내에 생성한다. 다른 디렉토리의 파일은 건드리지 않는다.
  </action>
  <verify>
    <automated>test -f workers/CAPTURE-GUIDE.md && grep -q "NEC_URL" workers/CAPTURE-GUIDE.md && grep -q "0020260603" workers/CAPTURE-GUIDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
- workers/CAPTURE-GUIDE.md 파일 존재
- Chrome DevTools 캡처 절차 5단계 포함
- NEC_URL 기입 방법 명시 (line 98)
- electionId 2026/2022 혼입 방지 경고 포함
- 체크리스트 포함 (ELEC-01 완료)
  </done>
</task>

</tasks>

<verification>
- `node workers/election-night/test-parser.cjs` → 8/8 tests passed
- `test -f workers/CAPTURE-GUIDE.md` → 파일 존재
- `grep "TODO(5/26)" workers/election-night/index.js` → TODO 마커 존재
- `grep "헌법 제2조" workers/election-night/index.js` → declared 주석 존재
</verification>

<success_criteria>
1. test-parser.cjs 8개 테스트 모두 PASS
2. parseNECResponse()에 정규식 파서 skeleton + TODO(5/26) 마커 존재
3. declared 판정에 헌법 제2조 주석 명시
4. CAPTURE-GUIDE.md에 5단계 캡처 절차 + 체크리스트 존재
</success_criteria>

<output>
After completion, create `.planning/phases/07-개표-시스템-완성/07-01-SUMMARY.md`
</output>
