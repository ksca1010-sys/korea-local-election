---
phase: 05-poll-supplement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/update-polls.yml
  - js/election-calendar.js
autonomous: false
requirements:
  - POLL-02

must_haves:
  truths:
    - "GitHub Actions workflow가 매일 KST 09:00에 1회 실행되어 신규 여론조사를 수집한다"
    - "workflow가 nesdc_poll_pipeline.py 실행 후 reparse_pdfs.py도 실행한다"
    - "커밋 메시지가 D-07 형식(data: poll sync {날짜})을 따른다"
    - "5/28 00:00 KST 이후 여론조사 탭이 공표금지 메시지를 표시한다"
    - "6/3 18:00 KST 이후 여론조사 탭이 정상 표시로 복귀한다"
  artifacts:
    - path: ".github/workflows/update-polls.yml"
      provides: "D-05 규격 poll-sync workflow (기존 파일 수정)"
      contains: "reparse_pdfs"
  key_links:
    - from: ".github/workflows/update-polls.yml"
      to: "scripts/nesdc_poll_pipeline.py"
      via: "GitHub Actions step"
      pattern: "nesdc_poll_pipeline"
    - from: ".github/workflows/update-polls.yml"
      to: "scripts/reparse_pdfs.py"
      via: "GitHub Actions step (신규 추가)"
      pattern: "reparse_pdfs"
    - from: "js/election-calendar.js"
      to: "js/tabs/poll-tab.js"
      via: "isPublicationBanned() 호출"
      pattern: "isPublicationBanned"
---

<objective>
GitHub Actions 여론조사 자동 수집 파이프라인을 D-05 규격으로 업그레이드하고, 공표금지 자동 숨김을 브라우저에서 검증한다.

Purpose: POLL-02 충족 — 선거일까지 신규 여론조사가 파이프라인으로 지속 추가됨. 공표금지 법적 요건 검증.
Output: 업데이트된 `.github/workflows/update-polls.yml`, 공표금지 브라우저 검증 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-poll-supplement/05-CONTEXT.md
@.planning/phases/05-poll-supplement/05-RESEARCH.md

@.github/workflows/update-polls.yml
@js/election-calendar.js
@js/tabs/poll-tab.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: update-polls.yml을 D-05 규격으로 업그레이드</name>
  <files>.github/workflows/update-polls.yml</files>
  <read_first>
    - .github/workflows/update-polls.yml (현재 워크플로우 전체 — 수정 대상)
    - .planning/phases/05-poll-supplement/05-RESEARCH.md (D-05 규격 poll-sync.yml 예시 코드, 라인 269~317)
  </read_first>
  <action>
기존 `.github/workflows/update-polls.yml` 파일을 D-05 규격으로 수정한다. 파일명은 유지한다 (rename 시 GitHub Actions에서 이전 workflow가 남아 혼란을 줄 수 있음 — 내용만 교체).

변경 사항:

1. **workflow name 변경:** `Update Poll Data (NESDC)` → `Poll Sync (NESDC)` (D-05)

2. **schedule 변경:** 하루 2회 → 1회
   - 기존: `cron: '0 0 * * *'` + `cron: '0 9 * * *'` (2회)
   - 변경: `cron: '0 0 * * *'` 만 유지 (KST 09:00 1회, per D-05)

3. **pip cache 추가:** `actions/setup-python@v5`에 `cache: 'pip'` 옵션 추가

4. **reparse_pdfs.py 실행 단계 추가:** (nesdc_poll_pipeline.py 다음)
   ```yaml
   - name: Reparse empty results
     run: python scripts/reparse_pdfs.py
   ```

5. **커밋 메시지 형식 변경 (D-07):**
   - 기존: `chore: auto-update poll data $(date +%Y-%m-%d)`
   - 변경: `data: poll sync $(date +%Y-%m-%d) -- 신규 ${NEW}건`
   - NEW 건수 계산: `git diff --cached data/polls/polls.json | grep '^\+' | grep '"nttId"' | wc -l | tr -d ' '`

6. **git pull --rebase 추가:** push 전에 `git pull --rebase origin main` 실행 (충돌 방지)

최종 파일 전체 내용:
```yaml
name: Poll Sync (NESDC)

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install httpx beautifulsoup4 pdfplumber

      - name: Run NESDC pipeline
        run: python scripts/nesdc_poll_pipeline.py

      - name: Reparse empty results
        run: python scripts/reparse_pdfs.py

      - name: Check for changes
        id: diff
        run: git diff --quiet data/polls/ || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push
        if: steps.diff.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/polls/
          NEW=$(git diff --cached data/polls/polls.json | grep '^\+' | grep '"nttId"' | wc -l | tr -d ' ')
          git commit -m "data: poll sync $(date +%Y-%m-%d) -- 신규 ${NEW}건"
          git pull --rebase origin main && git push
```

per D-05: 매일 KST 09:00 1회 실행, nesdc_poll_pipeline.py + reparse_pdfs.py 순서.
per D-07: 커밋 메시지 형식 `data: poll sync {날짜} -- {신규 N건}`.
per D-08: 5/27 이후 수동 disable — workflow 자체에는 공표금지 로직 미포함 (수동 조치).
  </action>
  <verify>
    <automated>grep -c "reparse_pdfs" .github/workflows/update-polls.yml && grep -c "Poll Sync" .github/workflows/update-polls.yml && grep "cron:" .github/workflows/update-polls.yml | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Poll Sync" .github/workflows/update-polls.yml` 출력이 1줄 (name 변경됨)
    - `grep "reparse_pdfs" .github/workflows/update-polls.yml` 출력이 1줄 (reparse 단계 추가됨)
    - `grep "cron:" .github/workflows/update-polls.yml` 출력이 정확히 1줄 (하루 1회만)
    - `grep "data: poll sync" .github/workflows/update-polls.yml` 출력이 1줄 (D-07 커밋 형식)
    - `grep "cache: 'pip'" .github/workflows/update-polls.yml` 출력이 1줄 (pip cache 추가됨)
    - `grep "git pull --rebase" .github/workflows/update-polls.yml` 출력이 1줄
  </acceptance_criteria>
  <done>update-polls.yml이 D-05 규격(1회/일, reparse 포함, D-07 커밋 형식)으로 업그레이드됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 공표금지 자동 숨김 브라우저 검증 (D-11/D-12)</name>
  <files>js/election-calendar.js</files>
  <read_first>
    - js/election-calendar.js (getKST() 함수 위치 — mock 수정 대상)
    - js/tabs/poll-tab.js (isPublicationBanned() 호출 분기 — 라인 ~486)
  </read_first>
  <action>
Claude가 검증을 위해 자동으로 수행하는 단계:

1. `js/election-calendar.js`의 `getKST()` 함수 첫 줄에 mock 반환문 삽입:
   ```javascript
   return new Date('2026-05-28T01:00:00+09:00');
   ```
2. 로컬 서버 시작: `python3 -m http.server 8000`
3. 사용자에게 경계값 3개 브라우저 검증 요청 (아래 how-to-verify 참조)
4. 검증 완료 후 getKST() 원복 (mock 코드 제거)
5. `git diff js/election-calendar.js` 실행하여 원복 확인 (변경 없어야 함)

per D-11: 날짜 mock 브라우저 테스트로 검증. 테스트 후 원복 커밋.
per D-12: 경계값 3개 (5/27 23:59, 5/28 00:00, 6/3 18:00) 검증.
  </action>
  <what-built>
    공표금지 자동 숨김 로직 (기존 구현 — 이번 Phase에서 신규 구현 아님).
    `js/election-calendar.js`의 `isPublicationBanned()` + `js/tabs/poll-tab.js`의 분기 로직.

    검증 방법: `getKST()` 함수를 임시로 특정 날짜를 반환하도록 수정하여 경계값 3개를 테스트한다.
    테스트 후 반드시 원복한다.

    Claude가 수행하는 자동화 단계:
    1. `js/election-calendar.js`의 `getKST()` 함수 첫 줄에 `return new Date('2026-05-28T01:00:00+09:00');` 삽입
    2. 로컬 서버 시작: `python3 -m http.server 8000`
  </what-built>
  <how-to-verify>
    **경계값 테스트 3개 (D-12):**

    1. **5/28 00:00 이후 (공표금지 시작):**
       - getKST()가 `2026-05-28T01:00:00+09:00` 반환하도록 수정됨
       - http://localhost:8000 → 아무 지역 클릭 → 여론조사 탭
       - 예상: 데이터 숨김 + "공표금지 기간" 안내 메시지 표시

    2. **5/27 23:59 (공표금지 직전):**
       - getKST() 반환값을 `2026-05-27T23:59:00+09:00`으로 변경
       - 브라우저 새로고침 → 여론조사 탭
       - 예상: 여론조사 데이터 정상 표시

    3. **6/3 18:00 (공표금지 종료):**
       - getKST() 반환값을 `2026-06-03T18:00:00+09:00`으로 변경
       - 브라우저 새로고침 → 여론조사 탭
       - 예상: 여론조사 데이터 정상 표시 (18:00 정각은 허용)

    테스트 완료 후 Claude가 getKST() 원복 + 커밋합니다.
  </how-to-verify>
  <verify>
    <automated>git diff js/election-calendar.js | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - 경계값 3개 테스트 모두 예상 동작과 일치 (사용자 확인)
    - `git diff js/election-calendar.js` 출력이 0줄 (원복 완료)
  </acceptance_criteria>
  <done>공표금지 경계값 3개 브라우저 테스트 통과 + getKST() 원복 완료</done>
  <resume-signal>"approved" 입력 또는 발견된 이슈 설명</resume-signal>
</task>

</tasks>

<verification>
```bash
# POLL-02: workflow 검증
grep "Poll Sync" .github/workflows/update-polls.yml
grep "reparse_pdfs" .github/workflows/update-polls.yml
grep -c "cron:" .github/workflows/update-polls.yml  # 1이어야 함

# 공표금지 원복 확인
git diff js/election-calendar.js  # 변경 없어야 함
```
</verification>

<success_criteria>
1. `.github/workflows/update-polls.yml`이 D-05 규격 (1회/일, reparse 포함, D-07 커밋 형식)
2. 공표금지 경계값 3개 테스트 통과 (사용자 확인)
3. `getKST()` 원복 완료 (git diff clean)
</success_criteria>

<output>
After completion, create `.planning/phases/05-poll-supplement/05-02-SUMMARY.md`
</output>
