---
phase: 08-선거일-운영-준비
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified: [workers/DEPLOY-CHECKLIST.md]
autonomous: true
requirements: [OPS-01]

must_haves:
  truths:
    - "DEPLOY-CHECKLIST.md를 보고 단계별로 최종 배포를 실행할 수 있다"
    - "체크리스트에 NEC URL 기입 → wrangler deploy → curl /health → 브라우저 스모크 전체 흐름이 포함된다"
    - "체크리스트에 Pages 배포(git push) + Worker 배포(wrangler deploy) 두 경로가 모두 포함된다"
  artifacts:
    - path: "workers/DEPLOY-CHECKLIST.md"
      provides: "선거일 최종 배포 체크리스트"
      min_lines: 50
  key_links:
    - from: "workers/DEPLOY-CHECKLIST.md"
      to: "workers/CAPTURE-GUIDE.md"
      via: "NEC URL 캡처 절차를 전제 문서로 참조"
      pattern: "CAPTURE-GUIDE"
    - from: "workers/DEPLOY-CHECKLIST.md"
      to: "workers/FALLBACK-GUIDE.md"
      via: "장애 시 폴백 절차를 참조"
      pattern: "FALLBACK-GUIDE"
    - from: "workers/DEPLOY-CHECKLIST.md"
      to: "workers/election-night/index.js"
      via: "NEC_URL 기입 위치 안내"
      pattern: "NEC_URL"
---

<objective>
선거일 최종 배포 체크리스트를 작성한다. CAPTURE-GUIDE.md(URL 캡처)와 FALLBACK-GUIDE.md(장애 대응)를 전제로 한 배포 절차 문서.

Purpose: OPS-01 — 6/1~2에 체크리스트를 따라 최종 배포를 실행할 수 있도록 한다 (per D-01, D-02).
Output: workers/DEPLOY-CHECKLIST.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-선거일-운영-준비/08-02-SUMMARY.md
@workers/CAPTURE-GUIDE.md
@workers/FALLBACK-GUIDE.md
@workers/election-night/wrangler.toml
@workers/election-night/index.js (line 95~98 — NEC_URL placeholder)

<interfaces>
<!-- Worker 배포 관련 설정 -->
From workers/election-night/wrangler.toml:
```toml
name = "election-night"
main = "index.js"
[triggers]
crons = [ "* * * * *" ]
[[kv_namespaces]]
binding = "ELECTION_RESULTS"
id = "db737acc9d624075bab261c60628f95c"
```

From workers/election-night/index.js (line 95~98):
```javascript
// TODO: 2022 아카이브 Chrome DevTools 캡처 후 실제 URL 기입
const NEC_URL = ''; // placeholder
```

Worker endpoints:
- /health → {"status":"ok"}
- /results → 개표 데이터 JSON (KV에서 조회)

Pages 배포:
- git push → Cloudflare Pages 자동 빌드
- 또는 `npx wrangler pages deploy` 수동 배포
- URL: https://korea-local-election.pages.dev
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEPLOY-CHECKLIST.md 작성 (per D-01, D-02)</name>
  <files>workers/DEPLOY-CHECKLIST.md</files>
  <action>
선거일 최종 배포 체크리스트를 작성한다 (per D-01: wrangler deploy 실배포 포함, per D-02: Pages + Worker + 스모크 테스트).

문서 구조 (CAPTURE-GUIDE.md와 동일한 체크박스 스타일):

**사전 조건:**
- [ ] CAPTURE-GUIDE.md 절차 완료 (NEC URL 확정)
- [ ] FALLBACK-GUIDE.md 숙지 완료

**Part 1: Worker 배포 (per D-01)**
- [ ] `workers/election-night/index.js` 열기
- [ ] `fetchAndParseNEC()` 내 `NEC_URL` (line 98) 에 캡처한 URL 기입
- [ ] `electionId` 파라미터가 `0020260603` (2026)인지 확인 — 2022 ID `0020220601` 혼입 방지 (헌법 제4조)
- [ ] `cd workers/election-night && npx wrangler deploy` 실행
- [ ] `curl https://election-night.ksca1010.workers.dev/health` → `{"status":"ok"}` 확인
- [ ] `curl https://election-night.ksca1010.workers.dev/results` → 응답 확인 (stub이라도 200 OK)

**Part 2: Pages 배포 (per D-02)**
- [ ] 모든 변경사항 git commit + push
- [ ] Cloudflare Pages 자동 빌드 완료 대기 (또는 `npx wrangler pages deploy`)
- [ ] https://korea-local-election.pages.dev 접속 확인

**Part 3: 브라우저 스모크 테스트 (per D-02 — 30초 수동)**
- [ ] 지도 로딩 확인 (D3.js 렌더링, 17개 광역 표시)
- [ ] 탭 전환 확인 (개요, 후보, 여론조사, 뉴스, 역대비교 중 3개 이상)
- [ ] election-banner 요소 존재 확인 (`document.getElementById('election-banner')`)

**Part 4: 최종 확인**
- [ ] Worker cron trigger 활성 확인 (Cloudflare Dashboard → Workers → election-night → Triggers)
- [ ] KV namespace binding 확인 (ELECTION_RESULTS)

**장애 대응:**
- Worker 장애 시: `workers/FALLBACK-GUIDE.md` 참조
- Pages 장애 시: Cloudflare Dashboard에서 이전 배포로 롤백 (2클릭)

**범위 외 (per deferred):**
- 모니터링 자동화, 롤백 절차 상세 문서화, 알림 시스템

참조 URL:
- Worker: https://election-night.ksca1010.workers.dev
- Pages: https://korea-local-election.pages.dev
- Cloudflare Dashboard: https://dash.cloudflare.com
  </action>
  <verify>
    <automated>test -f workers/DEPLOY-CHECKLIST.md && grep -c '\- \[ \]' workers/DEPLOY-CHECKLIST.md | awk '{if ($1 >= 10) print "OK: " $1 " checklist items"; else print "FAIL: too few items"}'</automated>
  </verify>
  <done>workers/DEPLOY-CHECKLIST.md가 10개 이상의 체크리스트 항목을 포함하며, Worker 배포 + Pages 배포 + 스모크 테스트 + 장애 대응 참조가 모두 포함된다.</done>
</task>

<task type="auto">
  <name>Task 2: 체크리스트 완결성 검증</name>
  <files></files>
  <action>
DEPLOY-CHECKLIST.md가 모든 필수 요소를 참조하는지 자동 검증.

검증 항목:
1. CAPTURE-GUIDE.md 참조가 포함되어 있는가
2. FALLBACK-GUIDE.md 참조가 포함되어 있는가
3. NEC_URL 기입 절차가 포함되어 있는가
4. wrangler deploy 명령어가 포함되어 있는가
5. curl /health 확인이 포함되어 있는가
6. Pages 배포 절차가 포함되어 있는가
7. 브라우저 스모크 테스트가 포함되어 있는가
8. 0020260603 (2026 선거 ID) 확인이 포함되어 있는가

각 항목을 grep으로 확인하여 8/8 통과 시 "OPS-01 PASS" 출력.
  </action>
  <verify>
    <automated>patterns=("CAPTURE-GUIDE" "FALLBACK-GUIDE" "NEC_URL" "wrangler deploy" "/health" "pages" "스모크\|smoke" "0020260603"); pass=0; total=${#patterns[@]}; for p in "${patterns[@]}"; do if grep -qi "$p" workers/DEPLOY-CHECKLIST.md 2>/dev/null; then pass=$((pass+1)); fi; done; if [ $pass -eq $total ]; then echo "OPS-01 PASS: $pass/$total"; else echo "FAIL: $pass/$total"; fi</automated>
  </verify>
  <done>DEPLOY-CHECKLIST.md에 8개 필수 참조(CAPTURE-GUIDE, FALLBACK-GUIDE, NEC_URL, wrangler deploy, /health, pages, 스모크테스트, 선거ID)가 모두 포함됨.</done>
</task>

</tasks>

<verification>
- workers/DEPLOY-CHECKLIST.md 존재, 50줄 이상, 10개 이상 체크박스
- 8개 필수 참조 항목 모두 grep으로 확인
- workers/election-night/index.js 수정 없음 (실배포는 6/1~2에 체크리스트 따라 실행)
</verification>

<success_criteria>
DEPLOY-CHECKLIST.md를 보고 단계별로 NEC URL 기입 → Worker 배포 → Pages 배포 → 스모크 테스트 → 장애 대응까지 전체 최종 배포 절차를 실행할 수 있다
</success_criteria>

<output>
After completion, create `.planning/phases/08-선거일-운영-준비/08-03-SUMMARY.md`
</output>
