---
phase: 08-선거일-운영-준비
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [OPS-02]

must_haves:
  truths:
    - "5/28 00:00 ~ 6/3 18:00 사이에 isPublicationBanned()이 true를 반환한다"
    - "공표금지 기간에 getFilteredPolls()가 빈 배열과 banned:true를 반환한다"
    - "공표금지 기간에 뉴스탭 여론조사 하부메뉴가 disabled된다"
    - "경계값(5/28 00:00 정각, 6/3 18:00 정각)에서 정확히 전환된다"
  artifacts:
    - path: "js/election-calendar.js"
      provides: "isPublicationBanned(), getFilteredPolls(), isNewsSubTabDisabled()"
      contains: "isPublicationBanned"
  key_links:
    - from: "js/election-calendar.js:isPublicationBanned"
      to: "js/election-calendar.js:getFilteredPolls"
      via: "getFilteredPolls calls isPublicationBanned internally"
      pattern: "isPublicationBanned\\(\\)"
    - from: "js/election-calendar.js:isPublicationBanned"
      to: "js/election-calendar.js:isNewsSubTabDisabled"
      via: "isNewsSubTabDisabled calls isPublicationBanned for poll subtab"
      pattern: "isPublicationBanned\\(\\)"
---

<objective>
공표금지 기간(5/28 00:00 ~ 6/3 18:00 KST) 여론조사 자동 숨김 로직을 코드 레벨에서 검증한다.

Purpose: OPS-02 — 선거법 위반 방지. isPublicationBanned() 함수가 경계값에서 정확히 동작하는지 확인.
Output: 검증 결과 로그 (코드 수정 없음 — 기존 구현체 검증만)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@js/election-calendar.js

<interfaces>
<!-- election-calendar.js public API (IIFE module) -->
From js/election-calendar.js:
```javascript
// Layer 1: 시간 기준
const getKST = () => { /* returns Date in KST */ };

// DATES 상수
const DATES = {
  PUBLICATION_BAN_START: new Date('2026-05-28T00:00:00+09:00'),
  VOTE_END: new Date('2026-06-03T18:00:00+09:00'),
};

// Layer 2A: 법적 필수
const isPublicationBanned = () => {
  const now = getKST();
  return now >= DATES.PUBLICATION_BAN_START && now < DATES.VOTE_END;
};

const getFilteredPolls = (polls) => {
  if (isPublicationBanned()) return { polls: [], banned: true, notice: '...' };
  return { polls: polls || [], banned: false, notice: null };
};

const isNewsSubTabDisabled = (subTabName) => {
  if (subTabName === '여론조사' && isPublicationBanned()) return { disabled: true, notice: '...' };
  return { disabled: false, notice: null };
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 공표금지 경계값 코드 검증</name>
  <files></files>
  <action>
Node.js 스크립트를 임시로 작성하여 election-calendar.js의 isPublicationBanned() 로직을 경계값 테스트한다. 파일 수정 없음 — 검증만 수행.

검증할 시나리오 (per D-03):
1. 5/27 23:59:59 KST → isPublicationBanned() === false (금지 전)
2. 5/28 00:00:00 KST → isPublicationBanned() === true (금지 시작 — >= 연산자)
3. 6/1 12:00:00 KST → isPublicationBanned() === true (금지 기간 중)
4. 6/3 17:59:59 KST → isPublicationBanned() === true (금지 종료 직전)
5. 6/3 18:00:00 KST → isPublicationBanned() === false (금지 종료 — < 연산자, 18:00 정각은 허용)
6. 6/3 18:00:01 KST → isPublicationBanned() === false (금지 종료 후)

방법: election-calendar.js는 IIFE 브라우저 모듈이므로 직접 require 불가. 대신:
- DATES 상수와 isPublicationBanned 로직을 inline으로 재현 (election-calendar.js line 41~74의 정확한 날짜값과 비교 연산자)
- Date 생성자를 mock하여 getKST()가 특정 시각을 반환하도록 설정
- 6개 시나리오 모두 assert하고 결과 출력

getFilteredPolls()도 검증:
7. 금지 기간에 polls 배열 전달 → { polls: [], banned: true } 반환
8. 금지 기간 외에 polls 배열 전달 → { polls: [...], banned: false } 반환

isNewsSubTabDisabled()도 검증:
9. 금지 기간에 '여론조사' 전달 → { disabled: true }
10. 금지 기간에 '전체' 전달 → { disabled: false }

모든 assert 통과 시 "OPS-02 PASS: 10/10 scenarios verified" 출력.
실패 시 어떤 시나리오가 실패했는지 상세 출력.

**중요:** 이 스크립트는 검증 후 삭제하지 않아도 됨 (임시 파일). election-calendar.js를 수정하지 않는다.
  </action>
  <verify>
    <automated>node /tmp/test-publication-ban.js 2>&1 | grep "OPS-02 PASS"</automated>
  </verify>
  <done>10개 경계값 시나리오 모두 통과. isPublicationBanned()가 5/28 00:00 ~ 6/3 18:00 경계에서 정확히 동작함이 확인됨.</done>
</task>

</tasks>

<verification>
- node /tmp/test-publication-ban.js 가 "OPS-02 PASS: 10/10 scenarios verified" 출력
- election-calendar.js 파일이 수정되지 않았음 (git diff js/election-calendar.js 변경 없음)
</verification>

<success_criteria>
5/28 00:00 KST 이후 isPublicationBanned()===true, 6/3 18:00 KST에 false로 전환됨이 코드 레벨에서 검증 완료
</success_criteria>

<output>
After completion, create `.planning/phases/08-선거일-운영-준비/08-01-SUMMARY.md`
</output>
