---
phase: 04-election-night
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - js/app.js
  - js/map.js
  - index.html
autonomous: false
requirements:
  - FEAT-06

must_haves:
  truths:
    - "election_night 페이즈에서 브라우저가 60초마다 Worker /results를 폴링한다"
    - "개표 데이터가 지도에 정당색 x 개표율 채도로 반영된다"
    - "1위 후보 득표율(%)이 폴리곤 중앙에 오버레이된다"
    - "선관위 공식 당선 플래그가 있는 지역은 굵은 테두리로 강조된다"
    - "Worker 장애 시 수동 업데이트 모드 메시지가 표시되고 수동 JSON 입력이 가능하다"
  artifacts:
    - path: "js/app.js"
      provides: "election_night 폴링 클라이언트 + 수동 폴백 UI"
      contains: "_startElectionNightPolling"
    - path: "js/map.js"
      provides: "개표 결과 지도 레이어 (색상 + 오버레이 + 당선 stroke)"
      contains: "applyElectionNightLayer"
    - path: "index.html"
      provides: "수동 폴백 UI 컨테이너"
      contains: "manual-fallback"
  key_links:
    - from: "js/app.js"
      to: "Worker /results"
      via: "fetch in setInterval"
      pattern: "fetch.*election-night.*results"
    - from: "js/app.js"
      to: "js/map.js"
      via: "MapModule.applyElectionNightLayer(data)"
      pattern: "applyElectionNightLayer"
    - from: "js/map.js"
      to: "hexToRgba"
      via: "개표율 채도 계산"
      pattern: "hexToRgba.*countRate"
---

<objective>
브라우저 폴링 클라이언트 + 수동 폴백 경로 + 지도 개표 결과 시각화

Purpose: election_night 페이즈 진입 시 브라우저가 Worker 엔드포인트를 60초마다 폴링하여 지도에 개표 결과를 실시간 반영한다. Worker 장애 시 수동 JSON 입력으로 폴백한다.

Output: app.js 폴링 클라이언트 코드 + map.js 개표 레이어 + index.html 수동 폴백 UI
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-election-night/04-CONTEXT.md
@.planning/phases/04-election-night/04-RESEARCH.md
@.planning/phases/04-election-night/04-01-SUMMARY.md

@js/app.js
@js/map.js
@js/election-calendar.js
@index.html

<interfaces>
<!-- 04-01에서 생성한 Worker 응답 스키마 -->
Worker /results 응답 JSON:
```json
{
  "fetchedAt": "2026-06-03T19:30:00+09:00",
  "electionId": "0020260603",
  "sgTypecode": "11",
  "regions": {
    "seoul": {
      "countRate": 72.5,
      "leadingCandidate": "홍길동",
      "leadingParty": "democratic",
      "leadingVoteRate": 54.3,
      "declared": false
    }
  },
  "_source": "info.nec.go.kr",
  "_parserVersion": "1.0"
}
```

<!-- 04-01에서 수정한 election-calendar.js API -->
From js/election-calendar.js (04-01에서 추가):
```javascript
ElectionCalendar.getCurrentPhase() // -> 'election_night' (18:00~24:00 KST)
ElectionCalendar.DATES.ELECTION_NIGHT_END // -> Date('2026-06-04T00:00:00+09:00')
```

<!-- 기존 map.js 핵심 함수 시그니처 -->
From js/map.js:
```javascript
function hexToRgba(hex, alpha) { /* rgba 문자열 반환 */ }
function getRegionColor(regionKey) { /* 현재 선거유형 기반 색상 */ }
function _neutralFill() { /* 회색 기본 fill */ }
// D3 패턴: d3.select('.region[data-region="KEY"]').attr('fill', ...).attr('stroke', ...)
```

<!-- 기존 app.js 패턴 (ElectionCalendar 사용) -->
From js/app.js:
```javascript
// ElectionCalendar.getCurrentPhase()로 페이즈별 분기 처리
// App 컨트롤러 IIFE 구조 내에서 private 함수 정의
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 브라우저 폴링 클라이언트 + 수동 폴백 경로 + 지도 개표 레이어</name>
  <files>js/app.js, js/map.js, index.html</files>
  <read_first>
    - js/app.js (수정 대상 -- 전체 IIFE 구조 파악, ElectionCalendar 사용 패턴)
    - js/map.js (수정 대상 -- hexToRgba 위치, getRegionColor 패턴, D3 fill/stroke 갱신 패턴)
    - js/election-calendar.js (04-01에서 수정된 상태 -- election_night 페이즈 확인)
    - index.html (수동 폴백 UI 삽입 위치)
    - workers/election-night/index.js (Worker 응답 스키마)
    - workers/election-night/fixtures/2022-sample.json (테스트용 데이터 구조)
    - .planning/phases/04-election-night/04-CONTEXT.md (D-09~D-12 시각화 결정, Claude's Discretion -- 수동 폴백 UX)
    - .planning/phases/04-election-night/04-01-SUMMARY.md (Worker 구현 결과)
  </read_first>
  <action>
**1. js/app.js 수정 -- 폴링 클라이언트 추가:**

App 컨트롤러 IIFE 내부에 election_night 폴링 관련 private 변수/함수를 추가한다. 기존 코드 구조를 깨지 않도록 IIFE 마지막(return 직전)에 블록으로 추가.

```javascript
// --- Election Night 실시간 개표 폴링 ---
const ELECTION_NIGHT_WORKER = 'https://election-night.<CF_SUBDOMAIN>.workers.dev';
// TODO: 배포 후 실제 Worker URL로 교체. 로컬 테스트: 'http://localhost:8787'
let _electionNightPoller = null;
let _manualFallbackMode = false;

function _checkElectionNightPhase() {
    const phase = ElectionCalendar.getCurrentPhase();
    if (phase === 'election_night' && !_electionNightPoller) {
        _startElectionNightPolling();
    } else if (phase !== 'election_night' && _electionNightPoller) {
        _stopElectionNightPolling();
    }
}

function _startElectionNightPolling() {
    if (_electionNightPoller) return;
    console.log('[election_night] 개표 폴링 시작');
    _pollElectionResults();  // 즉시 1회
    _electionNightPoller = setInterval(_pollElectionResults, 60_000);
}

function _stopElectionNightPolling() {
    if (_electionNightPoller) {
        clearInterval(_electionNightPoller);
        _electionNightPoller = null;
        console.log('[election_night] 개표 폴링 중지');
    }
}

async function _pollElectionResults() {
    try {
        const resp = await fetch(`${ELECTION_NIGHT_WORKER}/results`, {
            signal: AbortSignal.timeout(10_000)
        });
        if (!resp.ok) throw new Error(`Worker ${resp.status}`);
        const data = await resp.json();
        if (data.error) throw new Error(data.error);
        MapModule.applyElectionNightLayer(data);
        _setManualFallbackMode(false);
    } catch (err) {
        console.warn('[election_night] Worker 응답 실패, 수동 모드 전환', err);
        _setManualFallbackMode(true);
    }
}

function _setManualFallbackMode(active) {
    _manualFallbackMode = active;
    const container = document.getElementById('manual-fallback-container');
    if (!container) return;
    container.style.display = active ? 'block' : 'none';
}

function _handleManualJsonInput(jsonString) {
    try {
        const data = JSON.parse(jsonString);
        if (!data.regions || typeof data.regions !== 'object') {
            throw new Error('regions 필드 없음');
        }
        MapModule.applyElectionNightLayer(data);
        console.log('[election_night] 수동 JSON 적용 완료');
        const status = document.getElementById('manual-fallback-status');
        if (status) status.textContent = '적용 완료 (' + new Date().toLocaleTimeString('ko-KR') + ')';
    } catch (err) {
        console.error('[election_night] 수동 JSON 파싱 실패', err);
        const status = document.getElementById('manual-fallback-status');
        if (status) status.textContent = '오류: ' + err.message;
    }
}
```

기존 App 초기화 로직(init 또는 DOMContentLoaded 핸들러)에 `_checkElectionNightPhase()` 호출 추가. 또한 5분마다 페이즈 재확인하는 타이머 추가:
```javascript
setInterval(_checkElectionNightPhase, 300_000);  // 5분마다 페이즈 재확인
```

수동 폴백 버튼 이벤트 리스너:
```javascript
document.addEventListener('DOMContentLoaded', () => {
    const btn = document.getElementById('manual-fallback-apply');
    if (btn) {
        btn.addEventListener('click', () => {
            const textarea = document.getElementById('manual-fallback-input');
            if (textarea?.value) _handleManualJsonInput(textarea.value);
        });
    }
});
```

**2. js/map.js 수정 -- 개표 결과 시각화 레이어:**

MapModule IIFE 내부에 개표 레이어 함수를 추가한다. 기존 `getRegionColor()` 함수 아래에 위치.

```javascript
// --- Election Night 개표 결과 레이어 ---
let _electionNightData = null;

function applyElectionNightLayer(data) {
    _electionNightData = data;
    const regions = data?.regions || {};

    // 각 광역 지역 폴리곤 색상 갱신
    d3.selectAll('.region').each(function() {
        const el = d3.select(this);
        const key = el.attr('data-region');
        if (!key || !regions[key]) {
            el.attr('fill', _neutralFill());
            el.attr('stroke', null).attr('stroke-width', null);
            return;
        }
        const r = regions[key];
        // D-09: 색상 = 1위 정당색 x 개표율 채도
        const partyColor = ElectionData.getPartyColor(r.leadingParty);
        const alpha = Math.max((r.countRate / 100) * 0.85, 0.12);  // 최소 채도 0.12
        el.attr('fill', hexToRgba(partyColor, alpha));

        // D-12: 당선 확정 = 선관위 공식 플래그 -> 굵은 stroke
        if (r.declared === true) {
            el.attr('stroke', '#ffffff').attr('stroke-width', 3);
        } else {
            el.attr('stroke', null).attr('stroke-width', null);
        }
    });

    // D-10: 1위 후보 득표율(%) 오버레이 텍스트
    _updateVoteRateOverlay(regions);
}

function _updateVoteRateOverlay(regions) {
    // 기존 오버레이 제거
    d3.selectAll('.election-night-overlay').remove();

    const svg = d3.select('#map-container svg');
    if (!svg.size()) return;

    const overlayGroup = svg.append('g').attr('class', 'election-night-overlay');

    Object.entries(regions).forEach(([key, r]) => {
        if (!r.leadingVoteRate) return;
        const regionEl = d3.select(`.region[data-region="${key}"]`);
        if (!regionEl.size()) return;

        // 폴리곤 바운딩 박스 중앙 계산
        const bbox = regionEl.node().getBBox();
        const cx = bbox.x + bbox.width / 2;
        const cy = bbox.y + bbox.height / 2;

        overlayGroup.append('text')
            .attr('x', cx)
            .attr('y', cy)
            .attr('text-anchor', 'middle')
            .attr('dominant-baseline', 'central')
            .attr('font-size', '11px')
            .attr('font-weight', 'bold')
            .attr('fill', '#fff')
            .attr('stroke', '#000')
            .attr('stroke-width', 0.5)
            .attr('pointer-events', 'none')
            .text(`${r.leadingVoteRate.toFixed(1)}%`);
    });
}

function clearElectionNightLayer() {
    _electionNightData = null;
    d3.selectAll('.election-night-overlay').remove();
    // 기존 색상으로 복원
    d3.selectAll('.region').each(function() {
        const el = d3.select(this);
        const key = el.attr('data-region');
        if (key) el.attr('fill', getRegionColor(key));
        el.attr('stroke', null).attr('stroke-width', null);
    });
}
```

MapModule 공개 API(return 객체)에 추가:
```javascript
applyElectionNightLayer,
clearElectionNightLayer,
```

**주의사항 (D-11, 헌법 제2조):**
- `declared: true` 판정은 Worker가 선관위 HTML에서 파싱한 공식 플래그만 사용
- map.js는 `r.declared === true` 비교만 수행 -- 자체적으로 개표율 기반 당선 추정 코드를 절대 포함하지 않음
- `r.leadingVoteRate`는 NEC 원본 값 그대로 표시 -- 반올림/보정 없음

**3. index.html 수정 -- 수동 폴백 UI 컨테이너:**

사이드 패널(#side-panel 또는 #info-panel) 하단, 탭 콘텐츠 영역 밖에 수동 폴백 UI를 추가한다. 기본적으로 `display: none` 상태.

```html
<!-- Election Night 수동 폴백 UI -->
<div id="manual-fallback-container" style="display:none; padding:12px; background:#1a1a2e; border-top:2px solid #e74c3c;">
  <div style="color:#e74c3c; font-weight:bold; margin-bottom:8px;">
    <i class="fas fa-exclamation-triangle"></i> 수동 업데이트 모드
  </div>
  <p style="color:#ccc; font-size:12px; margin-bottom:8px;">
    실시간 서버 연결이 끊어졌습니다. 개표 데이터 JSON을 직접 입력하세요.
  </p>
  <textarea id="manual-fallback-input" rows="6" style="width:100%; background:#0d0d1a; color:#eee; border:1px solid #333; border-radius:4px; padding:8px; font-family:monospace; font-size:11px;" placeholder='{"regions":{"seoul":{"countRate":72.5,...}}}'></textarea>
  <div style="display:flex; justify-content:space-between; align-items:center; margin-top:8px;">
    <button id="manual-fallback-apply" style="background:#e74c3c; color:#fff; border:none; padding:6px 16px; border-radius:4px; cursor:pointer; font-size:12px;">적용</button>
    <span id="manual-fallback-status" style="color:#888; font-size:11px;"></span>
  </div>
</div>
```

정확한 삽입 위치는 index.html을 읽고 #side-panel 또는 #info-panel의 닫는 태그 직전에 배치.
  </action>
  <verify>
    <automated>
node -e "
const fs = require('fs');
// 1. app.js에 폴링 클라이언트 코드 존재
const app = fs.readFileSync('js/app.js', 'utf8');
console.assert(app.includes('_startElectionNightPolling'), '_startElectionNightPolling missing');
console.assert(app.includes('_pollElectionResults'), '_pollElectionResults missing');
console.assert(app.includes('_setManualFallbackMode'), '_setManualFallbackMode missing');
console.assert(app.includes('_handleManualJsonInput'), '_handleManualJsonInput missing');
console.assert(app.includes('election_night'), 'election_night phase check missing');
console.assert(app.includes('/results'), 'Worker /results endpoint missing');

// 2. map.js에 개표 레이어 존재
const map = fs.readFileSync('js/map.js', 'utf8');
console.assert(map.includes('applyElectionNightLayer'), 'applyElectionNightLayer missing');
console.assert(map.includes('clearElectionNightLayer'), 'clearElectionNightLayer missing');
console.assert(map.includes('election-night-overlay'), 'overlay class missing');
console.assert(map.includes('hexToRgba') && map.includes('countRate'), 'D-09 color calc missing');
console.assert(map.includes('r.declared === true'), 'D-12 declared check missing');
console.assert(!map.includes('mathEstimate'), 'No estimation code allowed (D-11)');

// 3. index.html에 수동 폴백 UI 존재
const html = fs.readFileSync('index.html', 'utf8');
console.assert(html.includes('manual-fallback-container'), 'manual fallback container missing');
console.assert(html.includes('manual-fallback-input'), 'manual fallback textarea missing');
console.assert(html.includes('manual-fallback-apply'), 'manual fallback apply button missing');

console.log('Task 1 PASS: all assertions passed');
"
    </automated>
  </verify>
  <acceptance_criteria>
    - js/app.js에 `_startElectionNightPolling`, `_stopElectionNightPolling`, `_pollElectionResults`, `_setManualFallbackMode`, `_handleManualJsonInput` 함수 존재
    - js/app.js에서 `ElectionCalendar.getCurrentPhase() === 'election_night'` 조건으로 폴링 시작
    - js/app.js에서 Worker 응답 실패 시 `_setManualFallbackMode(true)` 호출
    - js/map.js에 `applyElectionNightLayer(data)` 함수가 public API(return 객체)에 포함
    - js/map.js에서 `hexToRgba(partyColor, (countRate/100)*0.85)` 패턴으로 색상 계산 (D-09)
    - js/map.js에서 `r.declared === true`일 때만 stroke-width: 3 적용 (D-12)
    - js/map.js에서 개표율 기반 당선 추정 코드가 존재하지 않음 (D-11)
    - js/map.js에서 `leadingVoteRate.toFixed(1)` + '%' 텍스트 오버레이 (D-10)
    - index.html에 `id="manual-fallback-container"` div 존재 (기본 display:none)
    - index.html에 `id="manual-fallback-input"` textarea 존재
    - index.html에 `id="manual-fallback-apply"` 버튼 존재
  </acceptance_criteria>
  <done>election_night 페이즈에서 60초 폴링이 자동 시작되고, 개표 결과가 지도에 반영되며, Worker 장애 시 수동 JSON 입력이 가능하다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 개표 시각화 + 수동 폴백 브라우저 검증</name>
  <files>js/app.js, js/map.js, index.html</files>
  <action>
    브라우저에서 election_night 모드를 강제 진입시키고, 2022 fixture 데이터로 지도 시각화와 수동 폴백 기능을 검증한다. 이 태스크는 자동 구현이 아닌 사람의 시각적 확인을 요구한다.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify -- 브라우저 수동 검증 필요"</automated>
  </verify>
  <done>사용자가 지도 색상, 득표율 오버레이, 당선 확정 stroke, 수동 폴백 UI를 브라우저에서 확인하고 승인함</done>
  <what-built>
    election_night 페이즈 감지 + Worker 폴링 + 지도 개표 레이어 + 수동 폴백 UI
  </what-built>
  <how-to-verify>
    1. 브라우저 콘솔에서 election_night 모드 강제 진입:
       ```javascript
       // getCurrentPhase()를 임시 오버라이드
       ElectionCalendar.getCurrentPhase = () => 'election_night';
       // 수동 폴백 모드 테스트 (Worker가 아직 배포 전이므로 자동 전환됨)
       ```
    2. 수동 폴백 UI가 표시되는지 확인 ("수동 업데이트 모드" 메시지)
    3. 2022 fixture JSON을 textarea에 붙여넣고 "적용" 클릭:
       ```json
       {"fetchedAt":"2022-06-01T22:00:00+09:00","regions":{"seoul":{"countRate":100,"leadingCandidate":"오세훈","leadingParty":"ppp","leadingVoteRate":59.0,"declared":true},"gyeonggi":{"countRate":72,"leadingCandidate":"김동연","leadingParty":"democratic","leadingVoteRate":51.5,"declared":false},"gwangju":{"countRate":100,"leadingCandidate":"강기정","leadingParty":"democratic","leadingVoteRate":65.7,"declared":true}}}
       ```
    4. 지도 확인:
       - 서울: 국민의힘 빨간색, 100% 채도, 굵은 테두리(당선 확정), "59.0%" 오버레이
       - 경기: 더불어민주당 파란색, 72% 채도(연한), 테두리 없음(미확정), "51.5%" 오버레이
       - 광주: 더불어민주당 파란색, 100% 채도, 굵은 테두리(당선 확정), "65.7%" 오버레이
       - 데이터 없는 지역: 회색(중립색)
    5. 배너에 "개표가 진행 중입니다" 메시지 표시 확인
  </how-to-verify>
  <resume-signal>"approved" 또는 발견된 문제 설명</resume-signal>
</task>

</tasks>

<verification>
1. `grep -n '_startElectionNightPolling' js/app.js` -> 함수 존재
2. `grep -n 'applyElectionNightLayer' js/map.js` -> 함수 존재 + return 객체에 포함
3. `grep -n 'manual-fallback-container' index.html` -> UI 컨테이너 존재
4. `grep -n 'r.declared === true' js/map.js` -> 당선 확정 체크 (D-12)
5. `grep -c 'hexToRgba.*countRate\|countRate.*hexToRgba' js/map.js` -> D-09 색상 계산
6. 브라우저 검증: election_night 강제 진입 + 2022 fixture 수동 적용 -> 지도 색상 변화 확인
</verification>

<success_criteria>
- election_night 페이즈에서 60초 폴링이 자동 시작됨
- Worker 장애(현재 미배포 상태) 시 자동으로 수동 폴백 모드 전환
- 수동 JSON 입력 시 지도에 정당색 x 개표율 채도로 색상 반영 (D-09)
- 득표율(%) 텍스트가 폴리곤 중앙에 오버레이 (D-10)
- 당선 확정 지역(declared: true)에 굵은 테두리 표시 (D-12)
- 개표율 기반 당선 추정 코드 없음 (D-11, 헌법 제2조)
- CLAUDE.md 수정 규칙 준수: 탭 파일 미수정, app.js와 map.js만 수정
</success_criteria>

<output>
After completion, create `.planning/phases/04-election-night/04-02-SUMMARY.md`
</output>
