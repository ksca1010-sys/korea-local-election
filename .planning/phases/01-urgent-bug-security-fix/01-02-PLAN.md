---
plan: 01-02
phase: 01
wave: 1
depends_on: []
files_modified:
  - _headers
  - index.html
  - js/clarity-consent.js
autonomous: true
requirements:
  - BUG-05
  - QUAL-01
  - QUAL-02
---

# Plan 01-02: 보안 헤더 + Clarity PIPA 동의 게이트 + CSS 버전 통일

## Goal
Cloudflare Pages `_headers` 파일로 HTTP 보안 헤더를 추가하고, Microsoft Clarity 로드를 PIPA 동의 게이트로 전환하며, `index.html`의 CSS/JS 버전 타임스탬프를 통일한다.

## must_haves
- `_headers` 파일이 repo 루트에 존재하고 `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Content-Security-Policy`, `Referrer-Policy: strict-origin-when-cross-origin` 4개 헤더를 포함
- CSP에 `d3js.org`, `cdn.jsdelivr.net`, `fonts.googleapis.com`, `fonts.gstatic.com`, `cdnjs.cloudflare.com`, `www.clarity.ms`, `*.workers.dev` 도메인이 포함
- `index.html`의 Clarity 즉시 실행 스크립트(lines 24-29)가 제거되고, 동의 후에만 동적 로드되는 구조로 전환
- 동의 배너가 `position: fixed; bottom: 0` sticky 하단 바로 표시됨
- `localStorage` key `clarity_consent`에 `{ status, timestamp }` 형태로 저장
- `index.html` CSS 버전이 `v=1774711234`로 통일됨 (JS와 동일)

## Tasks

### Task 1: _headers 보안 헤더 파일 생성 (QUAL-01, per D-08~D-11)

<read_first>
- index.html (lines 32-41) — 현재 사용 중인 CDN 도메인 확인 (preconnect, script src, link href)
- index.html (lines 909-910) — Cloudflare analytics 스크립트 도메인 확인
</read_first>

<action>
**Per D-08~D-11:** repo 루트에 `_headers` 파일을 생성한다. Cloudflare Pages는 이 파일을 자동으로 읽어 응답 헤더에 적용한다.

파일 내용 (정확히 아래 내용으로 생성):

```
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://d3js.org https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://www.clarity.ms https://*.workers.dev https://static.cloudflareinsights.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdnjs.cloudflare.com; font-src 'self' https://fonts.gstatic.com https://cdnjs.cloudflare.com; img-src 'self' data: blob:; connect-src 'self' https://www.clarity.ms https://*.workers.dev https://*.cloudflareinsights.com https://apis.data.go.kr; frame-src 'none'
```

설명:
- `script-src`: `'unsafe-inline'`은 현재 index.html 내 inline script 호환, `'unsafe-eval'`은 D3/Chart.js 호환. CDN 전체 + Clarity + Workers + CF analytics 허용.
- `style-src`: `'unsafe-inline'`은 inline style 호환. Google Fonts + Font Awesome CDN 허용.
- `font-src`: Google Fonts gstatic + Font Awesome CDN.
- `connect-src`: Clarity 비콘, Workers, CF analytics, NEC API 허용.
- `frame-src: 'none'`은 D-09의 `X-Frame-Options: DENY`와 일치.
- `report-uri` 미포함 (per D-10).
- nonce 미사용 (per D-08).
</action>

<acceptance_criteria>
- [ ] `test -f _headers && echo "EXISTS"` 출력이 `EXISTS`
- [ ] `grep 'X-Frame-Options: DENY' _headers` 매칭
- [ ] `grep 'X-Content-Type-Options: nosniff' _headers` 매칭
- [ ] `grep 'Referrer-Policy: strict-origin-when-cross-origin' _headers` 매칭
- [ ] `grep 'Content-Security-Policy:' _headers` 매칭
- [ ] `grep 'd3js.org' _headers` 매칭
- [ ] `grep 'cdn.jsdelivr.net' _headers` 매칭
- [ ] `grep 'fonts.googleapis.com' _headers` 매칭
- [ ] `grep 'fonts.gstatic.com' _headers` 매칭
- [ ] `grep 'cdnjs.cloudflare.com' _headers` 매칭
- [ ] `grep 'www.clarity.ms' _headers` 매칭
- [ ] `grep 'workers.dev' _headers` 매칭
- [ ] `grep -c 'report-uri' _headers` 출력이 0 (per D-10)
- [ ] `grep -c 'nonce' _headers` 출력이 0 (per D-08)
</acceptance_criteria>

### Task 2: Clarity PIPA 동의 게이트 구현 (QUAL-02, per D-03~D-07)

<read_first>
- index.html (lines 23-30) — 현재 Clarity 즉시 실행 스크립트. Clarity project ID: `vxpaked5fs`
- index.html (lines 906-907) — 메인 app.js 로드 위치 (consent JS를 그 전에 로드해야 함)
</read_first>

<action>
**이 작업은 3개 파일을 수정/생성한다:**

**A. `js/clarity-consent.js` 신규 생성 (per D-03~D-07):**

IIFE 모듈 패턴으로 `ClarityConsent` 모듈을 생성한다. 정확한 코드:

```javascript
/**
 * ClarityConsent — Microsoft Clarity PIPA 동의 게이트
 * 사용자 동의 없이 Clarity를 로드하지 않음 (PIPA 준수)
 */
const ClarityConsent = (() => {
    const STORAGE_KEY = 'clarity_consent';
    const CLARITY_ID = 'vxpaked5fs';
    const EXPIRY_MS = 365 * 24 * 60 * 60 * 1000; // 365일

    function _getConsent() {
        try {
            const raw = localStorage.getItem(STORAGE_KEY);
            if (!raw) return null;
            const data = JSON.parse(raw);
            if (!data.timestamp || Date.now() - data.timestamp > EXPIRY_MS) {
                localStorage.removeItem(STORAGE_KEY);
                return null;
            }
            return data;
        } catch (e) {
            console.warn('[ClarityConsent] localStorage read failed:', e);
            return null;
        }
    }

    function _setConsent(status) {
        try {
            localStorage.setItem(STORAGE_KEY, JSON.stringify({
                status: status,
                timestamp: Date.now()
            }));
        } catch (e) {
            console.warn('[ClarityConsent] localStorage write failed:', e);
        }
    }

    function _loadClarity() {
        if (document.querySelector('script[src*="clarity.ms"]')) return;
        (function(c,l,a,r,i,t,y){
            c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
            t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
            y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
        })(window, document, "clarity", "script", CLARITY_ID);
        // ConsentV2 API: 동의 상태 전달
        if (typeof window.clarity === 'function') {
            window.clarity("consent");
        }
    }

    function _showBanner() {
        const banner = document.createElement('div');
        banner.id = 'clarity-consent-banner';
        banner.style.cssText = 'position:fixed;bottom:0;left:0;right:0;z-index:9999;background:#1a1a2e;border-top:1px solid rgba(255,255,255,0.1);padding:12px 16px;display:flex;align-items:center;justify-content:center;gap:12px;flex-wrap:wrap;font-size:14px;color:#ccc;';
        banner.innerHTML = `
            <span>사이트 개선을 위해 Microsoft Clarity 세션 기록에 동의하시겠습니까?</span>
            <button id="clarity-accept" style="background:#4CAF50;color:#fff;border:none;padding:6px 16px;border-radius:4px;cursor:pointer;font-size:14px;">동의</button>
            <button id="clarity-reject" style="background:transparent;color:#aaa;border:1px solid #555;padding:6px 16px;border-radius:4px;cursor:pointer;font-size:14px;">거부</button>
        `;
        document.body.appendChild(banner);

        document.getElementById('clarity-accept').addEventListener('click', () => {
            _setConsent('accepted');
            banner.remove();
            _loadClarity();
        });
        document.getElementById('clarity-reject').addEventListener('click', () => {
            _setConsent('rejected');
            banner.remove();
        });
    }

    function init() {
        const consent = _getConsent();
        if (consent && consent.status === 'accepted') {
            _loadClarity();
            return;
        }
        if (consent && consent.status === 'rejected') {
            return; // 거부 상태 — 배너 재표시 안 함 (per D-07)
        }
        // 동의 기록 없음 — 배너 표시
        if (document.readyState === 'loading') {
            document.addEventListener('DOMContentLoaded', _showBanner);
        } else {
            _showBanner();
        }
    }

    init();

    return { init };
})();
```

**B. `index.html` 수정 1 — Clarity 즉시 실행 스크립트 제거:**

Lines 23-30 (아래 블록)을 완전히 삭제한다:
```html
    <!-- Microsoft Clarity -->
    <script type="text/javascript">
        (function(c,l,a,r,i,t,y){
            c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
            t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
            y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
        })(window, document, "clarity", "script", "vxpaked5fs");
    </script>
```

**C. `index.html` 수정 2 — clarity-consent.js 로드 추가:**

Line 907 (`<script defer src="js/app.js?v=1774711234"></script>`) 직전에 다음 라인을 추가:
```html
    <script defer src="js/clarity-consent.js?v=1774711234"></script>
```

이렇게 하면 consent 모듈이 app.js 이전에 로드되어 페이지 로드 시점에 배너를 표시하거나 동의 상태에 따라 Clarity를 로드한다.
</action>

<acceptance_criteria>
- [ ] `test -f js/clarity-consent.js && echo "EXISTS"` 출력이 `EXISTS`
- [ ] `grep -c "const ClarityConsent" js/clarity-consent.js` 출력이 1
- [ ] `grep -c "clarity_consent" js/clarity-consent.js` 출력이 1 이상 (STORAGE_KEY)
- [ ] `grep -c "vxpaked5fs" js/clarity-consent.js` 출력이 1 (CLARITY_ID)
- [ ] `grep -c "365 \* 24" js/clarity-consent.js` 출력이 1 (365일 만료)
- [ ] `grep -c '동의' js/clarity-consent.js` 출력이 1 이상
- [ ] `grep -c '거부' js/clarity-consent.js` 출력이 1 이상
- [ ] `grep '사이트 개선을 위해 Microsoft Clarity 세션 기록에 동의하시겠습니까' js/clarity-consent.js` 매칭 (per D-04 문구)
- [ ] `grep 'position:fixed;bottom:0' js/clarity-consent.js` 매칭 (per D-03 sticky bottom)
- [ ] `grep 'z-index:9999' js/clarity-consent.js` 매칭
- [ ] `grep -c '"consent"' js/clarity-consent.js` 출력이 1 (ConsentV2 API, per D-06)
- [ ] `grep -c 'clarity-consent.js' index.html` 출력이 1 (script 태그 추가됨)
- [ ] index.html에 `(function(c,l,a,r,i,t,y)` 패턴이 `<head>` 내에 없음: `sed -n '/<head>/,/<\/head>/p' index.html | grep -c 'function(c,l,a,r,i,t,y)'` 출력이 0
- [ ] `grep -c 'Microsoft Clarity' index.html` 출력이 0 (주석 포함 즉시 실행 블록 전체 삭제됨)
</acceptance_criteria>

### Task 3: index.html CSS 버전 타임스탬프 통일 (BUG-05, per D-12)

<read_first>
- index.html (line 42) — `css/style.css?v=1774589813` (CSS 버전, JS보다 오래됨)
- index.html (lines 872-907) — JS 파일들의 `v=1774711234` (통일 대상 값)
</read_first>

<action>
**Per D-12:** `index.html` line 42의 CSS 버전 타임스탬프를 JS와 동일하게 변경한다.

```
// Before (line 42):
<link rel="stylesheet" href="css/style.css?v=1774589813">

// After:
<link rel="stylesheet" href="css/style.css?v=1774711234">
```

`1774589813` → `1774711234`로 1곳만 변경한다.
</action>

<acceptance_criteria>
- [ ] `grep 'style.css?v=1774711234' index.html` 매칭
- [ ] `grep -c 'v=1774589813' index.html` 출력이 0 (구 버전 완전 제거)
- [ ] `grep -o 'v=[0-9]*' index.html | sort -u` 출력이 `v=1774711234` 1줄만 (모든 버전 통일)
</acceptance_criteria>

## Verification

```bash
# QUAL-01: _headers 파일 존재 + 4개 헤더 포함
test -f _headers && echo "PASS: _headers exists" || echo "FAIL"
for h in "X-Frame-Options: DENY" "X-Content-Type-Options: nosniff" "Referrer-Policy: strict-origin-when-cross-origin" "Content-Security-Policy:"; do
  grep -q "$h" _headers && echo "PASS: $h" || echo "FAIL: $h missing"
done

# QUAL-02: Clarity 동의 게이트
test -f js/clarity-consent.js && echo "PASS: consent module exists" || echo "FAIL"
grep -q 'clarity-consent.js' index.html && echo "PASS: consent loaded in HTML" || echo "FAIL"
! grep -q 'function(c,l,a,r,i,t,y)' <(sed -n '/<head>/,/<\/head>/p' index.html) && echo "PASS: no inline Clarity in head" || echo "FAIL: inline Clarity still in head"

# BUG-05: 버전 통일
VERSIONS=$(grep -o 'v=[0-9]*' index.html | sort -u | wc -l)
[ "$VERSIONS" -eq 1 ] && echo "PASS: version unified" || echo "FAIL: $VERSIONS distinct versions"
```
