---
phase: 06-본후보-등록-대응
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/candidate_pipeline/fetch_nec_candidates.py
  - data/candidates/unmatched_candidates.json
  - .github/workflows/update-candidates.yml
autonomous: true
requirements: [CAND-01]

must_haves:
  truths:
    - "fetch_nec_candidates.py 실행 시 NEC PofelcddInfoInqireService에서 광역단체장/교육감/기초단체장 본후보 데이터를 수집한다"
    - "수집된 후보의 status가 NOMINATED으로, ballotNumber가 저장된다"
    - "선거구 매핑 실패 후보가 unmatched_candidates.json에 보관된다"
    - "GitHub Actions cron이 매일 1회 NEC 수집을 실행하고 수동 dispatch도 가능하다"
  artifacts:
    - path: "scripts/candidate_pipeline/fetch_nec_candidates.py"
      provides: "NEC 본후보 API 호출 및 기존 데이터 병합"
      contains: "fetch_nec_official"
    - path: "data/candidates/unmatched_candidates.json"
      provides: "선거구 미매핑 후보 보관"
      contains: "_meta"
    - path: ".github/workflows/update-candidates.yml"
      provides: "NEC 본후보 수집 단계"
      contains: "fetch_nec_candidates.py"
  key_links:
    - from: "scripts/candidate_pipeline/fetch_nec_candidates.py"
      to: "NEC PofelcddInfoInqireService API"
      via: "urllib.request (nec_precand_sync.py 패턴)"
      pattern: "PofelcddInfoInqireService"
    - from: "scripts/candidate_pipeline/fetch_nec_candidates.py"
      to: "data/candidates/governor.json, superintendent.json, mayor_candidates.json"
      via: "merge_candidates() 함수"
      pattern: "merge_candidates"
    - from: ".github/workflows/update-candidates.yml"
      to: "scripts/candidate_pipeline/fetch_nec_candidates.py"
      via: "python 실행 단계"
      pattern: "fetch_nec_candidates.py"
---

<objective>
NEC 공식 후보자 API(PofelcddInfoInqireService)를 호출하여 본후보 데이터를 자동 수집하는 파이프라인을 구현한다.

Purpose: 5/14 본후보 등록 개시 후 cron 또는 수동 실행으로 공식 후보(NOMINATED)와 기호(ballotNumber)를 자동 수집한다 (CAND-01 달성).
Output: fetch_nec_candidates.py의 NEC API 호출 구현, unmatched_candidates.json 초기 파일, update-candidates.yml에 NEC 수집 단계 삽입
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-본후보-등록-대응/06-CONTEXT.md
@.planning/phases/06-본후보-등록-대응/06-RESEARCH.md

@scripts/candidate_pipeline/fetch_nec_candidates.py
@scripts/candidate_pipeline/nec_precand_sync.py
@.github/workflows/update-candidates.yml

<interfaces>
<!-- nec_precand_sync.py에서 재사용할 패턴 -->

From scripts/candidate_pipeline/nec_precand_sync.py:
```python
NEC_API_BASE = "http://apis.data.go.kr/9760000"
NEC_SERVICE = f"{NEC_API_BASE}/PofelcddInfoInqireService/getPoelpcddRegistSttusInfoInqire"
SG_ID = "20260603"

SIDO_MAP = {
    "서울특별시": "seoul", "부산광역시": "busan", ...
    "강원특별자치도": "gangwon", "강원도": "gangwon", ...
}

def _normalize_party(name): ...
def fetch_precandidates(sg_typecode, nec_key=None): ...  # 페이지네이션 루프, resultCode 판정, dict→list 변환
```

From scripts/candidate_pipeline/fetch_nec_candidates.py:
```python
REGION_MAP = { '1100': 'seoul', ... }  # 선관위 코드→regionKey
PARTY_MAP = { '더불어민주당': 'democratic', ... }
def merge_candidates(existing, new_data): ...  # 병합 규칙: 있으면 업데이트, 없으면 WITHDRAWN, 새 후보 추가
def validate(data): ...  # 중복 이름, 필수 필드 검증
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fetch_nec_preliminary()를 NEC 본후보 API 호출로 교체</name>
  <files>scripts/candidate_pipeline/fetch_nec_candidates.py, data/candidates/unmatched_candidates.json</files>
  <action>
1. **fetch_nec_preliminary() 교체 (line 72~87)** — 함수명을 `fetch_nec_official()`로 변경하고, `nec_precand_sync.fetch_precandidates()` 패턴을 복사하여 NEC PofelcddInfoInqireService를 호출하는 실제 로직으로 교체한다. per D-03.

   구현 상세:
   - `nec_precand_sync`를 import하고, `fetch_precandidates()` 함수를 직접 재사용한다 (Don't Hand-Roll 원칙). 동일 API 엔드포인트(`PofelcddInfoInqireService`)이며 sgTypecode만 다르므로 함수 재사용이 가능.
   - sgTypecode "3"(광역단체장), "10"(교육감), "4"(기초단체장)을 순차 호출한다. per D-05. 광역의원/기초의원은 호출하지 않는다. per D-06.
   - 환경변수 `NEC_API_KEY`에서 API 키를 읽는다 (이미 `nec_precand_sync.fetch_precandidates()`가 이 패턴 사용).

2. **NEC 응답 → 프로젝트 후보 객체 변환 함수** `_convert_nec_item(item, election_type)` 추가:
   - `item.get("sdName")` → `nec_precand_sync.SIDO_MAP`으로 regionKey 변환 (Don't Hand-Roll — SIDO_MAP 재사용, REGION_MAP 사용하지 않음. SIDO_MAP이 "강원특별자치도"/"강원도" 등 변형명을 처리함)
   - `item.get("jdName")` → `nec_precand_sync._normalize_party()`로 partyKey 변환
   - `item.get("name")` → name
   - `item.get("career1")` → career
   - `item.get("wiwName")` → districtName (기초단체장 전용)
   - **ballotNumber 필드:** 후보 필드명이 불확실하므로(LOW confidence), `item.get("giho") or item.get("gihoSn") or item.get("huboCnt")` 순서로 시도하고, 찾으면 `int()` 변환 후 저장. 못 찾으면 `None`. (Anti-Pattern: ballotNumber를 문자열로 저장 금지)
   - status: `"NOMINATED"` 고정
   - dataSource: `"nec_official"`

3. **raw response 로깅** (Pitfall 2 대응): 5/14 첫 실행 시 필드명 확인을 위해, 각 sgTypecode별 첫 번째 아이템의 전체 필드를 `print(json.dumps(item, ensure_ascii=False, indent=2))` 로 출력하고, `data/candidates/nec_raw_sample.json`에 샘플 3건(typecode별 1건씩)을 저장한다.

4. **선거구 미매핑 후보 처리** per D-12: `_convert_nec_item()`에서 `SIDO_MAP`에 없는 sdName이 반환되면, 해당 후보를 별도 리스트에 모아 `data/candidates/unmatched_candidates.json`에 저장한다. 기초단체장(typecode "4")에서 `wiwName`이 기존 mayor_candidates.json의 district 키에 매핑 안 되는 경우도 동일 처리.

5. **unmatched_candidates.json 초기 파일 생성**: `{"_meta": {"version": "1.0", "lastUpdated": null, "description": "선거구 미매핑 후보 보관"}, "candidates": []}` 구조로 생성.

6. **날짜 게이팅** (Pitfall 1, Pitfall 5 대응): `fetch_nec_official()` 함수 시작부에 `if datetime.now() < datetime(2026, 5, 14, 0, 0):` 이면 경고 메시지 출력하고 빈 dict 반환. 이 시점 전에는 cron이 실행되어도 NEC API를 호출하지 않는다.

7. **`--dry-run` 플래그 추가**: `if __name__ == '__main__'` 블록에서 `sys.argv`에 `--dry-run`이 있으면 API 호출 + 변환 + 검증까지만 실행하고 파일 저장은 건너뛴다. validate() 함수(line 196)는 반드시 호출한다. per 헌법 제3조.

8. **`--log-raw` 플래그 추가**: raw response 샘플을 `data/candidates/nec_raw_sample.json`에 저장하는 옵션. `--dry-run`과 함께 사용 가능.

9. **main 함수 흐름 수정** (line 229~ 근처의 `if __name__` 블록):
   ```
   fetch_nec_official() → 3개 typecode별 NEC 데이터 수집
   → _convert_nec_item()으로 프로젝트 형식 변환
   → merge_candidates(existing, new_data)로 기존 데이터와 병합
   → validate(merged) 호출 (헌법 제3조)
   → --dry-run이 아니면 파일 저장
   ```
   교육감은 superintendent.json, 기초단체장은 mayor_candidates.json에 각각 저장. 현재 `merge_candidates()`는 governor.json만 처리하므로, 교육감/기초단체장용 병합 함수도 추가한다. 기초단체장은 2단계 중첩(candidates["seoul"]["종로구"] = 배열)이므로 Pitfall 3 주의.

10. **교육감 stance 유지** (Open Question 2 대응): 교육감 병합 시 NEC API 응답에 stance 필드가 없으면 기존 superintendent.json의 stance 값을 보존한다. 덮어쓰지 않는다.

주의사항:
- `nec_precand_sync.py`의 `fetch_precandidates()` 함수를 import하여 재사용할 것. 새 HTTP 클라이언트를 만들지 말 것 (Don't Hand-Roll).
- `SIDO_MAP`도 `nec_precand_sync`에서 import. `fetch_nec_candidates.py`의 기존 `REGION_MAP`(선관위 코드 기반)은 API 응답이 시도명 문자열을 반환하므로 사용하지 않는다.
- 헌법 제4조: sgTypecode "3"=광역단체장, "4"=기초단체장, "10"=교육감 절대 혼용 금지.
  </action>
  <verify>
    <automated>cd /Users/isawufo/Desktop/AI-cording-project/korea-local-election && python scripts/candidate_pipeline/fetch_nec_candidates.py --dry-run 2>&1 | head -30</automated>
    unmatched_candidates.json 파일이 존재하고 유효한 JSON인지 확인: python -c "import json; json.load(open('data/candidates/unmatched_candidates.json'))"
  </verify>
  <done>
    - fetch_nec_official() 함수가 nec_precand_sync.fetch_precandidates()를 재사용하여 sgTypecode 3/4/10 호출
    - _convert_nec_item()이 NEC 응답을 프로젝트 후보 객체(status=NOMINATED, ballotNumber 포함)로 변환
    - 날짜 게이팅으로 5/14 이전 실행 시 API 미호출
    - --dry-run 플래그로 파일 저장 없이 검증만 실행 가능
    - --log-raw 플래그로 원본 응답 샘플 저장 가능
    - unmatched_candidates.json 초기 파일 생성 완료
    - validate() 호출로 검증 단계 포함 (헌법 제3조)
  </done>
</task>

<task type="auto">
  <name>Task 2: update-candidates.yml에 NEC 본후보 수집 단계 삽입</name>
  <files>.github/workflows/update-candidates.yml</files>
  <action>
1. **NEC 본후보 수집 단계 추가** per D-01, D-04: 기존 `0-c단계(Sync byelection winners)` 다음, `1단계(Factcheck governor)` 앞에 새 스텝을 삽입한다. update-polls.yml 패턴을 준용한다 (per D-04).

   ```yaml
   # 0.5단계: NEC 본후보 등록 데이터 수집 (5/14~ 활성화)
   - name: Fetch NEC official candidates
     env:
       NECDC_CODE: ${{ secrets.NECDC_CODE }}
       NDCDC_LOCAL: ${{ secrets.NDCDC_LOCAL }}
       NDCDC_PAPER: ${{ secrets.NDCDC_PAPER }}
       NDCDC_PERSON: ${{ secrets.NDCDC_PERSON }}
     run: |
       KEY="${NECDC_CODE:-${NDCDC_LOCAL:-${NDCDC_PAPER:-$NDCDC_PERSON}}}"; export NEC_API_KEY="$KEY"
       python scripts/candidate_pipeline/fetch_nec_candidates.py --log-raw
   ```

   - `--log-raw` 플래그를 포함하여 첫 실행 시 raw 응답을 기록한다 (Pitfall 2 대응).
   - 별도 날짜 조건 `if:`를 yml에 넣지 않는다. 날짜 게이팅은 Python 스크립트 내에서 처리한다 (Task 1의 `datetime(2026, 5, 14)` 체크). 이유: GitHub Actions의 `if:` 조건으로 날짜를 비교하면 타임존 문제가 발생할 수 있다.
   - `PYTHONPATH` 환경변수 추가: `PYTHONPATH: scripts:scripts/candidate_pipeline` (nec_precand_sync import를 위해 필요).

2. **Commit 메시지 형식** per D-04: 기존 `git commit -m "chore: auto-update candidate data $(date +%Y-%m-%d)"` 유지. 변경 없음.

3. **수동 dispatch 지원** per D-02: 이미 `workflow_dispatch:` 트리거가 있으므로 추가 작업 없음.

4. **diff 체크 범위**: 기존 `git diff --quiet data/candidates/` 패턴에 이미 모든 candidates 파일이 포함되므로 변경 없음.

주의사항:
- update-polls.yml과 분리된 별도 워크플로우이다 (per D-01). update-polls.yml은 건드리지 않는다.
- pip install 단계에 새 패키지 추가 불필요 (urllib은 표준라이브러리).
  </action>
  <verify>
    <automated>cd /Users/isawufo/Desktop/AI-cording-project/korea-local-election && python -c "import yaml; y=yaml.safe_load(open('.github/workflows/update-candidates.yml')); steps=[s['name'] for s in y['jobs']['update']['steps'] if 'name' in s]; print('\n'.join(steps)); assert 'Fetch NEC official candidates' in steps" 2>&1 || (echo 'yaml 미설치 — grep 대체'; grep -c 'Fetch NEC official candidates' .github/workflows/update-candidates.yml)</automated>
  </verify>
  <done>
    - update-candidates.yml에 "Fetch NEC official candidates" 단계가 0-c 이후, 1단계 이전에 삽입됨
    - NEC_API_KEY 환경변수와 PYTHONPATH 설정 포함
    - --log-raw 플래그 포함
    - 기존 cron(매일 09:00 KST) + workflow_dispatch 트리거 유지
  </done>
</task>

</tasks>

<verification>
1. `python scripts/candidate_pipeline/fetch_nec_candidates.py --dry-run` 실행 시 에러 없이 "5/14 이전" 경고 메시지 출력
2. `data/candidates/unmatched_candidates.json` 파일이 유효한 JSON
3. `.github/workflows/update-candidates.yml`에 NEC 수집 단계 존재
4. `nec_precand_sync` import가 정상 동작 (ImportError 없음)
</verification>

<success_criteria>
- fetch_nec_candidates.py가 NEC API를 호출할 수 있는 상태 (5/14 이전에는 날짜 게이팅으로 스킵)
- 3개 선거 유형(광역단체장/교육감/기초단체장) 수집 로직 구현 완료
- ballotNumber 필드 추출 시도 + raw 로깅으로 필드명 확인 준비 완료
- GitHub Actions 워크플로우에 NEC 수집 단계 삽입 완료
- CAND-01 요건: "본후보 등록 후 candidates 데이터가 공식 후보(NOMINATED)로 자동 전환된다" 달성 준비 완료
</success_criteria>

<output>
After completion, create `.planning/phases/06-본후보-등록-대응/06-01-SUMMARY.md`
</output>
