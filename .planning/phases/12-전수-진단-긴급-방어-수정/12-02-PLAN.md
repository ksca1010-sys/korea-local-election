---
phase: 12-전수-진단-긴급-방어-수정
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/candidate_pipeline/nec_precand_sync.py
  - scripts/fetch_candidate_disclosures.py
  - scripts/candidate_pipeline/fetch_nec_candidates.py
  - scripts/data_health_check.py
autonomous: true
requirements:
  - CRASH-01
  - CRASH-02

must_haves:
  truths:
    - "nec_precand_sync.py가 API 응답에 body 키가 없을 때 KeyError 대신 경고 로그를 출력하고 루프를 스킵한다"
    - "fetch_candidate_disclosures.py가 빈 name 레코드를 data/ JSON에 저장하지 않는다"
    - "fetch_nec_candidates.py의 dict comprehension이 빈 name 후보를 키로 사용하지 않는다"
    - "data_health_check.py가 name 키 없는 레코드에서 KeyError를 발생시키지 않는다"
    - "모든 수정된 Python 파일이 구문 오류 없이 파싱된다"
  artifacts:
    - path: "scripts/candidate_pipeline/nec_precand_sync.py"
      provides: "NEC API 응답 body 안전 접근"
      contains: ".get(\"body\")"
    - path: "scripts/fetch_candidate_disclosures.py"
      provides: "빈 name 레코드 필터링"
      contains: "not.*name.*strip"
    - path: "scripts/candidate_pipeline/fetch_nec_candidates.py"
      provides: "dict comprehension 빈 name 방어"
      contains: "if.*get.*name.*strip"
    - path: "scripts/data_health_check.py"
      provides: "안전한 name 필드 접근"
      contains: ".get(\"name\")"
  key_links:
    - from: "scripts/candidate_pipeline/nec_precand_sync.py"
      to: "NEC API 응답"
      via: ".get() 체인으로 body 접근"
      pattern: "data\\.get.*response.*get.*body"
    - from: "scripts/fetch_candidate_disclosures.py"
      to: "data/candidates/disclosures.json"
      via: "저장 전 빈 name 필터"
      pattern: "not.*name.*strip"
---

<objective>
Python 파이프라인 스크립트에 방어 코드를 적용하여, API 응답 필드 누락 시 KeyError 크래시를 방지하고 빈 name 레코드가 data/ JSON에 저장되지 않도록 한다.

Purpose: 무인 파이프라인 실행 시 크래시 원인 제거 (5/14 본후보 수집 전 완료 필수)
Output: 4개 Python 스크립트에 방어 코드 적용, 구문 검증 완료

수정 대상 스크립트 선정 근거 (RESEARCH.md 기반):
- 나머지 파이프라인 스크립트(factcheck_byelection.py, factcheck_candidates.py, fetch_byelection.py, sync_overview_candidates.py 등)는 RESEARCH.md 전수 분석에서 이미 `.get()` 패턴 또는 빈 name 검증 로직을 보유한 것으로 확인됨. 따라서 아래 4개 스크립트만 수정 대상이며 이것이 "모든 파이프라인 스크립트" 요구사항을 충족한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-전수-진단-긴급-방어-수정/12-RESEARCH.md

@scripts/candidate_pipeline/nec_precand_sync.py
@scripts/fetch_candidate_disclosures.py
@scripts/candidate_pipeline/fetch_nec_candidates.py
@scripts/data_health_check.py
</context>

<interfaces>
<!-- 기존 방어 코딩 패턴 (참조용 — factcheck_byelection.py에서 발췌) -->

올바른 패턴 (이미 구현된 곳):
```python
# factcheck_byelection.py:78
if not c.get("name"):
    continue

# fetch_byelection.py:300-301
name = nc.get("name", "").strip()
if not name:
    continue
```

위험 패턴 (수정 대상):
```python
# nec_precand_sync.py:93
body = data["response"]["body"]  # KeyError 위험

# fetch_nec_candidates.py:257
existing_by_name = {c["name"]: c for c in existing_list}  # 빈 name 오염
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: nec_precand_sync.py KeyError 방어 + data_health_check.py name 안전 접근</name>
  <files>scripts/candidate_pipeline/nec_precand_sync.py, scripts/data_health_check.py</files>
  <action>
**nec_precand_sync.py (CRASH-01 핵심 수정)**

line 93 부근의 `body = data["response"]["body"]` 를 안전한 .get() 체인으로 교체한다:

```python
# AS-IS (line 93):
body = data["response"]["body"]

# TO-BE:
body = data.get("response", {}).get("body")
if body is None:
    print(f"  [WARN] NEC API 응답에 'body' 키 없음 — 스킵 (typecode={sg_typecode})")
    print(f"  [DEBUG] 실제 응답 키: {list(data.keys())}")
    break
```

기존 header 체크 로직(`header = data.get("response", {}).get("header", {})`)은 그대로 유지한다. body 접근만 .get() 체인으로 변경.

**data_health_check.py (CRASH-01 보조 수정)**

line 177, 224 부근의 `name = c["name"]` 를 안전한 .get() 접근으로 교체:

```python
# AS-IS:
name = c["name"]

# TO-BE:
name = c.get("name", "")
if not name:
    print(f"[WARN] name 키 누락 레코드 스킵: {list(c.keys())[:5]}")
    continue
```

두 위치(line 177, 224) 모두 수정한다. 파일을 먼저 읽어 정확한 라인 번호를 확인한 후 수정한다.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/candidate_pipeline/nec_precand_sync.py').read()); print('nec_precand_sync.py: OK')" && python3 -c "import ast; ast.parse(open('scripts/data_health_check.py').read()); print('data_health_check.py: OK')"</automated>
  </verify>
  <done>nec_precand_sync.py의 body 직접 접근이 .get() 체인으로 교체됨. data_health_check.py의 c["name"] 직접 접근이 .get("name", "")으로 교체됨. 두 파일 모두 구문 오류 없음.</done>
</task>

<task type="auto">
  <name>Task 2: fetch_candidate_disclosures.py + fetch_nec_candidates.py 빈 name 방어</name>
  <files>scripts/fetch_candidate_disclosures.py, scripts/candidate_pipeline/fetch_nec_candidates.py</files>
  <action>
**fetch_candidate_disclosures.py (CRASH-02 수정)**

item_to_disclosure() 호출 결과를 저장하는 부분에 빈 name 필터를 추가한다. RESEARCH.md의 권고 패턴을 적용:

```python
# AS-IS:
result[rkey] = [item_to_disclosure(it) for it in items]

# TO-BE:
raw_records = [item_to_disclosure(it) for it in items]
valid_records = []
for rec in raw_records:
    if not rec.get("name", "").strip():
        print(f"[WARN] 빈 name 공보물 레코드 스킵 ({election_type}/{rkey})")
        continue
    valid_records.append(rec)
result[rkey] = valid_records
```

파일을 먼저 읽어 `result[rkey] = [item_to_disclosure(...)` 패턴이 나타나는 정확한 위치를 확인한 후 수정한다. 여러 곳에 있을 수 있으므로 모두 수정한다.

**fetch_nec_candidates.py (CRASH-02 수정)**

dict comprehension에서 빈 name 후보를 필터링한다. RESEARCH.md에 따르면 line 257, 325, 433, 434 부근에 `{c["name"]: c for c in ...}` 패턴이 있다:

```python
# AS-IS:
existing_by_name = {c["name"]: c for c in existing_list}

# TO-BE:
existing_by_name = {
    c["name"]: c for c in existing_list
    if isinstance(c, dict) and c.get("name", "").strip()
}
```

파일을 읽어 `c["name"]: c for c in` 패턴이 나타나는 모든 위치를 grep으로 확인하고 전부 수정한다. 또한 `_convert_nec_item()` 함수 내 `"name": item.get("name", "")` 부분에 빈 name 경고 로그를 추가한다:

```python
# _convert_nec_item() 내부, name 할당 직후:
name = item.get("name", "").strip()
if not name:
    print(f"[WARN] NEC API 빈 name 후보 스킵: {item.get('jdName', '')} / {item.get('sggName', '')}")
    return None  # 또는 호출부에서 None 필터링
```

호출부에서 None을 필터링하는 것이 더 안전하다면 그 방식을 선택한다. 기존 코드 흐름을 파악한 후 최소 침습적으로 수정한다.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('scripts/fetch_candidate_disclosures.py').read()); print('fetch_candidate_disclosures.py: OK')" && python3 -c "import ast; ast.parse(open('scripts/candidate_pipeline/fetch_nec_candidates.py').read()); print('fetch_nec_candidates.py: OK')"</automated>
  </verify>
  <done>fetch_candidate_disclosures.py에 빈 name 레코드 필터가 추가됨. fetch_nec_candidates.py의 dict comprehension 전부에 빈 name 필터가 추가됨. 두 파일 모두 구문 오류 없음.</done>
</task>

</tasks>

<verification>
- 4개 Python 파일 모두 `python3 -c "import ast; ast.parse(open(...).read())"` 통과
- nec_precand_sync.py에 `data["response"]["body"]` 직접 접근이 더 이상 없음 (grep 확인)
- fetch_nec_candidates.py에 `c["name"]: c for c in` 패턴에 모두 빈 name 필터가 추가됨
- fetch_candidate_disclosures.py에 빈 name 레코드 필터가 존재함
</verification>

<success_criteria>
1. 4개 Python 스크립트 모두 구문 오류 없이 파싱됨
2. `grep -n 'data\["response"\]\["body"\]' scripts/candidate_pipeline/nec_precand_sync.py` 결과가 0건
3. `grep -n 'c\["name"\]: c for' scripts/candidate_pipeline/fetch_nec_candidates.py`에서 모든 매치에 `.strip()` 필터가 동반됨
4. `grep -n 'not.*name.*strip' scripts/fetch_candidate_disclosures.py` 결과가 1건 이상
</success_criteria>

<output>
After completion, create `.planning/phases/12-전수-진단-긴급-방어-수정/12-02-SUMMARY.md`
</output>
