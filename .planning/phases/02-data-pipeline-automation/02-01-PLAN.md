---
plan: 02-01
phase: 02
wave: 1
depends_on: []
files_modified:
  - scripts/poll_audit_pdf.py
  - data/polls/audit_report.json
  - scripts/audit_numeric_fields.py
  - scripts/data_health_check.py
  - package.json
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "poll_audit_pdf.py --batch가 data/polls/pdfs/ 내 모든 PDF를 순회하여 audit_report.json을 생성한다"
    - "audit_numeric_fields.py가 pollSource 없는 부동소수점 support 값을 감지하면 exit code 1을 반환한다"
    - "npm run check:polls:source 또는 pre-deploy 시 수치 검증이 자동 실행된다"
  artifacts:
    - path: "data/polls/audit_report.json"
      provides: "PDF 감사 보고서 (전체 PDF 일괄 처리 결과)"
    - path: "scripts/audit_numeric_fields.py"
      provides: "pollSource 없는 부동소수점 퍼센트 값 탐지 스크립트"
  key_links:
    - from: "scripts/poll_audit_pdf.py --batch"
      to: "data/polls/audit_report.json"
      via: "JSON 파일 출력"
    - from: "scripts/audit_numeric_fields.py"
      to: "data/polls/polls.json"
      via: "JSON 읽기 + exit code"
    - from: "package.json check:polls:source"
      to: "scripts/audit_numeric_fields.py"
      via: "npm script"
---

# Plan 02-01: PDF 일괄 감사 + 수치 검증 자동화 + pre-deploy 훅

## Goal

70+ 미처리 여론조사 PDF를 일괄 처리하여 `audit_report.json`을 생성하고, `pollSource` 없는 부동소수점 퍼센트 값을 자동 감지하는 `audit_numeric_fields.py`를 작성하여 pre-deploy 파이프라인에 연동한다.

## must_haves

1. `data/polls/audit_report.json`이 존재하고 `checked` 값이 20 이상이다
2. `python3 scripts/audit_numeric_fields.py`가 pollSource 없는 support 값 발견 시 exit code 1을 반환한다
3. `package.json`에 `check:polls:source` 스크립트가 존재한다

## Tasks

### Task 1: poll_audit_pdf.py에 --batch 모드 추가 및 실행 (DATA-01)

<read_first>
- scripts/poll_audit_pdf.py — 현재 argparse 구조, extract_support_from_pdf(), compare_results(), main() 흐름 파악
- data/polls/audit_report.json — 기존 보고서 구조 확인 (generated, checked, mismatches 필드)
</read_first>

<action>
1. `scripts/poll_audit_pdf.py`의 `main()` 함수에 `--batch` 인자를 추가한다:
   ```python
   parser.add_argument("--batch", action="store_true", help="전체 PDF 일괄 처리, audit_report.json 생성")
   ```

2. `--batch` 모드일 때의 로직을 `main()` 끝에 추가한다:
   - 기존 `all_issues` 리스트를 활용하여 `audit_report.json`을 생성한다
   - 보고서 형식:
     ```python
     report = {
         "generated": datetime.now().isoformat(),
         "checked": checked,
         "no_pdf": no_pdf,
         "mismatched": mismatched,
         "mismatches": all_issues
     }
     report_path = BASE / "data" / "polls" / "audit_report.json"
     report_path.write_text(json.dumps(report, ensure_ascii=False, indent=2), encoding="utf-8")
     print(f"audit_report.json 저장: {report_path}")
     ```
   - `--batch` 모드에서는 `--limit`를 무시하고 전체 PDF를 처리한다 (이미 `args.limit` 기본값이 0이므로, `--batch`일 때 `args.limit = 0`을 강제 설정)

3. `--batch` 모드에서 `--fix`가 함께 지정되면 교정도 수행한다 (기존 `--fix` 로직 그대로 유지)

4. 스크립트 실행:
   ```bash
   cd /Users/isawufo/Desktop/AI-cording-project/korea-local-election
   python3 scripts/poll_audit_pdf.py --batch
   ```
   - pdfplumber가 설치되어 있지 않으면 `pip install pdfplumber` 실행
   - 실행 후 `data/polls/audit_report.json`이 갱신되었는지 확인

5. datetime import가 파일 상단에 없으면 추가: `from datetime import datetime`
</action>

<acceptance_criteria>
- [ ] `grep -c "batch" scripts/poll_audit_pdf.py` >= 3 (argparse 정의 + 조건 분기 + 보고서 저장)
- [ ] `python3 scripts/poll_audit_pdf.py --batch` 실행 후 exit code 0
- [ ] `python3 -c "import json; d=json.load(open('data/polls/audit_report.json')); print(d['checked'])"` 출력이 20 이상
- [ ] `data/polls/audit_report.json`에 `generated`, `checked`, `mismatches` 키가 존재
</acceptance_criteria>

### Task 2: audit_numeric_fields.py 작성 + pre-deploy 훅 연동 (DATA-02, DATA-03)

<read_first>
- data/polls/polls.json (첫 50줄) — results 배열 내 support 필드 구조 확인
- scripts/data_health_check.py (첫 80줄) — 기존 검증 패턴, BASE_DIR 경로 확인
- package.json — 기존 scripts 확인
</read_first>

<action>
1. `scripts/audit_numeric_fields.py` 신규 작성:

```python
#!/usr/bin/env python3
"""
pollSource 없는 부동소수점 퍼센트 값 감지

polls.json의 모든 results를 순회하면서:
- support 필드가 부동소수점(float)인데
- 해당 poll에 sourceUrl 또는 nttId(NESDC 등록번호)가 없으면
exit code 1을 반환한다.

용도: pre-deploy 훅에서 검증되지 않은 수치가 배포되는 것을 방지
"""

import json
import sys
from pathlib import Path

BASE = Path(__file__).resolve().parent.parent
POLLS_PATH = BASE / "data" / "polls" / "polls.json"


def check_polls():
    """pollSource(sourceUrl 또는 nttId) 없는 support 값 탐지.

    Returns:
        list[dict]: 위반 항목 리스트
    """
    if not POLLS_PATH.exists():
        print("[audit] polls.json 없음 — 건너뜀")
        return []

    data = json.loads(POLLS_PATH.read_text(encoding="utf-8"))
    regions = data.get("regions", {})
    violations = []

    for region_key, poll_list in regions.items():
        for poll in poll_list:
            ntt_id = poll.get("nttId")
            source_url = poll.get("sourceUrl", "")
            has_source = bool(ntt_id) or bool(source_url)

            for result in poll.get("results", []):
                support = result.get("support")
                if support is None:
                    continue
                # 부동소수점 퍼센트 값인데 소스가 없는 경우
                if isinstance(support, float) and not has_source:
                    violations.append({
                        "region": region_key,
                        "candidate": result.get("candidateName", "?"),
                        "support": support,
                        "poll_title": poll.get("title", "?"),
                    })

    return violations


def check_candidates():
    """candidates/*.json 내 support 필드가 pollSource 없이 존재하는지 탐지."""
    candidates_dir = BASE / "data" / "candidates"
    if not candidates_dir.exists():
        return []

    violations = []
    for json_file in candidates_dir.glob("*.json"):
        data = json.loads(json_file.read_text(encoding="utf-8"))
        # candidates JSON 구조: 지역별 candidates 배열
        if isinstance(data, dict):
            for region_key, region_data in data.items():
                if region_key.startswith("_"):
                    continue
                candidates = []
                if isinstance(region_data, list):
                    candidates = region_data
                elif isinstance(region_data, dict):
                    candidates = region_data.get("candidates", [])

                for c in candidates:
                    if not isinstance(c, dict):
                        continue
                    support = c.get("support")
                    poll_source = c.get("pollSource")
                    if support is not None and isinstance(support, (int, float)) and not poll_source:
                        violations.append({
                            "file": json_file.name,
                            "region": region_key,
                            "candidate": c.get("name", c.get("candidateName", "?")),
                            "support": support,
                        })

    return violations


def main():
    poll_violations = check_polls()
    candidate_violations = check_candidates()

    total = len(poll_violations) + len(candidate_violations)

    if poll_violations:
        print(f"\n[audit] polls.json: pollSource 없는 support 값 {len(poll_violations)}건")
        for v in poll_violations[:10]:
            print(f"  - {v['region']} / {v['candidate']}: {v['support']}%")
        if len(poll_violations) > 10:
            print(f"  ... 외 {len(poll_violations) - 10}건")

    if candidate_violations:
        print(f"\n[audit] candidates: pollSource 없는 support 값 {len(candidate_violations)}건")
        for v in candidate_violations[:10]:
            print(f"  - {v['file']} / {v['region']} / {v['candidate']}: {v['support']}%")
        if len(candidate_violations) > 10:
            print(f"  ... 외 {len(candidate_violations) - 10}건")

    if total == 0:
        print("[audit] OK — pollSource 없는 수치 없음")
        sys.exit(0)
    else:
        print(f"\n[audit] FAIL — 총 {total}건의 미검증 수치 발견")
        sys.exit(1)


if __name__ == "__main__":
    main()
```

2. `package.json`의 `scripts`에 추가:
   ```json
   "check:polls:source": "python3 scripts/audit_numeric_fields.py"
   ```

3. `scripts/data_health_check.py`의 끝부분 또는 메인 검증 루프에서 `audit_numeric_fields.py`를 서브프로세스로 호출하는 단계를 추가한다:
   - `data_health_check.py` 전체를 읽고, 메인 검증 흐름에 추가
   - 만약 `data_health_check.py`가 deploy 훅으로 쓰이지 않는 구조라면, `package.json`의 `check:all` 스크립트가 있으므로 `scripts/run_quality_gate.js`에 audit_numeric_fields 호출을 추가하거나, 독립 npm script로만 유지
   - 최소한 `package.json`의 `deploy` 스크립트(`bash deploy.sh`)에서 `deploy.sh` 내부에 `python3 scripts/audit_numeric_fields.py || exit 1` 라인을 추가하는 방식으로 연동
   - `deploy.sh`가 없으면 `package.json`의 `deploy` 스크립트를 `"python3 scripts/audit_numeric_fields.py && bash deploy.sh"` 로 변경

4. 실행하여 현재 상태 확인:
   ```bash
   python3 scripts/audit_numeric_fields.py
   ```
</action>

<acceptance_criteria>
- [ ] `test -f scripts/audit_numeric_fields.py && echo OK` 출력이 OK
- [ ] `grep "check:polls:source" package.json` 출력이 존재
- [ ] `python3 scripts/audit_numeric_fields.py; echo $?` 실행 후 exit code가 0 또는 1 (에러 없이 정상 종료)
- [ ] `grep "audit_numeric_fields" package.json` 또는 `grep "audit_numeric_fields" deploy.sh` 중 하나 이상 존재 (pre-deploy 연동 증거)
- [ ] `head -5 scripts/audit_numeric_fields.py | grep "#!/usr/bin/env python3"` 성공
</acceptance_criteria>

## Verification

```bash
cd /Users/isawufo/Desktop/AI-cording-project/korea-local-election

# DATA-01: audit_report.json 존재 및 checked >= 20
python3 -c "import json; d=json.load(open('data/polls/audit_report.json')); assert d['checked'] >= 20, f'checked={d[\"checked\"]}'; print(f'OK: {d[\"checked\"]} PDFs checked')"

# DATA-02: audit_numeric_fields.py 실행 가능
python3 scripts/audit_numeric_fields.py; echo "Exit code: $?"

# DATA-03: pre-deploy 훅 연동 확인
grep "audit_numeric_fields" package.json || grep "audit_numeric_fields" deploy.sh 2>/dev/null
```
