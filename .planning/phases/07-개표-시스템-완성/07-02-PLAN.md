---
phase: 07-개표-시스템-완성
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - js/app.js
autonomous: true
requirements: [ELEC-02]
user_setup:
  - service: cloudflare-workers
    why: "wrangler dev 로컬 테스트 (배포 불필요)"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "wrangler dev 실행 후 curl /results가 fixture JSON을 반환한다"
    - "curl /health가 {status: ok}를 반환한다"
    - "_pollElectionResults() 성공 시 #election-banner 텍스트가 개표율로 업데이트된다"
    - "election_night 페이즈가 아닐 때 배너가 숨겨진다"
  artifacts:
    - path: "js/app.js"
      provides: "_updateElectionBanner(data) 함수"
      contains: "_updateElectionBanner"
  key_links:
    - from: "js/app.js (_pollElectionResults)"
      to: "js/app.js (_updateElectionBanner)"
      via: "폴링 성공 후 배너 텍스트 업데이트 호출"
      pattern: "_updateElectionBanner\\(data\\)"
    - from: "js/app.js (_updateElectionBanner)"
      to: "index.html (#election-banner .banner-text)"
      via: "DOM textContent 직접 업데이트"
      pattern: "banner-text.*textContent"
---

<objective>
Worker 통합 테스트를 실행하고, 개표 배너 동적 업데이트 코드를 app.js에 추가한다.

Purpose: ELEC-02 달성 (Worker 통합 테스트 완료). 선거 당일 개표 진행률이 배너에 실시간 표시되도록 한다.
Output: Worker 통합 테스트 통과 로그, _updateElectionBanner() 구현
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-개표-시스템-완성/07-CONTEXT.md
@.planning/phases/07-개표-시스템-완성/07-RESEARCH.md
@.planning/phases/07-개표-시스템-완성/07-01-SUMMARY.md

@workers/election-night/index.js
@workers/election-night/wrangler.toml
@workers/election-night/fixtures/2022-sample.json
@js/app.js (line 996~1110)

<interfaces>
<!-- js/app.js Election Night 섹션 (line 996~1110) -->
From js/app.js:
```javascript
// Line 998: Worker URL
const ELECTION_NIGHT_WORKER = 'https://election-night.ksca1010.workers.dev';
let _electionNightPoller = null;
let _manualFallbackMode = false;

// Line 1028-1044: _pollElectionResults()
async function _pollElectionResults() {
    try {
        const resp = await fetch(`${ELECTION_NIGHT_WORKER}/results`, { signal: AbortSignal.timeout(10_000) });
        if (!resp.ok) throw new Error(`Worker ${resp.status}`);
        const data = await resp.json();
        if (data.error) throw new Error(data.error);
        if (typeof MapModule !== 'undefined' && MapModule.applyElectionNightLayer) {
            MapModule.applyElectionNightLayer(data);
        }
        _setManualFallbackMode(false);
        // ← 여기에 _updateElectionBanner(data) 호출 추가
    } catch (err) {
        console.warn('[election_night] Worker 응답 실패, 수동 모드 전환', err);
        _setManualFallbackMode(true);
    }
}

// Line 1046-1051: _setManualFallbackMode()
function _setManualFallbackMode(active) {
    _manualFallbackMode = active;
    const container = document.getElementById('manual-fallback-container');
    if (!container) return;
    container.style.display = active ? 'block' : 'none';
}
```

From index.html:
```html
<!-- Line 95 -->
<div id="election-banner" class="election-banner" aria-live="polite" style="display:none"></div>
```

From js/sidebar.js (renderElectionBanner):
```javascript
// Line 239-268: election_night phase시 배너 활성화
// banner.innerHTML = '<div class="election-banner-content banner-{type}"><span class="banner-icon">...</span><span class="banner-text">...</span></div>'
// banner.style.display = '' (표시) 또는 'none' (숨김)
```

From workers/election-night/fixtures/2022-sample.json (KV 응답 스키마):
```json
{
  "fetchedAt": "2022-06-01T22:00:00+09:00",
  "regions": {
    "seoul": { "countRate": 100, "leadingCandidate": "오세훈", "leadingParty": "ppp", "leadingVoteRate": 59.0, "declared": true },
    ...17개 지역
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Worker 통합 테스트 (ELEC-02)</name>
  <files>workers/election-night/index.js</files>
  <action>
Worker 통합 테스트를 실행하여 ELEC-02를 달성한다. `workers/election-night/index.js` 파일 자체는 수정하지 않는다 (Plan 01에서 parseNECResponse skeleton 이미 구현됨).

**테스트 절차 (per D-02, RESEARCH Pattern 2):**

1. **unit test 확인:**
   ```bash
   node workers/election-night/test-parser.cjs
   ```
   8/8 통과 확인.

2. **wrangler dev 시작:**
   ```bash
   cd workers/election-night
   npx wrangler dev --test-scheduled
   ```
   백그라운드로 실행.

3. **헬스체크:**
   ```bash
   curl http://localhost:8787/health
   ```
   `{"status":"ok"}` 확인.

4. **KV에 fixture 직접 주입 (per RESEARCH Pitfall 1 — 시간 범위 조건 우회):**
   scheduled() 핸들러는 KST 18:00~24:00에만 실행되므로, KV에 fixture를 직접 주입한다.
   ```bash
   npx wrangler kv key put "latest" "$(cat fixtures/2022-sample.json)" --binding=ELECTION_RESULTS --local
   ```

5. **/results 응답 검증:**
   ```bash
   curl http://localhost:8787/results
   ```
   - 응답이 2022-sample.json의 내용과 일치하는지 확인
   - `regions` 객체에 17개 지역이 모두 존재하는지 확인
   - 각 region에 `countRate`, `leadingCandidate`, `leadingParty`, `leadingVoteRate`, `declared` 필드 존재 확인

6. **wrangler dev 종료.**

**SUMMARY에 기록할 것:**
- test-parser.cjs 실행 결과 (8/8)
- curl /health 응답
- curl /results 응답 (첫 3개 region만 발췌)
- KV 주입 방식으로 시간 범위 조건을 우회했음을 명시
  </action>
  <verify>
    <automated>node workers/election-night/test-parser.cjs</automated>
  </verify>
  <done>
- test-parser.cjs 8/8 passed
- curl /health → {"status":"ok"}
- curl /results → 17개 region 포함 JSON 반환
- ELEC-02 달성
  </done>
</task>

<task type="auto">
  <name>Task 2: _updateElectionBanner(data) 구현 + _pollElectionResults 연결</name>
  <files>js/app.js</files>
  <action>
`js/app.js`의 Election Night 섹션 (line 996~1070 부근)에 다음 코드를 추가한다.

**1. _updateElectionBanner(data) 함수 추가 (per D-03, D-04):**

`_setManualFallbackMode()` 함수 (line 1046~1051) 바로 아래에 새 함수를 추가한다:

```javascript
function _updateElectionBanner(data) {
    const banner = document.getElementById('election-banner');
    if (!banner) return;
    const regions = data?.regions || {};
    const rates = Object.values(regions).map(r => r.countRate).filter(v => typeof v === 'number');
    if (!rates.length) return;
    const avgRate = (rates.reduce((a, b) => a + b, 0) / rates.length).toFixed(1);
    const fetchedAt = data?.fetchedAt ? new Date(data.fetchedAt) : new Date();
    const hhmm = fetchedAt.toLocaleTimeString('ko-KR', { hour: '2-digit', minute: '2-digit', hour12: false });
    const textEl = banner.querySelector('.banner-text');
    if (textEl) {
        textEl.textContent = `개표 진행 중 — 전체 ${avgRate}% (${hhmm} 기준)`;
    }
}
```

핵심:
- 17개 지역 `countRate` 평균 계산 (per D-03)
- `fetchedAt` 시각을 HH:MM 형태로 표시 (per D-03: "포단 HH:MM 기준")
- `banner.querySelector('.banner-text')` — sidebar.js `renderElectionBanner()`가 생성하는 `<span class="banner-text">` 를 찾아 textContent 교체
- 배너 표시/숨김은 sidebar.js `renderElectionBanner()`가 이미 처리 (per RESEARCH Pattern 3 line 297~299). `display:block/none` 코드 추가 불필요.

**2. _pollElectionResults()에 _updateElectionBanner 호출 추가:**

`_pollElectionResults()` 함수 내 `_setManualFallbackMode(false);` (현재 line 1039) 바로 위에 추가:
```javascript
_updateElectionBanner(data);
```

즉, `MapModule.applyElectionNightLayer(data)` 호출 → `_updateElectionBanner(data)` 호출 → `_setManualFallbackMode(false)` 순서.

**3. _updateElectionBanner를 Public API에 추가:**

`return { ... }` 블록 (line 1093~)에 디버그용으로 추가:
```javascript
_updateElectionBanner,
```

**주의:**
- sidebar.js 수정 금지 — renderElectionBanner()는 건드리지 않음
- map.js 수정 금지 — applyElectionNightLayer 이미 완성
- 다른 탭 파일 수정 금지 (CLAUDE.md 수정 규칙)
- app.js의 Election Night 섹션만 수정 (line 996~1110 범위)
  </action>
  <verify>
    <automated>grep -n "_updateElectionBanner" js/app.js | head -5</automated>
  </verify>
  <done>
- _updateElectionBanner(data) 함수가 app.js에 존재
- _pollElectionResults() 내에서 _updateElectionBanner(data) 호출
- 배너 텍스트: "개표 진행 중 — 전체 XX% (HH:MM 기준)" 형식
- 17개 지역 countRate 평균 계산 로직 구현
- Public API에 _updateElectionBanner 노출
  </done>
</task>

</tasks>

<verification>
- `node workers/election-night/test-parser.cjs` → 8/8 passed
- `grep "_updateElectionBanner" js/app.js` → 함수 정의 + 호출 존재
- `grep "개표 진행 중" js/app.js` → 배너 텍스트 형식 확인
- `grep "avgRate" js/app.js` → 평균 계산 로직 존재
</verification>

<success_criteria>
1. Worker 통합 테스트: curl /results가 17개 region JSON 반환 (KV 주입 방식)
2. test-parser.cjs 8/8 passed
3. _updateElectionBanner()가 countRate 평균 → 배너 텍스트 업데이트
4. _pollElectionResults() 성공 시 _updateElectionBanner(data) 호출
</success_criteria>

<output>
After completion, create `.planning/phases/07-개표-시스템-완성/07-02-SUMMARY.md`
</output>
