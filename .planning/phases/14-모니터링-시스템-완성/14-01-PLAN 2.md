---
phase: 14-모니터링-시스템-완성
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/monitor-failures.yml
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - MON-01
  - MON-02

must_haves:
  truths:
    - "13개 전체 유효 워크플로우가 monitor-failures.yml의 workflow_run 트리거 목록에 포함된다"
    - "공보물 데이터 수집 (선관위 API)가 실패하면 monitor_failures.py가 실행되어 Issue가 자동 생성된다"
    - "Update Local Council Members가 실패하면 monitor_failures.py가 실행된다"
    - "Update Local Media Pool가 실패하면 monitor_failures.py가 실행된다"
  artifacts:
    - path: ".github/workflows/monitor-failures.yml"
      provides: "workflow_run 트리거 — 13개 워크플로우 전체 포함"
      contains: "공보물 데이터 수집 (선관위 API)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MON-01, MON-02 완료 표시"
      contains: "[x] **MON-01**"
  key_links:
    - from: ".github/workflows/monitor-failures.yml"
      to: ".github/workflows/fetch-disclosures.yml"
      via: "workflow_run 트리거 — name 필드 정확 일치"
      pattern: "공보물 데이터 수집 \\(선관위 API\\)"
    - from: ".github/workflows/monitor-failures.yml"
      to: "scripts/monitor_failures.py"
      via: "github.event.workflow_run.name 인자 전달"
      pattern: "monitor_failures.py.*--workflow"
---

<objective>
monitor-failures.yml의 workflow_run 트리거 목록에 누락된 워크플로우 3개를 추가하여 13개 전체 유효 워크플로우의 실패 감지 커버리지를 완성한다.

Purpose: 현재 10개만 감시 중인 상태에서 fetch-disclosures.yml, update-local-council.yml, update-local-media.yml의 실패가 감지되지 않는 사각지대를 제거한다. monitor_failures.py의 Issue 생성/닫기 로직은 이미 완전 구현되어 있으므로 트리거 목록 확장만으로 MON-01, MON-02 동시 충족이 가능하다.

Output:
- .github/workflows/monitor-failures.yml — workflow_run.workflows 목록에 3개 추가
- .planning/REQUIREMENTS.md — MON-01, MON-02 체크 표시 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.github/workflows/monitor-failures.yml
@scripts/monitor_failures.py
</context>

<interfaces>
<!-- 현재 monitor-failures.yml 트리거 목록 (10개) — 여기에 3개를 추가한다 -->

```yaml
# 현재 on.workflow_run.workflows (10개):
workflows:
  - "Update Candidate Data"
  - "Update Gallup National Poll"
  - "Poll Sync (NESDC)"
  - "Update Election Overview"
  - "Update Election Stats"
  - "Update Governor Status"
  - "Update Mayor Status"
  - "Update Superintendent Status"
  - "Update By-Election Data"
  - "Data Health Check"
```

<!-- 추가할 3개 워크플로우 — 각 yml 파일의 name: 필드와 정확히 일치 -->
<!-- fetch-disclosures.yml → name: 공보물 데이터 수집 (선관위 API)    -->
<!-- update-local-council.yml → name: Update Local Council Members      -->
<!-- update-local-media.yml → name: Update Local Media Pool             -->

<!-- monitor_failures.py 호출 방식 (변경 없음) -->
```bash
python scripts/monitor_failures.py \
  --workflow "${{ github.event.workflow_run.name }}" \
  --run-id "${{ github.event.workflow_run.id }}" \
  --conclusion "${{ github.event.workflow_run.conclusion }}" \
  --run-url "${{ github.event.workflow_run.html_url }}"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: monitor-failures.yml 트리거 목록 확장</name>
  <files>.github/workflows/monitor-failures.yml</files>
  <action>
파일을 먼저 읽어 현재 workflows 목록을 확인한다. 그런 다음 on.workflow_run.workflows 리스트의 "Data Health Check" 항목 아래에 다음 3개 항목을 추가한다:

```yaml
      - "공보물 데이터 수집 (선관위 API)"
      - "Update Local Council Members"
      - "Update Local Media Pool"
```

주의사항:
- 들여쓰기는 기존 목록과 동일하게 유지 (공백 6개 또는 기존 패턴 그대로)
- 워크플로우명은 각 yml 파일의 name: 필드와 정확히 일치해야 한다
  - fetch-disclosures.yml의 name: 공보물 데이터 수집 (선관위 API) — 한국어 + 괄호 그대로
  - update-local-council.yml의 name: Update Local Council Members
  - update-local-media.yml의 name: Update Local Media Pool
- "Monitor Automation Failures" (monitor-failures.yml 자신)는 절대 목록에 추가하지 않는다 (무한 루프 방지)
- Python 스크립트(monitor_failures.py)는 변경하지 않는다

수정 후 파일 전체를 다시 읽어 13개 항목이 올바르게 포함되었는지 확인한다.
  </action>
  <verify>
    <automated>grep -c '^\s*- "' /Users/isawufo/Desktop/AI-cording-project/korea-local-election/.github/workflows/monitor-failures.yml</automated>
  </verify>
  <done>
workflows 목록에 13개 항목이 존재한다. "공보물 데이터 수집 (선관위 API)", "Update Local Council Members", "Update Local Media Pool" 세 항목이 목록에 포함된다.
  </done>
</task>

<task type="auto">
  <name>Task 2: REQUIREMENTS.md MON-01, MON-02 완료 표시</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
.planning/REQUIREMENTS.md를 읽어 MON-01, MON-02 항목을 찾는다.

다음 두 줄을 수정한다:
- `- [ ] **MON-01**:` → `- [x] **MON-01**:`
- `- [ ] **MON-02**:` → `- [x] **MON-02**:`

Traceability 표 하단의 MON-01, MON-02 행도 업데이트한다:
- `| MON-01 | Phase 14 | TBD | Pending |` → `| MON-01 | Phase 14 | 14-01 | Done |`
- `| MON-02 | Phase 14 | TBD | Pending |` → `| MON-02 | Phase 14 | 14-01 | Done |`

다른 항목은 변경하지 않는다.
  </action>
  <verify>
    <automated>python3 -c "
content = open('.planning/REQUIREMENTS.md').read()
assert '[x] **MON-01**' in content, 'MON-01 not checked'
assert '[x] **MON-02**' in content, 'MON-02 not checked'
print('PASS: MON-01 and MON-02 marked complete')
"</automated>
  </verify>
  <done>
REQUIREMENTS.md에서 MON-01, MON-02가 [x]로 표시되고 Traceability 표에 14-01 플랜과 Done 상태가 기록된다.
  </done>
</task>

</tasks>

<verification>
1. monitor-failures.yml 구문 유효성:
   ```
   python3 -c "import yaml; yaml.safe_load(open('.github/workflows/monitor-failures.yml'))"
   ```
   (PyYAML 없으면 cat으로 목록 육안 확인)

2. 누락 3개 워크플로우명 포함 확인:
   ```
   grep "공보물 데이터 수집" .github/workflows/monitor-failures.yml
   grep "Update Local Council Members" .github/workflows/monitor-failures.yml
   grep "Update Local Media Pool" .github/workflows/monitor-failures.yml
   ```
   각 줄이 출력되어야 한다.

3. 자기 참조 없음 확인:
   ```
   grep "Monitor Automation Failures" .github/workflows/monitor-failures.yml
   ```
   출력이 없어야 한다 (또는 name: 줄만 있어야 하며 workflows 목록에는 없어야 한다).

4. REQUIREMENTS.md 완료 확인:
   ```
   grep "MON-0" .planning/REQUIREMENTS.md
   ```
   `[x]` 두 줄이 출력되어야 한다.
</verification>

<success_criteria>
- monitor-failures.yml의 workflow_run.workflows 목록에 13개 항목이 존재한다
- "공보물 데이터 수집 (선관위 API)", "Update Local Council Members", "Update Local Media Pool" 세 항목이 목록에 정확히 포함된다
- "Monitor Automation Failures"는 목록에 없다
- REQUIREMENTS.md에서 MON-01, MON-02가 [x]로 표시된다
- monitor_failures.py는 변경되지 않는다
</success_criteria>

<output>
완료 후 `.planning/phases/14-모니터링-시스템-완성/14-01-SUMMARY.md`를 생성한다.

Summary에 포함할 내용:
- 수정된 파일: .github/workflows/monitor-failures.yml
- 추가된 워크플로우 3개: 공보물 데이터 수집 (선관위 API), Update Local Council Members, Update Local Media Pool
- 최종 커버리지: 13/13 유효 워크플로우
- 충족된 요구사항: MON-01, MON-02
</output>
