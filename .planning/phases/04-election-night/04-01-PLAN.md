---
phase: 04-election-night
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/election-calendar.js
  - workers/election-night/index.js
  - workers/election-night/wrangler.toml
  - workers/election-night/test-parser.js
  - workers/election-night/fixtures/2022-sample.json
autonomous: false
requirements:
  - FEAT-06
user_setup:
  - service: cloudflare-workers
    why: "KV namespace 생성 필요 (Worker가 개표 결과 캐싱)"
    env_vars: []
    dashboard_config:
      - task: "KV namespace 생성"
        location: "npx wrangler kv:namespace create ELECTION_RESULTS -> wrangler.toml에 id 기입"

must_haves:
  truths:
    - "getCurrentPhase()가 2026-06-03 18:00~24:00 KST 사이에 'election_night'을 반환한다"
    - "Worker scheduled()가 NEC 파싱 결과를 KV에 저장한다"
    - "Worker fetch /results가 KV 데이터를 CORS 헤더와 함께 200으로 반환한다"
    - "2022 아카이브 fixture로 파서 단위 테스트가 통과한다"
    - "Worker가 wrangler deploy로 배포되어 실제 URL이 확정된다"
  artifacts:
    - path: "js/election-calendar.js"
      provides: "election_night 페이즈 감지"
      contains: "ELECTION_NIGHT_END"
    - path: "workers/election-night/index.js"
      provides: "Cron 폴링 Worker + API 엔드포인트"
      exports: ["scheduled", "fetch"]
    - path: "workers/election-night/wrangler.toml"
      provides: "Worker 배포 설정"
      contains: "crons"
    - path: "workers/election-night/test-parser.js"
      provides: "NEC 파서 단위 테스트"
      min_lines: 30
  key_links:
    - from: "workers/election-night/index.js"
      to: "env.ELECTION_RESULTS"
      via: "KV put/get"
      pattern: "ELECTION_RESULTS\\.(put|get)"
    - from: "js/election-calendar.js"
      to: "DATES.ELECTION_NIGHT_END"
      via: "getCurrentPhase if-else chain"
      pattern: "election_night"
---

<objective>
Cloudflare Worker 골격 구축 + election_night 페이즈 추가 + 2022 아카이브 기반 파서 테스트 + Worker 배포

Purpose: Worker가 선관위 개표 페이지를 60초마다 폴링하여 KV에 저장하고, 브라우저가 /results 엔드포인트로 접근할 수 있는 서버 측 인프라를 완성한다. election-calendar.js에 election_night 페이즈를 추가하여 브라우저 클라이언트가 개표 모드 진입 시점을 감지한다.

Output: workers/election-night/ 디렉토리 (index.js + wrangler.toml + test-parser.js + fixtures/) + election-calendar.js 수정 + Worker 배포 URL 확정

**NOTE (D-13 경로 명확화):** CONTEXT.md D-13 원문은 `workers/election-night.js`(단일 파일)를 언급하나, Cloudflare Worker는 `wrangler.toml` + `index.js`의 디렉토리 구조가 표준이다. 따라서 `workers/election-night/` 디렉토리 구조를 의도적으로 채택한다. D-13의 핵심 의도("현재 레포 내, 별도 레포 분리 없음")는 그대로 준수한다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-election-night/04-CONTEXT.md
@.planning/phases/04-election-night/04-RESEARCH.md

@js/election-calendar.js
@worker/index.js
@worker/wrangler.toml

<interfaces>
<!-- 기존 election-calendar.js 공개 API (수정 대상) -->
From js/election-calendar.js:
```javascript
const ElectionCalendar = (() => {
    const getKST = () => { /* UTC+9 변환 */ };
    const DATES = {
        VOTE_END: new Date('2026-06-03T18:00:00+09:00'),
        INAUGURATION: new Date('2026-07-01T00:00:00+09:00'),
        // ... 기타 상수
    };
    const getCurrentPhase = () => {
        // if-else chain: VOTE_END 이후 -> POST_ELECTION
        // 여기에 election_night 삽입 필요
    };
    return { getKST, DATES, getCurrentPhase, /* ... */ };
})();
```

From worker/wrangler.toml (기존 Worker 설정 패턴):
```toml
name = "election-news-proxy"
main = "index.js"
compatibility_date = "2024-01-01"
[[kv_namespaces]]
binding = "ANALYTICS"
id = "9b31e01259d849009422757c8ebaae27"
```

From worker/index.js (기존 CORS 패턴):
```javascript
const CORS_HEADERS = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Methods': 'GET, POST, OPTIONS',
  'Access-Control-Allow-Headers': 'Content-Type, Accept',
  'Access-Control-Max-Age': '86400',
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: election_night 페이즈 추가 + Worker 디렉토리 골격 생성</name>
  <files>js/election-calendar.js, workers/election-night/wrangler.toml, workers/election-night/index.js</files>
  <read_first>
    - js/election-calendar.js (수정 대상 -- DATES 상수 + getCurrentPhase if-else chain)
    - worker/wrangler.toml (기존 Worker 설정 패턴 참고)
    - worker/index.js (CORS_HEADERS 패턴 참고)
    - .planning/phases/04-election-night/04-CONTEXT.md (D-01~D-03, D-07, D-13~D-15 결정사항)
    - .planning/phases/04-election-night/04-RESEARCH.md (Pattern 1, Pattern 5, Pattern 6)
  </read_first>
  <action>
**1. js/election-calendar.js 수정 (per D-01, D-02, D-03):**

DATES 상수에 추가 (VOTE_END 바로 아래):
```javascript
ELECTION_NIGHT_END: new Date('2026-06-04T00:00:00+09:00'),
```

getCurrentPhase() if-else chain에서 기존 `if (now < DATES.INAUGURATION) return 'POST_ELECTION';` 줄 바로 위에 삽입:
```javascript
if (now < DATES.ELECTION_NIGHT_END)  return 'election_night';
```

결과적으로 VOTE_END(18:00) 이후 ~ ELECTION_NIGHT_END(24:00) 사이에 'election_night' 반환.
기존 POST_ELECTION은 ELECTION_NIGHT_END ~ INAUGURATION 구간으로 축소됨.

getBannerConfig()의 POST_ELECTION 케이스 내 기존 `if (now < new Date('2026-06-04T06:00:00+09:00'))` 분기는 election_night 기간(18:00~24:00)에 getCurrentPhase()가 POST_ELECTION이 아니라 election_night을 반환하므로 실행되지 않는다. getBannerConfig()에 election_night 케이스를 switch문에 추가:
```javascript
case 'election_night':
    return {
        show: true,
        text: '개표가 진행 중입니다. 실시간 결과를 지도에서 확인하세요.',
        type: 'result',
        icon: 'fas fa-chart-pie',
        link: null
    };
```

isPublicationBanned()는 수정하지 않음 -- getCurrentPhase()와 독립적인 함수이므로 변경 불필요.

**2. workers/election-night/wrangler.toml 생성 (per D-13, D-14, D-15):**

NOTE: D-13 원문은 `workers/election-night.js` 단일 파일을 언급하지만, Cloudflare Worker 표준은 `wrangler.toml` + `index.js`의 디렉토리 구조이다. `workers/election-night/` 디렉토리로 구성한다.

```toml
name = "election-night"
main = "index.js"
compatibility_date = "2024-01-01"

[triggers]
crons = [ "* * * * *" ]

[[kv_namespaces]]
binding = "ELECTION_RESULTS"
id = "<KV_NAMESPACE_ID>"
preview_id = "<DEV_KV_NAMESPACE_ID>"
```
id/preview_id는 placeholder -- 사용자가 `npx wrangler kv:namespace create ELECTION_RESULTS` 실행 후 기입.

**3. workers/election-night/index.js 생성 (Worker 골격):**

CORS_HEADERS 상수 (기존 worker/index.js 패턴 동일):
```javascript
const CORS_HEADERS = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Methods': 'GET, OPTIONS',
  'Access-Control-Allow-Headers': 'Content-Type',
  'Access-Control-Max-Age': '86400',
};
```

NEC_CONFIG 상수:
```javascript
const NEC_CONFIG = {
  USER_AGENT: 'ElectionInfoMap/1.0 (https://korea-local-eletion.pages.dev)',
  ELECTION_ID_2026: '0020260603',
  ELECTION_ID_2022: '0020220601',
  ELECTION_NIGHT_START: new Date('2026-06-03T18:00:00+09:00').getTime(),
  ELECTION_NIGHT_END: new Date('2026-06-04T00:00:00+09:00').getTime(),
};
```

export default 객체:
- `async scheduled(controller, env, ctx)`: KST 시간 체크 -> ELECTION_NIGHT 범위 밖이면 return -> `fetchAndParseNEC(env)` 호출 -> `env.ELECTION_RESULTS.put('latest', JSON.stringify(data), { expirationTtl: 120 })`. try/catch로 감싸고 catch에서 `console.error` 출력. KV put은 단일 호출만 (재시도 루프 금지 -- Pitfall 3).
- `async fetch(request, env)`: OPTIONS -> CORS preflight. `/results` -> KV get('latest') -> 200 + CORS + Content-Type: application/json. `/health` -> 200 + `{"status":"ok"}`. 그 외 -> 404.
- `async fetchAndParseNEC(env)`: **stub 구현** -- 2022 아카이브 AJAX 엔드포인트 확인 전까지는 빈 스키마 반환. 함수 시그니처와 반환 형식만 확정:
```javascript
async function fetchAndParseNEC(env) {
  // TODO: 2022 아카이브 Chrome DevTools 캡처 후 실제 URL 기입
  // 마감: 2026-05-26 (D-08)
  const NEC_URL = ''; // placeholder
  if (!NEC_URL) {
    return { fetchedAt: new Date().toISOString(), regions: {}, _source: 'stub', _parserVersion: '0.0' };
  }
  const resp = await fetch(NEC_URL, {
    headers: {
      'User-Agent': NEC_CONFIG.USER_AGENT,  // D-07
      'Accept': 'text/html',
      'Referer': 'https://info.nec.go.kr/',
    }
  });
  const html = await resp.text();
  return parseNECResponse(html);
}
```

parseNECResponse(html) stub:
```javascript
function parseNECResponse(html) {
  // TODO: 2022 아카이브 응답 포맷 확인 후 구현
  // 반환 스키마 (per RESEARCH.md Pattern 6):
  return {
    fetchedAt: new Date().toISOString(),
    electionId: NEC_CONFIG.ELECTION_ID_2026,
    sgTypecode: '11',
    regions: {},
    _source: 'info.nec.go.kr',
    _parserVersion: '1.0',
  };
}
```

regions 객체의 각 값은 이 스키마를 따른다:
```javascript
// { countRate: number, leadingCandidate: string, leadingParty: string, leadingVoteRate: number, declared: boolean }
```
`declared: true`는 선관위 공식 당선 플래그만 (D-11, 헌법 제2조). 수학적 추정으로 declared를 true로 설정하는 코드 금지.
  </action>
  <verify>
    <automated>
node -e "
const fs = require('fs');
// 1. election-calendar.js에 ELECTION_NIGHT_END 존재
const cal = fs.readFileSync('js/election-calendar.js', 'utf8');
console.assert(cal.includes('ELECTION_NIGHT_END'), 'ELECTION_NIGHT_END missing');
console.assert(cal.includes(\"return 'election_night'\"), 'election_night phase missing');
console.assert(cal.includes('2026-06-04T00:00:00+09:00'), 'Wrong date for ELECTION_NIGHT_END');
// 2. Worker 파일 존재
console.assert(fs.existsSync('workers/election-night/index.js'), 'Worker index.js missing');
console.assert(fs.existsSync('workers/election-night/wrangler.toml'), 'wrangler.toml missing');
// 3. wrangler.toml 내용 확인
const toml = fs.readFileSync('workers/election-night/wrangler.toml', 'utf8');
console.assert(toml.includes('crons'), 'crons trigger missing');
console.assert(toml.includes('ELECTION_RESULTS'), 'KV binding missing');
// 4. Worker index.js 내용 확인
const worker = fs.readFileSync('workers/election-night/index.js', 'utf8');
console.assert(worker.includes('scheduled'), 'scheduled handler missing');
console.assert(worker.includes('/results'), '/results route missing');
console.assert(worker.includes('User-Agent'), 'User-Agent header missing (D-07)');
console.assert(worker.includes('declared'), 'declared field missing in schema');
console.assert(!worker.includes('역전불가') && !worker.includes('mathEstimate'), 'No estimation code allowed (D-11)');
console.log('Task 1 PASS: all assertions passed');
"
    </automated>
  </verify>
  <acceptance_criteria>
    - js/election-calendar.js DATES 객체에 `ELECTION_NIGHT_END: new Date('2026-06-04T00:00:00+09:00')` 존재
    - getCurrentPhase() if-else chain에서 `return 'election_night'`이 `return 'POST_ELECTION'` 바로 위에 위치
    - getBannerConfig() switch문에 `case 'election_night':` 케이스 존재
    - workers/election-night/index.js에 `export default { scheduled, fetch }` 구조
    - workers/election-night/index.js에 `ELECTION_RESULTS.put` 및 `ELECTION_RESULTS.get` 호출
    - workers/election-night/index.js에 `User-Agent` 헤더 설정 (D-07)
    - workers/election-night/index.js에 hardcoded 개표율/득표율 없음 (헌법 제2조)
    - workers/election-night/wrangler.toml에 `crons = [ "* * * * *" ]` 존재
    - workers/election-night/wrangler.toml에 `binding = "ELECTION_RESULTS"` 존재
  </acceptance_criteria>
  <done>election-calendar.js에 election_night 페이즈 추가 완료. Worker 골격(index.js + wrangler.toml)이 workers/election-night/에 생성됨. fetchAndParseNEC는 stub 상태(NEC URL placeholder).</done>
</task>

<task type="auto">
  <name>Task 2: 2022 아카이브 fixture 생성 + 파서 단위 테스트 작성</name>
  <files>workers/election-night/test-parser.js, workers/election-night/fixtures/2022-sample.json</files>
  <read_first>
    - workers/election-night/index.js (Task 1에서 생성한 Worker -- parseNECResponse 함수 시그니처)
    - .planning/phases/04-election-night/04-RESEARCH.md (Pattern 6 -- JSON 스키마, Open Questions 1~2)
    - .planning/phases/04-election-night/04-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
**1. workers/election-night/fixtures/2022-sample.json 생성:**

2022년 8회 지방선거 확정 결과를 기반으로 파서 테스트용 fixture 생성. 이 fixture는 NEC 파싱 함수의 출력 스키마(입력이 아닌 출력)를 정의하는 "기대값"이다. 실제 NEC AJAX 응답 fixture는 2026-05-26 이전 Chrome DevTools 캡처 후 추가한다.

```json
{
  "_description": "2022년 8회 지방선거 광역단체장 확정 결과 (파서 출력 기대값). 출처: info.nec.go.kr 개표 결과",
  "_notice": "선관위 확정값 아님, 스키마 검증 전용 mock. 수치는 근사값이며 정밀 소수점은 NEC 원본 캡처 후 보정 예정. 이 fixture를 실제 선거 데이터로 사용하지 말 것 (헌법 제1조).",
  "fetchedAt": "2022-06-01T22:00:00+09:00",
  "electionId": "0020220601",
  "sgTypecode": "11",
  "regions": {
    "seoul": { "countRate": 100, "leadingCandidate": "오세훈", "leadingParty": "ppp", "leadingVoteRate": 59.0, "declared": true },
    "busan": { "countRate": 100, "leadingCandidate": "박형준", "leadingParty": "ppp", "leadingVoteRate": 66.5, "declared": true },
    "daegu": { "countRate": 100, "leadingCandidate": "홍준표", "leadingParty": "ppp", "leadingVoteRate": 74.3, "declared": true },
    "incheon": { "countRate": 100, "leadingCandidate": "유정복", "leadingParty": "ppp", "leadingVoteRate": 54.1, "declared": true },
    "gwangju": { "countRate": 100, "leadingCandidate": "강기정", "leadingParty": "democratic", "leadingVoteRate": 65.7, "declared": true },
    "daejeon": { "countRate": 100, "leadingCandidate": "이장우", "leadingParty": "ppp", "leadingVoteRate": 50.0, "declared": true },
    "ulsan": { "countRate": 100, "leadingCandidate": "김두겸", "leadingParty": "ppp", "leadingVoteRate": 55.0, "declared": true },
    "sejong": { "countRate": 100, "leadingCandidate": "최민호", "leadingParty": "ppp", "leadingVoteRate": 51.3, "declared": true },
    "gyeonggi": { "countRate": 100, "leadingCandidate": "김동연", "leadingParty": "democratic", "leadingVoteRate": 51.5, "declared": true },
    "gangwon": { "countRate": 100, "leadingCandidate": "김진태", "leadingParty": "ppp", "leadingVoteRate": 54.0, "declared": true },
    "chungbuk": { "countRate": 100, "leadingCandidate": "김영환", "leadingParty": "ppp", "leadingVoteRate": 51.7, "declared": true },
    "chungnam": { "countRate": 100, "leadingCandidate": "김태흠", "leadingParty": "ppp", "leadingVoteRate": 52.6, "declared": true },
    "jeonbuk": { "countRate": 100, "leadingCandidate": "김관영", "leadingParty": "democratic", "leadingVoteRate": 68.3, "declared": true },
    "jeonnam": { "countRate": 100, "leadingCandidate": "김영록", "leadingParty": "democratic", "leadingVoteRate": 84.0, "declared": true },
    "gyeongbuk": { "countRate": 100, "leadingCandidate": "이철우", "leadingParty": "ppp", "leadingVoteRate": 72.3, "declared": true },
    "gyeongnam": { "countRate": 100, "leadingCandidate": "박완수", "leadingParty": "ppp", "leadingVoteRate": 56.9, "declared": true },
    "jeju": { "countRate": 100, "leadingCandidate": "오영훈", "leadingParty": "democratic", "leadingVoteRate": 56.3, "declared": true }
  },
  "_source": "info.nec.go.kr",
  "_parserVersion": "1.0"
}
```

주의: 위 수치는 2022년 선관위 확정 결과를 정수로 근사한 fixture 데이터이다. 정밀 소수점은 NEC 원본 캡처 후 보정한다. fixture의 `_notice` 필드에 이 사실을 명시한다.

**2. workers/election-night/test-parser.js 생성:**

Node.js 단독 실행 (jest 없음, 프로젝트 패턴 준수). `node test-parser.js`로 실행.

**중요 (헌법 제1조 긴장 해소):** 테스트는 수치의 정확도(exact value)를 검증하지 않는다. 타입과 범위만 검증한다. 구체적으로:
- countRate: `typeof === 'number'` + `0 <= x <= 100` (정확한 값 비교 금지)
- leadingVoteRate: `typeof === 'number'` + `0 <= x <= 100` (정확한 값 비교 금지)
- leadingCandidate: `typeof === 'string'` + `length > 0` (이름 비교 금지)
- declared: `typeof === 'boolean'` (값 비교 금지)

테스트 항목:
1. **스키마 유효성**: fixture JSON의 모든 region이 필수 필드(countRate, leadingCandidate, leadingParty, leadingVoteRate, declared)를 갖는지 검증
2. **countRate 범위**: 0 <= countRate <= 100 (타입 + 범위만)
3. **leadingVoteRate 범위**: 0 <= leadingVoteRate <= 100 (타입 + 범위만)
4. **declared 타입**: boolean만 허용 (string "true" 금지)
5. **17개 광역지자체 완전성**: regions 키가 정확히 17개(seoul, busan, daegu, incheon, gwangju, daejeon, ulsan, sejong, gyeonggi, gangwon, chungbuk, chungnam, jeonbuk, jeonnam, gyeongbuk, gyeongnam, jeju)
6. **parseNECResponse stub 호출**: Worker index.js에서 parseNECResponse를 export하고, 빈 문자열 입력 시 빈 regions 반환 확인

테스트 출력 형식:
```
[PASS] 스키마 유효성 - 모든 region 필수 필드 존재
[PASS] countRate 범위 - 0~100 내
[FAIL] ... (실패 시 상세 메시지)
---
6/6 tests passed
```

테스트 파일 구조:
```javascript
const fs = require('fs');
const path = require('path');

// fixture 로드
const fixture = JSON.parse(fs.readFileSync(path.join(__dirname, 'fixtures/2022-sample.json'), 'utf8'));

const REQUIRED_REGIONS = ['seoul','busan','daegu','incheon','gwangju','daejeon','ulsan','sejong','gyeonggi','gangwon','chungbuk','chungnam','jeonbuk','jeonnam','gyeongbuk','gyeongnam','jeju'];
const REQUIRED_FIELDS = ['countRate','leadingCandidate','leadingParty','leadingVoteRate','declared'];

let passed = 0, failed = 0;
function test(name, fn) { /* try/catch, 결과 출력 */ }

// ... 6개 테스트
```

Worker index.js에서 `parseNECResponse`를 테스트에서 import 가능하도록 module.exports/export 처리가 필요하다. Worker는 ESM(`export default`)이므로 test-parser.js에서 직접 import가 어렵다. 대신 parseNECResponse 로직을 별도 함수로 정의하고, index.js 끝에 다음을 추가:
```javascript
// 테스트용 export (Worker 런타임에서는 무시됨)
export { parseNECResponse, NEC_CONFIG };
```

test-parser.js에서는 dynamic import 사용:
```javascript
const { parseNECResponse } = await import('./index.js');
```

만약 ESM import 이슈가 있으면 test-parser.js 자체를 .mjs로 만들거나, parseNECResponse 로직을 별도 parser.js로 분리하는 것도 가능. 실행 가능성을 우선시하여 판단.
  </action>
  <verify>
    <automated>node workers/election-night/test-parser.js</automated>
  </verify>
  <acceptance_criteria>
    - workers/election-night/fixtures/2022-sample.json에 17개 광역지자체 데이터 존재
    - fixture `_notice` 필드에 "선관위 확정값 아님, 스키마 검증 전용 mock" 문구 존재
    - 각 region에 countRate, leadingCandidate, leadingParty, leadingVoteRate, declared 필드 존재
    - test-parser.js가 수치 정확도(exact value)가 아닌 타입/범위만 검증
    - workers/election-night/test-parser.js가 `node workers/election-night/test-parser.js` 실행 시 0 exit code 반환
    - 테스트 출력에 최소 5개 [PASS] 행 표시
    - fixture에 hardcoded된 수치에 대해 `_description`에 출처("info.nec.go.kr 개표 결과") 명시 (헌법 제1조)
  </acceptance_criteria>
  <done>2022 아카이브 fixture와 파서 단위 테스트가 작성됨. `node workers/election-night/test-parser.js`가 모든 테스트를 통과한다. fixture _notice에 mock 데이터임이 명시됨. NEC 원본 HTML fixture는 2026-05-26 마감으로 별도 캡처 예정.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Worker 배포 + URL 확정</name>
  <files>workers/election-night/wrangler.toml</files>
  <action>
Worker를 Cloudflare에 배포하고 실제 URL을 확정한다. 이 URL은 04-02에서 브라우저 폴링 클라이언트가 사용한다.

**사전 조건:**
1. `wrangler.toml`의 KV namespace id가 실제 값으로 기입되어 있어야 함 (user_setup 참조)

**배포 단계:**
1. `cd workers/election-night && npx wrangler deploy` 실행
2. 배포 성공 시 출력되는 Worker URL 기록 (예: `https://election-night.<CF_SUBDOMAIN>.workers.dev`)
3. `curl https://election-night.<CF_SUBDOMAIN>.workers.dev/health` 로 200 응답 확인
4. 확정된 Worker URL을 04-02 실행 시 사용할 수 있도록 기록

**Worker URL 전달 방법:** 04-02 Task 1a의 `ELECTION_NIGHT_WORKER` 상수에 이 URL을 기입한다. 04-02 executor에게 이 URL을 전달하기 위해 04-01-SUMMARY.md에 Worker URL을 명시적으로 기록할 것.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://election-night.${CF_SUBDOMAIN}.workers.dev/health</automated>
  </verify>
  <done>Worker가 Cloudflare에 배포됨. /health 엔드포인트가 200을 반환함. Worker URL이 04-01-SUMMARY.md에 기록됨.</done>
  <resume-signal>Worker URL을 입력하세요 (예: https://election-night.xxx.workers.dev)</resume-signal>
</task>

</tasks>

<verification>
1. `grep -n 'ELECTION_NIGHT_END' js/election-calendar.js` -> DATES 내에 존재
2. `grep -n "election_night" js/election-calendar.js` -> getCurrentPhase()와 getBannerConfig()에 존재
3. `node workers/election-night/test-parser.js` -> 모든 테스트 PASS
4. `cat workers/election-night/wrangler.toml | grep crons` -> cron 트리거 확인
5. `grep -c 'declared' workers/election-night/index.js` -> declared 필드 참조 존재
6. `grep -c 'User-Agent' workers/election-night/index.js` -> D-07 준수
7. `curl -s https://<WORKER_URL>/health` -> 200 (배포 후)
</verification>

<success_criteria>
- election-calendar.js에 election_night 페이즈가 올바른 시간 범위(18:00~24:00 KST)로 동작
- Worker 골격이 scheduled() + fetch(/results, /health) 핸들러를 포함
- Worker가 NEC 요청 시 User-Agent 헤더를 명시(D-07)
- parseNECResponse stub이 올바른 JSON 스키마를 반환
- 2022 fixture 기반 테스트가 전수 통과 (타입/범위 검증, exact value 비교 없음)
- fixture _notice에 "스키마 검증 전용 mock" 명시
- 당선 확정은 선관위 공식 플래그만 신뢰하는 코드 구조 (D-11, 헌법 제2조)
- Worker가 Cloudflare에 배포되어 /health가 200을 반환
</success_criteria>

<output>
After completion, create `.planning/phases/04-election-night/04-01-SUMMARY.md`
반드시 SUMMARY에 배포된 Worker URL을 기록할 것 (04-02에서 참조).
</output>
