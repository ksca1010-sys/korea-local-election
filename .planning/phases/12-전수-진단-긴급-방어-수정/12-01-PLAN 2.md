---
phase: 12-전수-진단-긴급-방어-수정
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-전수-진단-긴급-방어-수정/12-AUDIT-REPORT.md
autonomous: true
requirements:
  - DIAG-01

must_haves:
  truths:
    - "15개 워크플로우 전체에 대한 감사 리포트가 존재하며, 각 워크플로우별 permissions/에러핸들링/출력검증 현황이 표로 정리됨"
    - "미처리 실패 패턴이 항목별로 목록화되어 Phase 13-14에서 참조 가능"
    - "fetch-disclosures 2.yml 중복 파일이 디스크에서 삭제됨"
  artifacts:
    - path: ".planning/phases/12-전수-진단-긴급-방어-수정/12-AUDIT-REPORT.md"
      provides: "15개 워크플로우 전수 감사 리포트"
      min_lines: 100
  key_links:
    - from: "12-AUDIT-REPORT.md"
      to: "12-RESEARCH.md"
      via: "RESEARCH.md 분석 결과를 구조화된 리포트로 정리"
      pattern: "워크플로우.*permissions.*에러핸들링"
---

<objective>
15개 GitHub Actions 워크플로우를 체계적으로 감사하여, permissions 블록, 에러 핸들링, 출력 검증 현황을 구조화된 리포트로 생성한다. 중복 워크플로우 파일(fetch-disclosures 2.yml)을 정리한다.

Purpose: Phase 13-14에서 참조할 잔존 문제 목록 확보 + 중복 파일로 인한 사고 방지
Output: 12-AUDIT-REPORT.md (감사 리포트)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-전수-진단-긴급-방어-수정/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 15개 워크플로우 전수 감사 리포트 생성</name>
  <files>.planning/phases/12-전수-진단-긴급-방어-수정/12-AUDIT-REPORT.md</files>
  <action>
12-RESEARCH.md에 이미 수집된 분석 결과를 기반으로 구조화된 감사 리포트를 작성한다.

리포트에 포함할 섹션:

1. **워크플로우 인벤토리** (15개 전체 목록, 각각):
   - 파일명, name, 트리거 방식 (cron/workflow_dispatch/workflow_run)
   - 핵심 Python 스크립트
   - permissions 블록 내용 및 최소권한 준수 여부

2. **에러 핸들링 현황 매트릭스** (표 형식):
   - 각 워크플로우별 continue-on-error 적용 여부
   - 핵심 스크립트 내 .get() vs 직접 접근 패턴
   - 빈 name 저장 방어 구현 여부

3. **미처리 실패 패턴 목록** (Phase 13-14 참조용):
   - 패턴 A: 직접 딕셔너리 접근 (KeyError 위험) — 대상 파일/라인 명시
   - 패턴 B: 빈 name 저장 위험 — 대상 파일 명시
   - 패턴 C: continue-on-error 미적용 워크플로우 목록
   - 패턴 D: 동시 git push 경쟁 상태 위험 워크플로우 목록

4. **권장 조치 우선순위** (Phase 12에서 처리 vs Phase 13-14로 이관):
   - Phase 12 즉시 처리: CRASH-01 (nec_precand_sync.py), CRASH-02 (빈 name 필터), PERM-01 (주석 정규화)
   - Phase 13 이관: continue-on-error 일괄 적용, git 경쟁 상태
   - Phase 14 이관: monitor_failures.py 커버리지 확대

RESEARCH.md의 워크플로우별 permissions 현황 표(15행), 에러 핸들링 현황 표, 미처리 실패 패턴 목록을 모두 포함하되, 리포트 형식으로 재구성한다. 추가로 각 워크플로우 YAML 파일을 직접 읽어서 트리거 방식, 핵심 스크립트 호출 부분을 확인하여 인벤토리를 완성한다.

워크플로우 파일 위치: .github/workflows/*.yml (15개, "fetch-disclosures 2.yml" 제외)
  </action>
  <verify>
    <automated>test -f .planning/phases/12-전수-진단-긴급-방어-수정/12-AUDIT-REPORT.md && wc -l .planning/phases/12-전수-진단-긴급-방어-수정/12-AUDIT-REPORT.md | awk '{if ($1 >= 100) print "PASS: " $1 " lines"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>12-AUDIT-REPORT.md가 100줄 이상이며, 15개 워크플로우 인벤토리 + 에러핸들링 매트릭스 + 미처리 패턴 목록 + 조치 우선순위가 포함됨</done>
</task>

<task type="auto">
  <name>Task 2: fetch-disclosures 2.yml 중복 파일 삭제</name>
  <files>.github/workflows/fetch-disclosures 2.yml</files>
  <action>
`.github/workflows/fetch-disclosures 2.yml` 파일을 삭제한다. 이 파일은 git-untracked 상태의 복사본으로, 실수로 커밋될 경우 동명 워크플로우가 2개 동시 실행되는 사고를 유발한다 (RESEARCH.md Pitfall 4 참조).

삭제 전 내용이 fetch-disclosures.yml과 동일한지 diff로 확인한다. 다르면 차이점을 12-AUDIT-REPORT.md에 기록한 후 삭제한다.

명령: `rm ".github/workflows/fetch-disclosures 2.yml"`
  </action>
  <verify>
    <automated>test ! -f ".github/workflows/fetch-disclosures 2.yml" && echo "PASS: duplicate deleted" || echo "FAIL: duplicate still exists"</automated>
  </verify>
  <done>fetch-disclosures 2.yml 파일이 디스크에서 삭제되어 중복 워크플로우 실행 위험이 제거됨</done>
</task>

</tasks>

<verification>
- 12-AUDIT-REPORT.md가 존재하고 15개 워크플로우 전체를 다룸
- fetch-disclosures 2.yml 파일이 삭제됨
- 리포트에 Phase 13-14 이관 항목이 명확히 구분됨
</verification>

<success_criteria>
1. 12-AUDIT-REPORT.md에 15개 워크플로우 인벤토리가 빠짐없이 포함됨
2. 미처리 실패 패턴이 4개 카테고리(KeyError/빈name/continue-on-error/git race)로 분류됨
3. fetch-disclosures 2.yml이 디스크에서 삭제됨
</success_criteria>

<output>
After completion, create `.planning/phases/12-전수-진단-긴급-방어-수정/12-01-SUMMARY.md`
</output>
