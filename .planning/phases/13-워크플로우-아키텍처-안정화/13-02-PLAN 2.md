---
phase: 13-워크플로우-아키텍처-안정화
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - .github/workflows/update-byelection.yml
  - .github/workflows/update-polls.yml
autonomous: true
requirements:
  - INDEP-02

must_haves:
  truths:
    - "update-byelection.yml 실행 후 validate_pipeline.py가 byelection.json 스키마를 검증한다"
    - "update-polls.yml 실행 후 polls.json 파싱 검증이 수행된다"
    - "검증 실패 시에도 커밋 단계까지 도달한다 (continue-on-error: true)"
  artifacts:
    - path: ".github/workflows/update-byelection.yml"
      provides: "validate_pipeline.py step added after factcheck"
      contains: "validate_pipeline"
    - path: ".github/workflows/update-polls.yml"
      provides: "inline JSON parsing validation step"
      contains: "Validate polls data"
  key_links:
    - from: ".github/workflows/update-byelection.yml"
      to: "scripts/candidate_pipeline/validate_pipeline.py"
      via: "workflow step"
      pattern: "python scripts/candidate_pipeline/validate_pipeline.py"
    - from: ".github/workflows/update-polls.yml"
      to: "data/polls/polls.json"
      via: "inline python validation"
      pattern: "json.load.*polls.json"
---

<objective>
주요 파이프라인(재보궐, 여론조사)에 스키마 검증 단계를 연결한다(INDEP-02). update-byelection.yml에는 기존 validate_pipeline.py를, update-polls.yml에는 인라인 JSON 파싱 검증을 추가한다.

Purpose: 파이프라인 실행 후 데이터 무결성을 조기 감지하여 오류 데이터 커밋을 사전 경고한다.
Output: 2개 수정된 워크플로우 YAML 파일 (validate step 추가)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-워크플로우-아키텍처-안정화/13-RESEARCH.md
@.planning/phases/13-워크플로우-아키텍처-안정화/13-01-SUMMARY.md

<interfaces>
<!-- validate_pipeline.py 구조 (scripts/candidate_pipeline/validate_pipeline.py) -->
<!-- DATA = BASE / "data" / "candidates" — 후보자 데이터 전용 -->
<!-- check_byelection() 함수가 data/candidates/byelection.json을 검증 -->
<!-- 오류 발견 시 sys.exit(1) 반환 — continue-on-error: true 필수 -->

기존 update-candidates.yml의 validate step 패턴:
```yaml
      - name: Validate pipeline data
        continue-on-error: true
        run: python scripts/candidate_pipeline/validate_pipeline.py
```

update-polls.yml 인라인 검증 패턴 (RESEARCH.md 옵션 B):
```yaml
      - name: Validate polls data
        continue-on-error: true
        run: python -c "import json; d=json.load(open('data/polls/polls.json')); print(f'polls: {len(d.get(\"polls\", []))}건 — JSON 파싱 정상')"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: update-byelection.yml에 validate_pipeline.py 연결</name>
  <files>.github/workflows/update-byelection.yml</files>
  <read_first>
    .github/workflows/update-byelection.yml (Plan 01 적용 후 현재 상태 확인),
    .github/workflows/update-candidates.yml (validate step 패턴 참조),
    scripts/candidate_pipeline/validate_pipeline.py (check_byelection 함수 존재 확인)
  </read_first>
  <action>
update-byelection.yml의 "Step 3 - Factcheck candidates" 스텝과 "Check for changes" 스텝 사이에 validate step을 추가한다.

추가할 스텝 (update-candidates.yml의 패턴과 동일):
```yaml
      - name: Validate pipeline data
        continue-on-error: true
        run: python scripts/candidate_pipeline/validate_pipeline.py
```

위치: Step 3 (factcheck) 이후, Check for changes (id: diff) 이전.
이유: factcheck 완료 후 데이터 무결성을 검증하고, 검증 실패해도 커밋 단계에 도달해야 하므로 continue-on-error: true 적용.

validate_pipeline.py는 data/candidates/byelection.json을 이미 검증하는 check_byelection() 함수를 포함하므로 별도 스크립트 작성 불필요.
  </action>
  <verify>
    <automated>cd /Users/isawufo/Desktop/AI-cording-project/korea-local-election && grep -c "validate_pipeline" .github/workflows/update-byelection.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/update-byelection.yml')); print('YAML 파싱 통과')"</automated>
  </verify>
  <acceptance_criteria>
    - grep "validate_pipeline" .github/workflows/update-byelection.yml 결과가 1줄 이상
    - validate step에 continue-on-error: true가 함께 있음
    - validate step이 factcheck 스텝 이후, diff 스텝 이전에 위치
    - YAML 파싱 성공
  </acceptance_criteria>
  <done>update-byelection.yml에서 파이프라인 실행 후 validate_pipeline.py가 byelection.json 스키마를 검증한다</done>
</task>

<task type="auto">
  <name>Task 2: update-polls.yml에 인라인 JSON 파싱 검증 추가</name>
  <files>.github/workflows/update-polls.yml</files>
  <read_first>
    .github/workflows/update-polls.yml (Plan 01 적용 후 현재 상태 확인),
    .planning/phases/13-워크플로우-아키텍처-안정화/13-RESEARCH.md (패턴 3: validate_pipeline.py 연결 방식 — 옵션 B)
  </read_first>
  <action>
update-polls.yml의 "Reparse empty results" 스텝과 "Check for changes" 스텝 사이에 인라인 JSON 파싱 검증 스텝을 추가한다.

추가할 스텝:
```yaml
      - name: Validate polls data
        continue-on-error: true
        run: python -c "import json; d=json.load(open('data/polls/polls.json')); print(f'polls: {len(d.get(\"polls\", []))}건 — JSON 파싱 정상')"
```

위치: Reparse empty results 이후, Check for changes (id: diff) 이전.
이유: validate_pipeline.py는 data/candidates/ 전용이므로 polls 데이터에 직접 적용 불가. 인라인 python -c로 polls.json 파싱이 성공하고 레코드 수를 출력하는 최소한의 검증을 수행한다.
continue-on-error: true 적용 이유: 검증 실패 시에도 커밋 단계에 도달해야 함 (INDEP-01 원칙 유지).

별도 validate_polls.py 작성 금지 — 스키마가 단순하므로 인라인으로 충분 (RESEARCH.md "Don't Hand-Roll" 참조).
  </action>
  <verify>
    <automated>cd /Users/isawufo/Desktop/AI-cording-project/korea-local-election && grep -c "Validate polls" .github/workflows/update-polls.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/update-polls.yml')); print('YAML 파싱 통과')"</automated>
  </verify>
  <acceptance_criteria>
    - grep "Validate polls data" .github/workflows/update-polls.yml 결과가 1줄
    - validate step에 continue-on-error: true가 함께 있음
    - validate step이 Reparse 스텝 이후, diff 스텝 이전에 위치
    - YAML 파싱 성공
  </acceptance_criteria>
  <done>update-polls.yml에서 파이프라인 실행 후 polls.json 파싱 검증이 수행된다</done>
</task>

</tasks>

<verification>
1. INDEP-02 검증 (byelection): `grep "validate_pipeline" .github/workflows/update-byelection.yml` → 1줄 이상
2. INDEP-02 검증 (polls): `grep "Validate polls" .github/workflows/update-polls.yml` → 1줄
3. 안전성: 두 validate step 모두 continue-on-error: true 적용
4. YAML 무결성: 2개 파일 yaml.safe_load() 통과
</verification>

<success_criteria>
- update-byelection.yml에 validate_pipeline.py 스텝이 factcheck 이후 diff 이전에 존재 (INDEP-02 충족)
- update-polls.yml에 인라인 JSON 파싱 검증 스텝이 reparse 이후 diff 이전에 존재 (INDEP-02 충족)
- 두 validate 스텝 모두 continue-on-error: true 적용 (INDEP-01 원칙 유지)
- 모든 YAML 파일 구문 유효
</success_criteria>

<output>
After completion, create `.planning/phases/13-워크플로우-아키텍처-안정화/13-02-SUMMARY.md`
</output>
