---
plan: 03-02
phase: 03-performance-features
type: execute
wave: 1
depends_on: []
files_modified:
  - js/tabs/poll-tab.js
  - js/app.js
  - js/election-calendar.js
  - index.html
autonomous: true
requirements:
  - FEAT-01
  - FEAT-02
  - FEAT-03
must_haves:
  truths:
    - "여론조사 탭에서 후보별 시계열 차트가 날짜 축(time scale)으로 렌더링된다"
    - "정보 패널 상단의 링크 복사 버튼 클릭 시 현재 지역+탭 URL이 클립보드에 복사된다"
    - "선거 일정 .ics 내보내기 버튼 클릭 시 캘린더 파일이 다운로드된다"
  artifacts:
    - path: "js/tabs/poll-tab.js"
      provides: "Chart.js time scale 시계열 차트"
      contains: "type: 'time'"
    - path: "js/app.js"
      provides: "링크 복사 버튼 핸들러"
      contains: "navigator.clipboard"
    - path: "js/election-calendar.js"
      provides: ".ics 내보내기 함수"
      contains: "BEGIN:VCALENDAR"
  key_links:
    - from: "js/tabs/poll-tab.js"
      to: "Chart.js time scale"
      via: "chartjs-adapter-date-fns CDN"
      pattern: "type.*time"
    - from: "js/app.js"
      to: "Router state"
      via: "현재 regionKey + tabName으로 URL 생성"
      pattern: "clipboard.writeText"
---

# Plan 03-02: 여론조사 트렌드 차트 + URL 공유 + .ics 내보내기

## Goal

여론조사 탭에 Chart.js time scale 시계열 차트를 추가하고, 정보 패널에 URL 공유 버튼과 선거 일정 .ics 내보내기 기능을 구현한다.

## must_haves

- poll-tab.js에서 `_buildPollTrendChart`가 `type: 'line'`을 반환할 때 x축이 `type: 'time'`으로 렌더링됨
- `chartjs-adapter-date-fns` CDN이 `index.html`에 추가됨
- app.js에 `copyShareLink()` 함수가 존재하고 `navigator.clipboard` + `execCommand` 폴백 포함
- election-calendar.js에 `exportICS()` 함수가 존재하고 RFC 5545 형식의 .ics 파일을 생성함

## Tasks

### Task 1: 여론조사 시계열 차트 (Chart.js time scale)

<read_first>
- js/tabs/poll-tab.js:347-468 — _buildPollTrendChart 로직, chart 타입 반환 구조
- js/tabs/poll-tab.js — renderPollChart 또는 차트 렌더링 함수 위치 (grep "new Chart" or "Chart(" 로 찾기)
- index.html:33 — Chart.js CDN 태그 확인
</read_first>

<action>
**FEAT-01: 여론조사 트렌드 차트**

1. `index.html`에 `chartjs-adapter-date-fns` CDN 추가 (Chart.js 태그 바로 다음):
   ```html
   <script defer src="https://cdn.jsdelivr.net/npm/chartjs-adapter-date-fns@3.0.0/dist/chartjs-adapter-date-fns.bundle.min.js" crossorigin="anonymous"></script>
   ```

2. `js/tabs/poll-tab.js`에서 차트 렌더링 함수 수정:
   - `_buildPollTrendChart`가 `{ type: 'line', datasetLabels }` 반환할 때의 차트 생성 코드를 찾는다
   - x축 설정을 `type: 'time'`으로 변경:
     ```js
     scales: {
       x: {
         type: 'time',
         time: {
           unit: 'day',
           displayFormats: { day: 'M/d' },
           tooltipFormat: 'yyyy-MM-dd',
         },
         title: { display: true, text: '조사일' },
       },
       y: {
         beginAtZero: true,
         title: { display: true, text: '지지율 (%)' },
         ticks: { callback: v => v + '%' },
       },
     }
     ```
   - 데이터셋의 `data` 배열을 `{ x: surveyEndDate 또는 publishDate, y: support }` 형태로 구성
   - 각 poll의 날짜는 `poll.surveyEndDate || poll.publishDate`에서 추출
   - 후보별로 그룹핑: `datasetLabels` 배열의 각 후보에 대해 polls를 순회하며 `results`에서 해당 후보의 support 값을 날짜별로 수집

3. 차트 옵션에 반응형 + 다크모드 지원:
   ```js
   plugins: {
     legend: { position: 'top' },
     tooltip: {
       callbacks: {
         label: ctx => `${ctx.dataset.label}: ${ctx.parsed.y}%`
       }
     }
   }
   ```

**주의:** poll-tab.js만 수정, 다른 탭 파일 미접촉. ElectionData를 통해서만 데이터 접근.
</action>

<acceptance_criteria>
- [ ] `grep -n "adapter-date-fns" index.html` 매치 1건
- [ ] `grep -n "type.*'time'" js/tabs/poll-tab.js` 매치 1건 이상
- [ ] `grep -n "surveyEndDate\|publishDate" js/tabs/poll-tab.js` 차트 데이터 구성에서 사용
- [ ] 여론조사 트렌드 차트가 있는 지역(예: 서울 광역단체장)에서 날짜 축이 M/d 형태로 표시
</acceptance_criteria>

### Task 2: URL 공유 버튼 + .ics 캘린더 내보내기

<read_first>
- js/app.js:587-600 — closePanel 근처, 패널 헤더 렌더링 위치 파악
- js/app.js — "share" 또는 "링크" 검색으로 기존 공유 코드 확인
- js/router.js — URL 상태 관리 방식 확인 (hash 기반 vs query 기반)
- js/election-calendar.js — electionCalendar 배열 구조, DATES 상수
</read_first>

<action>
**FEAT-02: URL 공유 버튼**

1. `js/app.js`에서 정보 패널 헤더를 렌더링하는 코드를 찾는다 (패널 제목 옆 또는 탭 바 근처)

2. 공유 버튼 HTML 삽입:
   ```html
   <button class="share-btn" onclick="App.copyShareLink()" title="링크 복사">
     <i class="fas fa-link"></i> 링크 복사
   </button>
   ```
   - 패널 헤더 영역(제목 오른쪽)에 배치

3. `App` IIFE 내부에 `copyShareLink()` 함수 추가:
   ```js
   function copyShareLink() {
     const url = window.location.href; // Router가 이미 hash에 상태를 반영
     if (navigator.clipboard && navigator.clipboard.writeText) {
       navigator.clipboard.writeText(url).then(() => {
         _showCopyToast('링크가 복사되었습니다');
       }).catch(() => _fallbackCopy(url));
     } else {
       _fallbackCopy(url);
     }
   }
   function _fallbackCopy(text) {
     const ta = document.createElement('textarea');
     ta.value = text;
     ta.style.position = 'fixed';
     ta.style.opacity = '0';
     document.body.appendChild(ta);
     ta.select();
     document.execCommand('copy');
     document.body.removeChild(ta);
     _showCopyToast('링크가 복사되었습니다');
   }
   function _showCopyToast(msg) {
     const toast = document.createElement('div');
     toast.className = 'copy-toast';
     toast.textContent = msg;
     document.body.appendChild(toast);
     setTimeout(() => toast.remove(), 2000);
   }
   ```
   - return 객체에 `copyShareLink` 추가

4. `css/style.css`에 `.share-btn` 스타일 추가 (간결한 아이콘 버튼 + hover):
   ```css
   .share-btn { background: none; border: 1px solid var(--border-color); border-radius: 4px; padding: 4px 8px; cursor: pointer; color: var(--text-color); font-size: 0.8rem; }
   .share-btn:hover { background: var(--hover-bg); }
   .copy-toast { position: fixed; bottom: 20px; left: 50%; transform: translateX(-50%); background: var(--accent-color); color: #fff; padding: 8px 16px; border-radius: 4px; z-index: 9999; animation: fadeInOut 2s; }
   @keyframes fadeInOut { 0% { opacity: 0; } 10% { opacity: 1; } 80% { opacity: 1; } 100% { opacity: 0; } }
   ```

**FEAT-03: .ics 캘린더 내보내기**

5. `js/election-calendar.js`에 `exportICS()` 함수 추가 (ElectionCalendar IIFE 내부):
   - `electionCalendar` 배열 (또는 `ElectionData.electionCalendar`)에서 주요 일정을 RFC 5545 형식으로 변환
   - DTSTART/DTEND는 `YYYYMMDD` 형식 (종일 이벤트)
   - 파일명: `election-schedule-2026.ics`
   ```js
   function exportICS() {
     const events = ElectionData.electionCalendar || [];
     let ics = 'BEGIN:VCALENDAR\r\nVERSION:2.0\r\nPRODID:-//AlSeonGeo//Election//KO\r\nCALSCALE:GREGORIAN\r\n';
     events.forEach(ev => {
       const dtStart = (ev.startDate || '').replace(/-/g, '');
       const dtEnd = ev.endDate ? ev.endDate.replace(/-/g, '') : dtStart;
       if (!dtStart) return;
       ics += 'BEGIN:VEVENT\r\n';
       ics += `DTSTART;VALUE=DATE:${dtStart}\r\n`;
       ics += `DTEND;VALUE=DATE:${dtEnd}\r\n`;
       ics += `SUMMARY:${(ev.title || '').replace(/[,;\\]/g, ' ')}\r\n`;
       ics += `DESCRIPTION:${(ev.description || '').replace(/[,;\\]/g, ' ').replace(/\n/g, '\\n')}\r\n`;
       ics += `UID:${ev.id || dtStart}@alseonger\r\n`;
       ics += 'END:VEVENT\r\n';
     });
     ics += 'END:VCALENDAR\r\n';
     const blob = new Blob([ics], { type: 'text/calendar;charset=utf-8' });
     const a = document.createElement('a');
     a.href = URL.createObjectURL(blob);
     a.download = 'election-schedule-2026.ics';
     a.click();
     URL.revokeObjectURL(a.href);
   }
   ```
   - return 객체에 `exportICS` 추가

6. 내보내기 버튼 배치 — `js/app.js` 또는 `js/sidebar.js`에서 선거 캘린더 섹션 근처에:
   ```html
   <button class="ics-export-btn" onclick="ElectionCalendar.exportICS()" title="캘린더에 추가">
     <i class="fas fa-calendar-plus"></i> 일정 내보내기
   </button>
   ```

**주의:** election-calendar.js의 DATES 상수는 수정하지 않음. isPublicationBanned() 미접촉.
</action>

<acceptance_criteria>
- [ ] `grep -n "copyShareLink" js/app.js` 함수 정의 + return 객체 포함
- [ ] `grep -n "navigator.clipboard" js/app.js` 매치 1건
- [ ] `grep -n "execCommand.*copy" js/app.js` 폴백 매치 1건
- [ ] `grep -n "share-btn" js/app.js` 버튼 HTML 포함
- [ ] `grep -n "BEGIN:VCALENDAR" js/election-calendar.js` 매치 1건
- [ ] `grep -n "exportICS" js/election-calendar.js` 함수 정의 + return 객체 포함
- [ ] `grep -n "ics-export-btn" js/app.js` 또는 sidebar.js에 버튼 존재
</acceptance_criteria>

## Verification

```bash
# FEAT-01: time scale adapter
grep -n "adapter-date-fns" index.html
grep -n "type.*'time'" js/tabs/poll-tab.js

# FEAT-02: share button
grep -n "copyShareLink" js/app.js
grep -n "clipboard" js/app.js

# FEAT-03: .ics export
grep -n "BEGIN:VCALENDAR" js/election-calendar.js
grep -n "exportICS" js/election-calendar.js
```
